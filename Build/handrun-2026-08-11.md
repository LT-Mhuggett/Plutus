# Hand-run findings — 2026-08-11, reported on till 1.41.0, fixes in **1.42.0**

**Matt's first real shop-day run of the post-Syncfusion till.** Every item below is his, in his
order, with my triage attached. ⚠ **Nothing here is closed until he says so** — I have marked what I
believe is fixed, never that it is verified.

This page exists because the last hand-run's findings lived in a chat message and had to be
re-derived twice. Findings go here first, then into `till-design.md` Part B when they close.

---

## The ones that stop a shop

| # | Finding | State |
|---|---|---|
| **A** | ⚠⚠ **The till CRASHES when you click into the Inventory search bar.** Reproducible. | 🟡 **fixed in 1.42.0 — re-test** |
| **B** | ⚠⚠ **A sale can be taken after the day is Z-CLOSED** | ✅ **BOTH GATES IN — till 1.42.0 + backend 1.10.0 (DEPLOYED)** |
| **C** | ⚠ **An item just SOLD cannot be added back to the basket** | 🟡 **fixed in 1.42.0 — re-test** |

### A — the search path had no exception guard, and FOCUS ALONE rebuilt the list

⚠ **My first theory was wrong; the real cause is plainer.** `InitItems` wraps its `RebuildGroups()`
call in a try/catch. **The search path did not.** Identical code was survivable on load and fatal on
a keystroke — anything escaping reached the MAUI dispatcher as an unhandled exception and took the
process with it.

⚠ **And clicking in was enough.** A MAUI `SearchBar` on WinUI writes `Text` on FOCUS, moving it from
`null` to `""` — a change as far as `SetProperty` is concerned. So merely clicking into the box
rebuilt the whole grouped collection under a rendered `CollectionView`, before a character was typed.

Three changes: blank/null `SearchText` is normalised so focus is a no-op; `RebuildGroups` is
guarded; and `ItemGroups` is now **replaced wholesale** rather than `Clear()`-then-`Add()` — one
notification instead of a Reset plus N Adds against a grouped view.

### B — nothing anywhere gated a SALE on the day being open

⚠⚠ **CORRECTION TO WHAT I FIRST TOLD MATT: the sale WAS captured.** It committed locally, drained
normally, and the **server accepted it as 201 Recorded** — against a day already counted and banked.
The day-closed gate existed only on the **cash-event** path, till and server. The sales path never
asked.

⚠ **That is worse than losing it.** A lost sale is one problem; a sale added to a reconciled day
means the Z-read, the banking and the platform's figures disagree **for ever**, with nothing
flagging it. It surfaces weeks later as an unexplained discrepancy.

Gated in `CheckoutCommit.CommitAsync` — every sale and refund passes through it, and it already owns
the "refuse rather than record something wrong" decision. ✅ **The server now has its own gate too** — `ValidateDayNotClosedAsync` in `SalesIngestService`,
deployed as backend 1.10.0. ⚠ **QUARANTINED (202), not rejected**: the sale is real, somebody paid
and left with the goods, so it must not vanish — quarantine keeps the money visible and puts it in
front of a person. `OutboxPusher` treats 202 as terminal, so nothing loops.

⚠ **Two gates, one rule, and they protect different things.** The till's refuses before money is
taken, with the basket intact — that protects the OPERATOR. The server's protects the LEDGER,
because a till cannot be the only thing enforcing a rule about the platform's own books: an older
build, a replayed queue or a second device on the same till all reach the endpoint without passing
through the till's check.

### C — a dangling basket selection incremented a ghost

⚠ `SelectedBasketRecord` is bound to the basket list's selection and **was not cleared when the
basket emptied after a sale**. It kept pointing at the `BasketItem` for the line just sold — an
object no longer in `Basket`. Scanning that item matched it, incremented a **detached object**, and
added nothing. No error, no line, and the scan box cleared as though it had worked.

⚠ **It could only ever affect the item just sold** — exactly what Matt hit, and exactly what makes it
look like the item is broken rather than the till.

Guarded on `Basket.Contains(...)` so it is structurally impossible however the selection comes to
dangle, and the selection is nulled wherever its line goes away. ⚠ **The same bug is in
`Plutus.Frontend.ClientUI`** and is fixed there too — ⚠ **but that project will not build from the
CLI on this box** (its Windows target is gated on Visual Studio's MSBuild), so that edit is
**verified by reading, not by a compile.**

---

## Wrong words in front of a customer

| # | Finding | Wanted |
|---|---|---|
| **D** | Card over-payment said **"Something went wrong"** | 🟡 **fixed in 1.42.0.** ⚠ It was a CRASH, not a wording choice: that string is the checkout's `catch`. The amount prompt was the one dialog in the tender loop NOT going through `Modal`, so a refusal looped from its teardown into the next dialog and WinUI threw a COMException |
| **E** | Insufficient cash said the same generic thing | 🟡 **fixed in 1.42.0.** `TenderLoop` now reports WHY it refused and the screen says it. ⚠ Over-paying in CASH stays silent — cash gives change, so £20 for £3.30 is an ordinary sale |

⚠ **"Something went wrong" is the failure, not the wording.** It is what a screen says when nobody
decided what should happen — and both of these are ordinary, expected operator actions, not errors.

---

## Missing capability

| # | Finding | Note |
|---|---|---|
| **F** | ⚠ **Split payments are not offered.** "I need this on the webtill too. The functionality needs to be the same." | ✅ **CLOSED — they already work on BOTH tills, and nothing needed writing on either.** ⚠ **I twice reported this wrongly**: first that the web half was "a Part B row", then that it was "written but uncompiled". Neither was true — the web till has had split payments all along and I changed no code in it. Verified by trace: **MAUI** is a sequential loop (`TenderLoop.RunAsync`) whose prompt names the remainder verbatim — *"How much to pay with {0}, There is {1} left to pay"* (`Strings.resx:477`). **Web till** is a different interaction model, not a lesser one: `CheckoutDialog.tsx` puts EVERY method on one screen with its own amount box, a live **"Remaining"** figure, a **`rest`** quick-fill button, and Complete disabled until the amounts sum exactly to the basket. Both emit multiple `IngestTender` rows on one sale (`api.ts:1193`). ⚠ **Two genuine asymmetries found while checking, neither of them "split payments":** MAUI can take the SAME method twice (two card tenders); the web till cannot, being one row per method. And MAUI **explains** a refusal (zero / wrong direction / overpay without change) where the web till just greys the button out with no sentence. ⚠⚠ **And one MONEY finding — see the note below.** |
| **G** | ⚠ **Refunds must offer ONLY the tender the sale was paid with.** "If it was a card payment, needs to go back to card." | ✅ **Fixed in 1.43.0.** `TillTenders.OfferedForRefund` filters the sheet to the tenders on the ORIGIN sale, read from this till's own stored payload (no network — a refund must work with the line down). ⚠ A SPLIT sale offers both. ⚠ An UNKNOWN origin (cross-till, or older than local history) offers everything — a guard rail, not a lock; refusing there would block a legitimate refund over a fact the till does not have. ⚠ So does an origin paid with something the till no longer offers, or the operator is trapped with no button. 8 tests, 3 mutations |
| **H** | **Stock write-offs are not reported anywhere.** "Where is this captured? I need a report on the portal (that will then be reflected in all tills) that shows stock adjustments" | ✅ **Server done + tested in backend 1.12.0; portal table written but UNBUILT (needs the Mac).** ⚠ **It was always captured** — every write-off has been a `StockMovement` row with its reason, its actor and its timestamp since WP5.1. Nothing was lost. `/api/v1/stock/movements` just could not be READ as a report: it is a per-ITEM drill, wants an `itemIdOne`, has no date range and returns raw GUIDs, so *"who has been writing stock off this month?"* — the question shrinkage is found by — had no answer. New `GET /api/v1/stock/adjustments`: date-ranged, **manual movements only** (`Adjustment` + `WriteOff`; sales and goods-in would bury them), and **names rather than GUIDs** — a report about accountability that cannot say who is not one. ⚠ An actor it cannot resolve shows as **"unknown"**, never dropped: those rows are the ones most worth seeing. ⚠ A capped page **says so** — a silently truncated list of write-offs reads as "that is all of them". 8 tests, 2 mutations. ⚠⚠ **And it exposed a live problem of my own making — see the Note below** |
| **I** | **Z-close variance is not flagged.** Open £150, spend £20, close £110 → should be flagged, and there should be a portal report/warning for a till that closed short | ✅ **Fixed in till 1.45.0 + backend 1.11.0 + portal 1.4.0.** ⚠ **The arithmetic was never missing** — the platform has computed expected-vs-counted since WP9 and Banking has shown it in red since §9.2. What was missing was anyone being TOLD: the till kept the POST's status code and **discarded the response body**, which is the only place either figure exists, so a drawer £20 short was accepted with a 201 in silence. Now recorded on the local row (schema **v6**) and shown on the Cash line and in the summary header — red, with SHORT/OVER and Plutus's expected figure. ⚠ **A new rule fell out of it: the Z now WAITS for its own day's sales.** Expected = float + **cash takings** + ins − outs, so a Z pushed ahead of queued sales reports a shortage equal to every penny not yet sent — a till that traded £400 through an outage would tell the operator who counted correctly that they were £400 down. And the Z is terminal server-side, so it would also land the day's real sales in quarantine behind their own close. ⚠ Portal: a **⚠ Drawers out of balance** pill, 7-day window, **short and over summed SEPARATELY** — netting them would report one till £300 short and another £300 over as a quiet week. ⚠ **The pill's TSX is unbuilt** — no Node on this machine; it compiles on the Mac with the other web work |

---

## Wrong on the screen

| # | Finding | Note |
|---|---|---|
| **J** | **Cash tab does not refresh.** "The open float was 'Waiting' and never updated. I navigated away and back onto the cash tab and it had updated." | ✅ **Fixed in 1.44.0.** A redraw problem, not a sync one — the data was right and the drain worked. `TillCadence` gained a `Ticked` event and the Cash screen subscribes while it is up. ⚠ **Raised from `TickAsync`, not from the loop** — the cash screen *nudges* a tick the instant it records a float, and a raise in the loop would have missed exactly that one and left the label wrong for up to a further minute. ⚠ **In a `finally`, so a tick that THREW still redraws** — "still waiting to send" is true, and holding the last render up because the network was down is how a stale figure outlives the fault behind it. ⚠ **Found while fixing it: the Reporting tab was worse** — see N |
| **N** | ⚠⚠ **Today's takings were read ONCE, at sign-in, and never again.** Not reported — found while fixing J. | ✅ **Fixed in 1.44.0.** `AppShell` builds every tab up front, so `LoadToday()` ran from a constructor at sign-in and the figure never moved for the rest of the day. ⚠ **This is worse than J and had no symptom.** A stuck "(waiting to send)" announces itself; a takings total that is eight hours stale looks exactly like a correct one — and it is the number a manager counts a drawer against. Same fix: refresh on appearing, and on every tick while the screen is up |
| **K** | ⚠⚠ **Edit item: "I can ONLY change the tax?"** — reported 2026-08-10 and AGAIN 2026-08-11 with a screenshot: *"When I right click and edit an item, it just gives me the tax rates still."* | ✅ **Actually fixed in 1.46.0. ⚠ MY FIRST TWO DIAGNOSES WERE BOTH WRONG.** I called it a form clipped below the fold and shipped a `ScrollView` (1.42.0). That never fitted the symptom: clipping from the bottom would hide **Tax band** (row 6) and show Name/Brand/Price (rows 1–5) — the exact opposite of what was reported. Matt's screenshot settled it. **TWO real defects, both confirmed by three independent adversarial reviewers:** ⚠ **(1) `InputAlert.CreateLabelEntry` gave an ENABLED field its value as a `Placeholder` and a DISABLED field its value as `Text`** — so the five rows you CAN edit (Name, Brand, Description, Cost, Price) rendered as empty boxes with grey ghosts, and the three deliberately READ-ONLY rows (Tax band, Category, Stock) were the only ones showing anything solid. The form genuinely looked like a tax viewer with five blanks above it. It also **lost edits in silence**, because `InputResults` reads `entry.Text` and a placeholder is not text: changing only the price submitted an EMPTY name and hit a bare `return`. Fixed with an opt-in `PrefillWithPlaceholder` — **not** a global flip, or the login screen would show "Email" inside the email box. ⚠ **(2) "Edit item" opens THREE action sheets before the form** (tax → category → stock → form), and **nothing said so**. A bare sheet headed "Tax band" reads as the whole feature, so backing out of step 1 was the rational move — and `:608`'s `if (taxId is null) return;` then threw the entire edit away with no message and no log entry. Every step is now titled **"Edit item — step N of 4"**, and the empty-name path says why it refused. 1 test, 1 mutation |
| **L** | ⚠ **"Delete item" exists and should not.** "This should be controlled on the portal." | ⚠⚠ **THERE IS NO DELETE-ITEM ANYWHERE IN THE TILL — I searched the whole app.** The only "Delete…" is in the CATEGORIES flow and it deletes a CATEGORY. On a screen reached from a list of items, a bare "Delete…" reads as deleting the item you were just looking at, so every label now says CATEGORY and the confirmation states plainly that items cannot be deleted from a till at all. ⚠ **Matt — if you saw it somewhere else, tell me where.** The legacy `AddEditView` is unreachable and has no delete either |
| **L** | ⚠ **"Delete item" exists and should not.** "This should be controlled on the portal." | ✅ **CLOSED 2026-08-11 — Matt found it himself: *"The delete now looks like 'Move to bin'."*** He was looking at **"Move to the Bin…"** on the right-click sheet, which is what my search kept finding and reporting as "not a delete". It never was one. ⚠ **Item delete is STRUCTURALLY IMPOSSIBLE, not merely absent**: `ItemController` inherits the **CRU** base, not CRUD, so there is no route to delete an item — verified across the MAUI till, legacy `AddEditView`, `Plutus.Frontend.ClientUI`, the web till and the portal. The Bin stamps `Item.BinnedAtUtc` server-side (`InventoryBulkController:148`), keeps the barcode every sale line and stock row keys on, and is reversible from **Portal → Inventory → Bin → Restore**. The confirmation already said so. ⚠ **What WAS wrong: the success alert was titled "Hmm"** — the app's generic error word — so the one dialog confirming it had worked looked like the four that report failure. Now titled **"Done"** and it repeats that nothing is deleted and where to restore it (1.46.0) |

---

## Not yet testable

**§5.6 (printer) and §6 (offline trading)** — *"I can't test this now."* Both still open.

---

## Where this stands

✅ **Fixed in 1.42.0, all needing Matt's re-test: A, B (till half), C, D, E, K.** F is answered —
split payments already work.
✅ **Fixed in 1.43.0: B (server half), G, L (wording), M.**
✅ **Fixed in 1.44.0: J — and N, which nobody had reported.**
✅ **Fixed in 1.45.0 (+ backend 1.11.0, portal 1.4.0): I.** ⚠ Its portal pill needs the Mac to build.

⚠ **Still open, in the order I intend to take them:**

| # | What | Why it is not done yet |
|---|---|---|
| **B server half** | The server accepts a sale for a Z-closed day | Needs a quarantine in `SalesIngestService`, matching the two-gates-one-rule shape `till-design` already claims |
| **G** | Refunds must offer only the original tender | Medium: the tender is on the stored payload, so it is a read plus a filtered picker |
| **L** | Remove "Delete item" | Small |
| **M** | Rename the tabs to match the web till | Small |
| **F web half** | **Split payments on the WEB till.** "I need this on the webtill too. The functionality needs to be the same." | ⚠⚠ **NOT WRITTEN — I wrongly reported it as written-but-uncompiled.** The MAUI half works and is tested; the web till has had no change at all this session. This is the one open item from the hand-run that is still code-to-write rather than code-to-verify |

⚠ **The pattern in this list is worth naming.** Almost none of these are logic errors — the tender
loop, the stock ledger, the Z variance and the split-payment rule are all built and tested. What is
missing is a SCREEN that offers them, or a SENTENCE that explains them. That is the same shape as
every previous hand-run: the components exist and nothing calls them.

---

## ⚠⚠ Note — writing H's test found a fault I had shipped into the test host

`RolePermissionReconciler` (added 2026-08-11 so permissions reach a tenant on BOOT, after Matt asked
why a manual seed step was needed at all) is a `BackgroundService` that **enumerates `Tenants` on
startup**. That is right in production. On the integration host it is not: `PlutusAppFactory` runs
every context on **one shared in-memory SQLite connection**, which is single-writer and not
thread-safe, and the reconciler's first query races the startup `EnsureCreated`.

⚠ **The symptom points nowhere near the cause.** It surfaces as
`SQLite Error 1: 'no such table: Tenants'` wrapped in *"a transient failure — consider enabling
`EnableRetryOnFailure()` on the `UseMySql` call"*, which reads as a connection-pool problem in a
suite that has no pool and no MySQL.

⚠ **And it was already intermittent.** The factory has always removed three hosted services for
exactly this reason and the reconciler was never added to the list, so whether it bit depended on
which class booted the host and how quickly its first request arrived. The suite stayed green until
`StockAdjustmentReportE2eTests` shifted the timing and took out **all eight** of its tests at once —
including two that touch almost no data, which is what gave it away. Now removed, with the reason
written where the next person will read it.

⚠ **The lesson is the one this file keeps repeating.** A green suite proves the components work; it
does not prove the harness is sound. This had been latent for hours behind 161 passing tests.

---

⚠ **J and N are that shape at its purest, and N is the warning.** The cadence knew the queue had
drained and the platform knew the day's takings; in both cases the fact was correct and no screen
was told. J was reported within an hour of a shop opening because a stuck "(waiting to send)"
announces itself. N — a takings total frozen at sign-in — was found only because I went looking for
siblings of J, and it would never have been reported at all, because a wrong total looks exactly
like a right one. **When a redraw bug turns up, search for the others straight away.** The one that
gets reported is rarely the one that costs the most.

---

## ⚠ The web till never charges the card surcharge — ALREADY A DECLARED GAP, and I raised it as if it were not

Found while verifying **F**. ⚠⚠ **I reported it as an undocumented money bug. It was neither
undocumented nor, in all likelihood, a live bug — and I should have read the register before raising
it.** `till-design.md:188` has carried the row since the day the feature shipped:

> `| **Card surcharge** … | ⬜ | ✅ | … |` — *"⚠ **Web till ⬜, deliberately**: it reads
> `charge`/`minimumCharge` off the legacy wire and ignores them — no tenant charges a fee today (UK
> consumer surcharges are BANNED since 2018-01-13; the portal helper text carries the warning), so
> the web half rides **WP15**'s checkout reshape rather than a one-off TS edit nothing can typecheck
> on this box."*

C1 says the same (`:267` — *"web till takes it with WP15"*). **This is Part B working exactly as
designed**: a ⬜ with a reason and a work-package number, which is the whole point of D3. My alarm
was the failure, not the gap.

### What the history actually says

`3cd4e6b`, **2026-08-09** — *"Card surcharge: tenant-set, VAT follows the basket, helper text says
so"*. It touched the migration, the entities, `GatewayConfigController`, `SharedKernel/CardSurchargeVat.cs`,
the **portal** (`CompanyPage.tsx`), and the **MAUI till** (`CheckoutCommit.cs`, `GatewaySurcharge.cs`,
`TillViewModel.cs`), with 17 + 87 tests. It touched **no `Plutus.Frontend.WebApp` file at all.**

⚠ **So Matt's recollection is right that it is a late addition that was not applied everywhere — but
the direction is inverted.** It WAS retrofitted to the MAUI till, on day one, by the commit that
built it. The **web till** is the one that never got it. That is worth stating plainly because it
inverts what needs building, and because it is the reverse of this project's usual drift: the whole
premise of the retrofit is that MAUI lags the browser, and here the browser lags MAUI.

### ⚠ One leg of the WP15 justification no longer holds

The row defers the web half partly because a *"one-off TS edit nothing can typecheck on this box"*
was not worth making. **That constraint is gone as of 2026-08-11**: the Mac builds the web till on
Node 26, and `Plutus.Frontend.WebApp` now has vitest and 19 tendering tests, so a TypeScript change
here can be typechecked, tested and built like any other. The remaining reasons stand — no tenant
charges a fee, consumer surcharges are illegal in the UK, and WP15 reshapes this checkout anyway —
but "we cannot verify it" is no longer one of them.

### What is still genuinely unverified

**✅ NOTHING — Matt checked, 2026-08-11: the rate is ZERO.** So there is no live discrepancy and never
has been; the web till's missing surcharge is a latent trap for a future B2B tenant only, and stays
with WP15 exactly as the register said. Original note, for the record: I could not read it: the only route from here was extracting the DB
password out of the pm2 environment, which the sandbox refused, and it was right to. One glance
settles it — **Portal → Company → Card surcharge**. Given consumer surcharges have been banned in
the UK since 2018-01-13 and Kapow sells to consumers, it should be **zero and stay zero**; the risk
is a future B2B tenant, not this one.

**Status: no change of plan.** The web half stays with **WP15**. What came out of this that was worth
having is the tendering twin's first tests — see the commit `test(web till): the tendering twin
finally has tests`.
