# Till design — the single source of truth for every till build

**Every till Plutus ships is designed here.** What the surfaces are, what each can do, and where
every shared rule lives. If a question is about a till, the answer is in this document or it belongs
in this document.

> ### The binding rule
>
> **Any work on any till reads this document first and updates it in the same commit.**
>
> **1. Parity is the default — new functionality ships to EVERY till.** Not to the till that
> happened to prompt it. A till left behind is a *decision*, and a decision has a name and a
> number: a ⬜ in Part B whose Notes say which work package will close it and why it can wait.
> "We'll do the other one later" may be written down exactly once — as a WP reference. If there
> is no WP, the feature is not finished.
>
> **2. A capability isn't done** until its row exists in Part B with every till marked ✅, a
> deliberate ⬜, or ➖.
>
> **3. A rule isn't done** until Part C says where it lives — and, if it exists in more than one
> place, what stops the copies drifting.
>
> This is what keeps till versions in sync. Not memory, not review, not good intentions.
>
> *Matt, 2026-08-08:* "**The tills need to be in parity. This is the point of the MAUI retrofit.**
> In addition when adding new functionality, it needs to be added to all tills going forward."

**Legend:** ✅ built · 🟡 partial · ⬜ absent · ➖ not applicable · ⏸ blocked externally.
Last verified against code **2026-08-08**. Retrofit WPs refer to
[`MAUI-retrofit.md`](To%20do/MAUI-retrofit.md).

*Consolidated 2026-08-08 from `till-parity.md` (features) and `till-anatomy.md` (build + rules) at
Matt's instruction — the two questions were always one question, and splitting them meant a till
could look complete in one document while being wrong in the other.*

---

## Why this document exists

Two failures, a week apart, and neither was carelessness.

**The web till gained nine features in eight days** at the end of July. The MAUI parity analysis
written on 2026-08-01 was already missing several of them by 2026-08-07 — because there was nowhere
for a gap to show up. Prose written once decays; a table every feature has to touch does not.
*That's Part B.*

**The VAT arithmetic existed only in the web till's TypeScript**, described in the retrofit plan as
"the reference implementation" — meaning every other till was expected to re-derive it by reading
someone else's language and getting it right. There were already three partial copies, including a
hardcoded UK VAT band list inside a client library. A feature register could never have shown this:
it read ✅ / ✅, because both tills *do* have VAT. Two tills that disagree by a penny on the same
basket disagree on **every VAT return, forever**, and nothing flags it. *That's Part C.*

---

# Part A — What the tills are

## A0. ⚠⚠ FUNCTIONAL PARITY AT A GLANCE — **can the till do the thing?**

> **Matt, 2026-08-17:** *"Can there be a table in there showing the parity across the tills, showing
> parity in FUNCTIONALITY, not in how the functions operate."*
>
> ⚠⚠ **AND ON 2026-08-19 HE WIDENED IT, WHICH THIS TABLE CANNOT EXPRESS:** *"I need the functionality
> and look and feel to be the same across both tills. So if a user swaps between the two, it doesnt
> matter and they would understand how to use it."* The test is now an operator moving between tills
> mid-shift with no retraining — so **"how the functions operate" is back in scope**, and a ✅/✅ row
> whose two tills look nothing alike IS a gap. This table still answers only its own one question; say
> the rest in Part B's Notes. ⚠ The first casualty of the old reading: §5c item 2, where MAUI's
> sequential tender prompts were defended as functional parity with the web till's one screen. They are
> not parity any more.
>
> So this table answers **one** question per row: *can an operator do this on that till?* It says
> nothing about how, which endpoint, which rule, or what went wrong on the way — all of that is
> **Part B**, and Part B wins on any disagreement. Read this to see the shape of the gap; read Part B
> before changing anything.
>
> ⚠ **Deliberately NOT a list of screens.** "Both have a Reports tab" can be true while one of them
> cannot answer what yesterday's VAT was. The rows below are things a shop needs done.

**Key:** ✅ works · 🟡 **built, never run by a person** · ⬜ cannot do it · ➖ deliberately not applicable

⚠⚠ **🟡 IS NOT A SOFT ✅.** It means the code exists and passes every test a machine can run, and **no
human has ever seen it work.** MAUI carries a lot of it right now because 21 versions have landed since
the last hand-run — and every hand-run so far has found faults the tests could not (2026-08-10: fourteen,
six invisible to every automated test). Treat 🟡 as unknown until [`Test Maui.md`](Test%20Maui.md) says
otherwise.

### Selling

| Can the till… | Web | MAUI |
|---|---|---|
| Ring up an item by scanning it | ✅ | ✅ |
| Find an item by name or part of a name | ✅ | ✅ |
| Change a line's quantity | ✅ | ✅ |
| Put an item back (return it) | ✅ | ✅ |
| Add a note to the sale | ✅ | ✅ |
| Take a fixed-amount discount off | ✅ | ✅ |
| Take a **percentage** discount off | ✅ | 🟡 |
| **Type an ad-hoc £/% discount with no catalogue row** | 🟡 | ✅ |
| **Discount CHOSEN lines, or every line, in one go** | 🟡 | 🟡 |
| **Apply a scheduled discount (day/time rule) automatically** | 🟡 | 🟡 |
| Refuse a discount bigger than the basket | ✅ | ✅ |
| Record **why** a discount was given | ✅ | ✅ |
| Hold a cashier to a discount **limit**, and step up to a supervisor | 🟡 | ✅ |
| Park a basket and recall it | ✅ | 🟠 *saves; the RETRIEVE is a toolbar item nobody finds — §5c item 1* |
| Take **cash** | ✅ | ✅ |
| Take **card** | ✅ | ✅ |
| Charge the tenant's **card fee**, with the fee's VAT following the basket | 🟡 | 🟡 |
| Split one sale across two tenders | ✅ | ✅ |
| Take payment on **one screen**, with a live Paid / Remaining / Change | ✅ | 🟡 |
| Take a **gift card** as payment | ✅ | 🟡 |
| **Sell** a gift card | ✅ | 🟡 |
| Take **store credit** as payment | ✅ | 🟡 |
| Attach a customer to the sale | ✅ | 🟡 |
| Apply a member's automatic discount | ✅ | 🟡 |
| Sell a **carrier bag** in one tap | 🟡 | 🟡 |
| Sell **more than one kind** of carrier bag (single-use *and* bag for life) | 🟡 | 🟡 |
| **Complete a sale with the network down** | ✅ | ✅ |

### Giving money back

| Can the till… | Web | MAUI |
|---|---|---|
| Refund a sale | ✅ | ✅ |
| Put the money back **the way it was paid** | ✅ | ✅ |
| Refund a sale that was paid **two** ways, correctly | ✅ | 🟡 |
| Refuse to refund more than was taken | ✅ | ✅ |

### Receipts and hardware

| Can the till… | Web | MAUI |
|---|---|---|
| Print a receipt | ✅ | ✅ |
| Open the cash drawer | ✅ | 🟡 |
| Reprint a receipt for an earlier sale | 🟡 | ✅ |
| Reprint a sale rung up on **another till** | 🟡 | 🟡 |
| Use the receipt layout set in the portal | ✅ | 🟡 |
| Show a customer the receipt on screen | ✅ | 🟡 |

### The drawer and the day

| Can the till… | Web | MAUI |
|---|---|---|
| Open a float | ✅ | ✅ |
| Record money in and out | ✅ | ✅ |
| Do an X read | ✅ | ✅ |
| Close the day (Z) | ✅ | ✅ |
| Say whether the drawer was **short or over** | ✅ | ✅ |
| **Record cash movements with the network down** | 🟡 | ✅ |
| Reopen a day closed by mistake | 🟡 | ✅ |
| Refuse to sell after the day is closed | ✅ | ✅ |

### Stock and the catalogue

| Can the till… | Web | MAUI |
|---|---|---|
| **Scan an item by ANY of its barcodes** | 🟡 | 🟡 |
| **Give an item another barcode, or correct one** | 🟡 | ⬜ |
| **See who changed an item, and when** | 🟡 | ⬜ |
| Look up what is in stock | ✅ | ✅ |
| Change an item's price or details | ✅ | ✅ |
| Work out the ex-VAT price from the band | ✅ | 🟡 |
| Add a **new** item | ✅ | 🟡 |
| Add a new item on **one screen** | ✅ | ⬜ |
| Adjust stock, with a reason | ✅ | ✅ |
| Withdraw an item from sale (the Bin) | ✅ | ✅ |
| Put a withdrawn item back | ✅ | ⬜ |
| Manage categories | ✅ | ✅ |
| **Scan an item with the network down** | ✅ | ✅ |
| Stop selling a withdrawn item **while offline** | ✅ | ✅ |

### Knowing what happened

| Can the till… | Web | MAUI |
|---|---|---|
| See today's takings | ✅ | ✅ |
| See sales over a date range | ✅ | 🟡 |
| See a VAT breakdown | ✅ | 🟡 |
| See what sold, and what sold best | ✅ | 🟡 |
| Sort, search and page a report | ✅ | 🟡 |
| Find a sale rung up on **another till** | ✅ | 🟡 |
| See a **chart** | ✅ | ➖ *the portal is the home for charts — Matt, 2026-08-16: "tables only"* |
| See pre-cutover history from the old system | ➖ | 🟡 *hidden, retained — see L4* |

### People

| Can the till… | Web | MAUI |
|---|---|---|
| Sign somebody in | ✅ | ✅ |
| **Sign somebody in with the network down** | 🟡 | ✅ |
| Show who works here | ✅ | 🟡 |
| Add a member of staff | ✅ | 🟡 |
| Set or reset a password | ✅ | 🟡 |
| Sign an operator out the moment they are disabled | 🟡 | ✅ |
| Set roles and permissions | ➖ | ➖ *portal only, deliberately — a till is the wrong place to widen authority from* |

### Being a citizen of the platform

| Can the till… | Web | MAUI |
|---|---|---|
| Be enrolled from the portal | ✅ | ✅ |
| Tell the platform it is alive, and which build it is | ✅ | ✅ |
| Report its **hardware agent** and printer | ✅ | 🟡 |
| Be told it is out of date | ✅ | ✅ |
| **Stop trading when it is revoked** | 🟡 | 🟡 |
| Ask to be taken off the estate | ✅ | 🟡 |
| Tell a shop something sold online needs picking off the shelf | ✅ | 🟡 |
| Show a platform announcement | ✅ | 🟡 |
| Raise a support ticket, and read the reply | ✅ | 🟡 |
| Show the shop's own details as the portal has them | ✅ | 🟡 |
| Show the shop's **opening hours** as the portal set them | 🟡 | 🟡 |
| Get its VAT bands from the portal | ✅ | ✅ |
| Apply a future-dated VAT change **while offline** | ✅ | ✅ |
| Take its colours from the portal | ✅ | 🟠 *fetches and applies them; only ONE screen paints with them — §5c item 10* |
| Tell network trouble from server trouble from being revoked | 🟡 | ✅ |
| Install its own updates | ➖ | ➖ *no self-update — Matt, 2026-08-11. ⚠ The **agent** is the exception: W5* |

### ⚠ Where the WEB till is behind — parity runs both ways

Reading down the ⬜ column on the web till's side, and it is not a short list:

| The web till cannot… | ⚠ |
|---|---|
| Sign anybody in offline | **The shop that loses broadband loses the till** — the thing the whole offline design exists to prevent |
| Record cash movements offline | A float taken while the line is down is a day that cannot be reconciled |
| Stop trading when revoked | It is on `navigator.onLine` — the network interface, not the server. A lost or stolen browser till keeps selling |
| Sign out a disabled operator proactively | It signs out only when something happens to 401, and tokens are cached 12 hours with their permissions |
| ~~Hold a cashier to a discount limit~~ | ✅ **CLOSED 2026-08-17 (web 1.11.0), §5b W-P3.** Original note: no client-side permission model at all — a web cashier could take off any amount |
| ~~Reprint an earlier receipt~~ | ✅ **CLOSED 2026-08-17 (web 1.11.0), §5b W-P6.** Original note: joins the reporting screen that already lists sales — which is where it landed. ⚠ It half-existed all along: Reporting → a sale → **Print copy receipt** already rendered a marked copy, but only through `window.print()`, so a shop with a thermal printer could not hand the customer paper. It now goes through the agent, with the browser dialog as the fallback |
| Reopen a day closed by mistake | Server side is done and live; nothing calls it |

⚠ **This half is easy to forget** because the web till is the one that gets used. Matt's 2026-08-08
ruling — *"the tills need to be in parity"* — points in **both** directions.

## A1. The surfaces

Everything that can produce a sale, and what it's made of.

| Surface | Technology | Runs on | Local state | Reaches the server via |
|---|---|---|---|---|
| **Web till** — `Plutus.Frontend.WebApp` | React 19 + TypeScript, Vite | Any browser | IndexedDB (outbox + catalogue cache) | `POST /api/v1/sales`, plus some legacy `/api/*` |
| **MAUI till** — `Plutus.Frontend.AppClient` | .NET MAUI (Sean's rework, NatApp lineage) | `net10.0-windows`, `-android`, `-ios` | **SQLite local store v2** (`TillDbContext`) — ⚠ the legacy schema is still present and still read by the hidden pre-cutover reports ([L4](To%20do/MAUI-retrofit.md)), but **sales, cash, the outbox, the catalogue, the roster and every Meta key are v2** | ✅ **`POST /api/v1/sales` through `Plutus.Client.Core`**, plus the 60 s cadence (heartbeat, catalogue, cash drain, VAT bands, receipt template, noticeboard, agent status, device standing) and some legacy `/api/*` for the item editor and staff. ⚠ **This cell read *"Not yet — WP5+ wires it"* until 2026-08-17**, long after it was wired |
| **Webstore connector** | Backend module `Plutus.Webstore` | Server | — | Its sink builds an `IngestSaleRequest` and calls `SalesIngestService` **directly** |
| **Hardware agent** — `tools/Plutus.TillAgent` | WinForms tray app + Kestrel on `127.0.0.1:9123` | `net10.0-windows` | Token in local config | Not a sales path — it prints and kicks the drawer for the *browser* till |
| **Portal** — `Plutus.Frontend.Portal` | React 19 + TypeScript | Any browser | — | The **source of truth**, not a till: it publishes what tills obey |
| ~~`Plutus.Frontend.ClientUI`~~ | MAUI | — | — | **Abandoned port, being retired.** Kept only to harvest its colour palette. Do not build on it. |

⚠ **The webstore is a sales channel, and it is easy to forget.** It doesn't look like a till, but it
writes sale lines, so every rule in Part C applies to it. It was the channel found sending no VAT
band at all.

## A2. The shared spine

The libraries any .NET till uses. All are plain `net10.0` — no MAUI, no UI, no OS-specific target.

| Project | What it holds | Rule |
|---|---|---|
| `src/Plutus.SharedKernel` | **The rules.** Money, VAT arithmetic, ids, permissions, band resolution. | No project references at all. |
| `src/Plutus.Contracts.Client` | The wire contract (DTOs + the `LineMeta` envelope). | **No references, no packages** — it ships onto tills. |
| `src/Plutus.Client.Core` | Outbox engine, pusher, API client, device-token provider. | May reference only SharedKernel + Contracts.Client. |
| `src/Plutus.Client.Storage` | Local store v2 (SQLite) + cutover. The only place that knows SQLite. | As above. |

**Three architecture tests hold this, so it can't rot by accident:**

| Test | What it refuses |
|---|---|
| `Till_client_libraries_stay_free_of_MAUI_and_backend_modules` | A UI/MAUI package, or a reference to a backend module, in a till library. |
| `Till_libraries_stay_platform_neutral_and_hold_no_VAT_rates_of_their_own` | An OS-specific target framework in a till library, or a literal VAT rate in one. |
| `No_module_references_another_module` | Backend modules reaching into each other. |

---

# Part B — What each till can do

**The rule:** when you add a capability to any till, add its row here in the same commit. If another
till isn't getting it, say so in Notes and why. A deliberate ⬜ is a decision; a missing row is a
surprise six months later.

## B1. Selling (the Till screen)

| Capability | Web | MAUI | Backend | Notes |
|---|:--:|:--:|---|---|
| Scan / search / manual add | ✅ | ✅ | `GET /api/Item` | |
| ⚠⚠ **MULTI-BARCODE — one item scans under SEVERAL codes** | 🟡 | 🟡 | `GET /api/v1/items/barcodes` (any authenticated caller — the web till pulls it) · `POST`/`DELETE api/v1/items/{itemIdOne}/barcodes` on `perm:portal.stock.adjust` · migration `AddItemBarcodes` · `CatalogueItemDto.Barcodes` | ✅ **BUILT 2026-08-20** ([`Multi-barcode plan.md`](archive/Multi-barcode%20plan.md), MB1–MB6). Matt: *"I know the till currently uses the barcode as a unique entry at the moment, but I need to move to having multiple barcodes."* ⚠⚠ **THIS REVERSES THE 2026-08-09 RULING** recorded in `TillStore`/`LocalSchema` — a local alias table was deleted then because nothing had ever written to it, and that note named the prerequisites: *"a server entity, a feed field and a portal UI first — a platform decision, not a till change."* All three now exist, so the table is **fed**; both comments were rewritten to record the reversal rather than left asserting the opposite of the code. ⚠⚠ **`Item.IdOne` REMAINS THE IDENTITY, FOREVER** — it seeds `DeterministicGuid.ForItem` (frozen golden vector, TS twin), it is half the composite PK with five FK families on it, and it is on every historical sale line. Aliases are additive rows; **nothing is re-keyed**. ⚠⚠ **AND THE ALIAS STRING NEVER TRAVELS PAST RESOLUTION** — the three doors (`ItemController.FindById`, `TillStore.FindByBarcodeAsync`, web `cachedItemByAlias`) each return the CANONICAL item. The two faults a leak causes are both silent: `StockProjectionConsumer` creates a phantom `StockLevel` for an id it does not know, and `VatBandStamp` leaves the line's VAT band null. ⚠ The scanned code IS recorded — `LineMeta.barcodeScanned`, omitted when it equals `itemIdOne`, so an ordinary line's metadata is byte-identical to before. ⚠ Aliases resolve on the EXACT-scan path only; token search still matches name/idOne/brand (deliberate — four implementations would have to move in lockstep). ⚠ A binned item's aliases do not scan. ⚠ 🟡 both: no person has run it — hand-run **§G66**. |
| ⚠ **MANAGE an item's barcodes — add, correct, remove** | 🟡 | ⬜ | `PUT api/v1/items/{itemIdOne}/barcodes/{code}` on `perm:portal.stock.adjust` · web `src/ItemBarcodes.tsx` + `canManageBarcodes()` · portal `src/ItemBarcodes.tsx` · the shared rule `src/barcodeProblem.ts` (C1) | ✅ **BUILT 2026-08-20.** Matt: *"When I am trying to edit an item in the portal or on the webtill, I cannot edit or add a new barcode?"* — the endpoints existed and neither surface had a control. ⚠⚠ **CORRECTION IS ONE `PUT`, NOT DELETE-THEN-ADD.** Two calls can fail between them and leave the item with **neither** code, which for a barcode means an item that silently stops scanning. ⚠⚠ **EVERY ROW IS LOCKED BEHIND `🔒 Edit`** — Matt: *"Barcodes should be 'Locked' to avoid accidently changing the barcode."* A barcode is the string a scanner matches on, so a stray keystroke in an always-live box is an item that stops scanning with nothing to say so. ⚠ **THE LIVE CHECK IS A COURTESY, NOT THE GATE** — `barcodeProblem` answers uniqueness from the whole-tenant list (no round trip) and warns on leading/trailing whitespace; the reserved shapes (member cards, gift cards, bag ids, platform ids) stay **only** in `SharedKernel.ItemBarcodeRules` and arrive as a sentence shown verbatim, because a copy of an identity rule in a client is the C2 fault. ⚠ Whitespace **warns and still saves** (the server trims) while a clash **blocks**; two colours, because "you can carry on" and "you cannot" must not look the same. ⚠ ⬜ MAUI: it has **no item editor at all** — `ExecuteOpenAddItem` is unreachable dead code and the screen offers only "View all items", because a till-created item reaches no report, no other till and no VAT return. Item writes are the portal's (C1 "Portal decides, till obeys"); inventory parity is **WP10** / `MAUI-retrofit.md` §10 (L2). ⚠ 🟡 web: built, **not yet exercised by a person** — §G66. |
| ⚠ **An item's CHANGE HISTORY — who changed what, when** | 🟡 | ⬜ | `GET api/v1/items/{itemIdOne}/history` on `perm:portal.reports.view` · `ItemHistoryController` · web + portal `ItemHistory` in `ItemBarcodes.tsx` · `canViewItemHistory()` | ✅ **BUILT 2026-08-20.** Matt: *"At the bottom of an item listing when editing, can they be a history kept of every change to this item, from initial creation etc, logging time, what was changed and who by?"* ⚠ Built on the **existing `AuditLog`**, not a new table: `ItemController.Put` now writes `{before, after}` and the controller turns that into field-by-field *"Label was X → now Y"* sentences, merging in the `ItemBarcode` audit rows. ⚠⚠ **THE PRE-LOGGING ERA IS SHOWN AS SYNTHETIC BOOKENDS** derived from `Item.CreatedAt`/`ModifiedAt`, and says so in the row. An audit trail that is silently incomplete is worse than one that admits a gap, because a short list reads as a complete one. ⚠ `portal.reports.view`, **not** `portal.stock.adjust` — naming who changed a price is a supervisory record, not a stock task, and different people hold the two. ⚠ Collapsed and lazy-loaded: the common reason to open an item is to change a price, and every such open should not fetch an audit trail nobody asked for. ⚠ ⬜ MAUI: no item editor — see the row above. ⚠ 🟡 web/portal: not yet exercised by a person — §G66. |
| Basket qty, price adjust, reorder | ✅ | ✅ | — | ⚠ **2026-08-20 — the basket LIST is the only thing on the selling screen that scrolls.** Matt: *"The buttons always need to stay and the items need to become 'Scrollable'"* — a long basket was pushing Checkout off the bottom of **both** tills. MAUI: the screen is now a star-row `Grid`, not a `StackLayout` (`TillView.xaml` — a vertical StackLayout measures children unbounded, so the list grew and took the buttons with it). Web: `.shell.till-locked` bounds the app to the viewport **on the Till tab only**, so `.basket-grid`'s existing `overflow-y` actually engages; other tabs keep page scroll on purpose. ⚠ Invisible to every suite — hand-run **§G62** (retrofit §5c item 11) |
| Discounts | ✅ | ✅ | — | ⚠ **The button is "Apply Discounts" on BOTH tills, and the dialog is "Apply discount" on both** (Matt, 2026-08-20). It was *Alter Transaction* / *Alterations* — NatApp's wording, which named the mechanism (a basket alteration record) rather than the job. MAUI's half is the `AltTransaction` and `Alterations` resx **values**; the KEYS are unchanged, because a missing resx key throws in DEBUG as the Till tab is constructed. ⚠⚠ **AND THE LINE LIST NOW FOLLOWS THE TILL'S OWN ORDER.** The web till's dialog read the RAW basket while the screen behind it renders `displayLines`, which honours the `newestFirst` preference — so with that preference on the two lists were exact MIRRORS and an operator ticking "the top one" discounted the item they had scanned FIRST. Ordering cannot change which lines are discounted (they are keyed, not positional), so this is about whether a person can trust what they are ticking. MAUI needed no change: it has no such preference. Hand-run **§G64h2**. |
| ⚠⚠ **SCHEDULED DISCOUNTS — "Wednesday Warhammer"** | 🟡 | 🟡 | `GET /api/v1/discounts/rules` (any authenticated caller) · portal writes on `portal.prices.manage` · migration `AddDiscountSchedule` | ✅ **BUILT 2026-08-20** (Discount plan DP1–DP5). Matt: *"I have Wednesday Warhammer discount that should flag items in the warhammer catergory on a Wednesday as 'Should have 10%'."* ⚠⚠ **IT EXTENDS THE EXISTING `Discounts` TABLE rather than adding a rival entity**, and that is the load-bearing decision: the legacy row has a REAL int id that `LegacySaleBridgeConsumer` projects into `Transaction_Discount`, both tills already fetch and cache that catalogue, and `Discount_Category`/`Discount_Item` already said "this discount targets that category/item". A new entity would have needed a bridge change or the members'-discount exclusion dance for every rule discount ever taken. ⚠ `AutoApply` stops being a dead column — TRUE is what makes a rule apply itself. ⚠⚠ **THE SCHEDULE TRAVELS RAW AND THE TILL JUDGES IT** (day mask bit 0 = Sunday, LOCAL window, UTC validity — `PermissionGrant`'s shape, mirrored expression by expression including the inclusive-both-ends window and the unsupported midnight wrap). A pre-evaluated answer would apply Monday's discounts all week to a till that went offline on Monday. ⚠⚠ **A RULE THAT TARGETS NOTHING MEANS NOTHING, NEVER EVERYTHING** — the carrier-bag "fail towards NONE" precedent, refused by the reader (`IsWellFormed`) *and* by the writer. A category list that failed to load must not become a whole-basket discount. ⚠ **ONE discount per line, the LARGER of tier-vs-rule, never both** (decision D2) — through `SharedKernel.AutoDiscounts`, the one resolver both tills call; ties go to the member. ⚠ An operator's own discount always wins, and **clearing an automatic one WAIVES it for that line** or it reappears on the next scan. ⚠ 🟡 both: no person has run it — hand-run **§G64**. |
| **Ad-hoc typed discount (£/%) with no catalogue row** | 🟡 | ✅ | `POST /api/v1/sales` — no server change | ✅ **WEB TILL BUILT 2026-08-20 (D8)**, closing a real parity gap: MAUI has always been able to type a figure (`PercentDiscountInput`) and the web till could only pick from the catalogue, so a dented box could be marked down on one till and not the other. ⚠⚠ **IT CARRIES `AD_HOC_DISCOUNT_ID` (−1), NOT AN INVENTED POSITIVE ONE**, and checkout's filter tightened from `!== 0` to **`> 0`**: `LineMeta.discounts[]` projects into `Transaction_Discount` keyed on a real `DiscountId`, so one rule ("only a positive id is a real catalogue discount") now covers both sentinels and a future third is safe by construction. ⚠ The ceiling, the step-up and the mandatory reason are the SAME code path as a catalogue discount — a second path is how the new one ends up ungated. ⚠ **MAUI was already wire-safe**: it populates `LineMeta.discounts[]` for nothing at all, so its typed discounts have always travelled as `DiscountPence` + authority. That asymmetry is recorded in C2 rather than "fixed". |
| **A discount records WHY, and WHO authorised it** | ✅ | ✅ | `POST /api/v1/sales` — **no server change, no migration** | ✅ **Both tills, 2026-08-14 — binding default 22(c)**, Matt: *"All discounts need to be tracked — till, logged-in employee and reason."* The till and the employee were already on the sale header (`DeviceId` from the token, `OperatorUserId`); **the reason was collected nowhere on any till**, and a supervisor's authorisation went to the till's LOCAL LOG and stopped there — so *"who approved this?"* needed that till's log file, and a re-imaged till has none. Now on the sale, via `LineMeta.discountAuthority[]`. ⚠⚠ **NOT on `LineMeta.discounts[]`, and that is the design decision, not a detail**: the legacy bridge projects every entry there into a `Transaction_Discount` row keyed on a REAL `DiscountId` (`LegacySaleBridgeConsumer.cs:195`), which is why the members' auto-discount is *already* filtered out of it — an entry with a synthetic id would FK-fail the projection of every discounted sale. Pinned by `An_authority_does_not_add_a_catalogue_discount_the_legacy_bridge_would_FK_fail_on`. ⚠ **One entry PER DISCOUNT, not per line** — `IngestLine.DiscountPence` is their sum, so it cannot say which half a supervisor approved. A basket-wide discount repeats on every line it lands on, each carrying **that line's share from the same `DiscountApportionment` call the money used** — never a second apportionment, which would drift only on baskets that do not divide evenly. ⚠ **The reason is MANDATORY on both tills**: an optional reason is an empty column, and the one discount anybody asks about is the one where nobody typed anything. ⚠ **Absent ≠ unauthorised** — every sale before 2026-08-14 has none, and a reader must not render the two the same way. ⚠⚠ **UPDATED 2026-08-17 (§5b W-P3): the web till now WRITES `authorisedBy`.** Until then it correctly sent none, because no step-up was possible on that till — *"absent means no step-up was required, which on this till is true of every discount today"* was a true sentence and is now false. It sends the supervisor's `userId` when, and only when, a step-up actually happened; every web-till discount taken **before** 2026-08-17 has none for the original reason, so "absent" now covers two different histories and a reader must still not treat it as "unauthorised". |
| **Discount ceiling + supervisor step-up** (default 22(b)) | 🟡 | ✅ | RBAC `pos.discount` `MaxPence` per role | ✅ **WEB TILL CLOSED 2026-08-17 (web 1.11.0), §5b W-P3** — `src/permissions.ts` is a C2 twin of `SharedKernel/Permissions.cs` (`IsActiveAt` + `Merge` + `Can`), 22 tests, two mutants killed (intersection-instead-of-union on ceilings; the gate failing open on an unknown code). The dialog resolves `pos.discount` from the **W-P2 cached roster**, so the gate works with the line down. ⚠ The amount checked is `plannedDiscountPence` — the **real engine's** answer via `lineDiscountPence`, never a second derivation, exactly as MAUI builds-then-checks (default 22). ⚠⚠ **Self-approval refused** at the point of entry, and **the authoriser must themselves pass the gate for the amount** — otherwise "step up" becomes "ask anyone at all". ⚠ An operator with **no** `pos.discount` grant is told why rather than left with a dead button. ⚠ **`authorisedBy` now travels to the sale** — see the row below, whose note this change falsified. Original note: found 2026-08-14 while building 22(c). MAUI gates on `TillGate.Check(..., PosDiscount, requestedPence)` and steps up through `RequestSupervisorOverrideAsync`; the web till has **no `pos.discount` check, no ceiling and no step-up**, so a web-till cashier can take off any amount and the only enforcement is whatever the server does at ingest. ⚠ Matt, 2026-08-14: *"Base it on roles"* — a level IS a role's `MaxPence`, so there is nothing to design, only to build. ⚠ It is **not** a small addition: the web till reads no permissions client-side at all (`session.ts` holds token/employeeId/name and nothing else), so this needs the permission set on the session first — the same plumbing `pipeline.ts mayAddCustomer` had to invent for `pos.customers.add`. **Sized and homed: WP15 / a web-till slice of its own.** ⚠ Until then the web till's `discountAuthority` correctly writes **no** `authorisedBy` — absent means "no step-up was required", which on that till is true of every discount because none is possible. |
| Returns / refund lines | ✅ | ✅ | `GET /api/v1/sales/{saleId}` | **Cutover steps 15+16, 2026-08-09.** `Services.Sales.ReturnLookup` prefers the SERVER record whenever it answers — goods bought on till B and returned at till A is ordinary retail, and only the platform knows what has already been given back elsewhere — and falls back to this till's own record ONLY inside the 14-day rolling window. Outside it the answer is "reconnect", never a guess. `RefundRules.Authorise` (shared) decides; `WasCapped` is surfaced to the operator because silently handing over less than was asked for is how a refund becomes an argument. ⚠ It replaced a block that **crashed the app**: `trans.First()` on the legacy `Trans` table threw from an `async void` with no catch the moment the item wasn't on that sale, so scanning the wrong receipt closed the till — and that table is empty and permanently so on a portal till, while its "refunds left" test counted quantities on one machine. ⚠ The refunded price is the one from the ORIGINAL sale, not today's catalogue price. ⚠ **The local cap is per SALE, not per line** (`LocalRefunds` records money per origin sale): it enforces "never more than was paid" in aggregate, and per-line accounting is step 17's, server-side. |
| **Refund-only baskets** (negative sale) | ✅ | ✅ | `POST /api/v1/sales` | Server half shipped 2026-08-07 (invariants are sign-agnostic). ⚠ **The MAUI half was marked ⬜ until 2026-08-16 and had in fact been built** — corrected against the tree, and this is the **fifth** stale marker found in a week. `TillViewModel:1507` derives `refundOnly` (a basket with no SALE lines), and it drives four things: the tender set is restricted to the origin sale's tenders (`TillTenders.Offered(refundOnly: true)`), each tender is **capped** at what it actually took (finding Y), the card surcharge is suppressed, and the prompts say *refund* rather than *pay*. Pinned by `RefundTenderTests` + `TillTendersTests`, and **hand-run** — A4b on 1.49.3+ returns a split-paid sale, which IS a refund-only basket. ⚠ The Notes that justified the ⬜ described the SERVER half and were read as though they described the client's. |
| Park / retrieve basket | ✅ | ✅ | — | MAUI must reserialise as contract JSON, no `$type` — WP2. |
| Split payment / change | ✅ | ✅ | — | |
| **Gift cards — sell & redeem** | ✅ | 🟡 | `/api/v1/giftcards/*` | ✅ **BOTH HALVES BUILT — activation landed 2026-08-16 (till 1.60.0).** ⚠ **This row said "ACTIVATION is still ⬜" until 2026-08-17**, after the commit that closed it. Selling one now posts through `SharedKernel/GiftCardVat.PairFor` in `CheckoutCommit` — the tenant's voucher treatment decides the VAT and **nothing at the till invents it**: multi-purpose is ex == inc (no VAT at sale, it is a liability not revenue), single-purpose carries VAT inside the face value, and `NotChosen` **throws** rather than guessing. 🟡 rather than ✅ only because no person has run it — §G13. Original note: **Whole feature missing.** Zero references in MAUI. ⚠ Gated on the per-tenant VAT-treatment decision — `GiftCardSettings`' absence **409s** generate/activate/redeem, so a MAUI till that doesn't understand this gets errors it can't explain. **WP13** (body + DoD). ⚠ **REDEEM landed first (till 1.59.0).** A card can be scanned, looked up, held in page state and spent — capped at its balance through finding Y's tender-cap mechanism, redeemed **before** the sale is recorded with a failure aborting, and never offered on a refund (refunding onto a card turns returned goods into fresh spendable value — the classic way a scheme is drained). ⚠ **`unsold` is refused at the till with its own sentence**: a card off the rack has a real code and scans perfectly, and taking it would hand over goods against value nobody bought. ⚠ Activation was held back deliberately at that point — it is where the zero-VAT treatment lives, and it was the half that must not be guessed. Shared client: 18 tests. |
| Customer attach at sale + auto-discount | ✅ | 🟡 | `/api/v1/customers` | WP12. ⚠⚠ **THIS ROW WAS A LIVE MONEY DIFFERENCE** (found 2026-08-13): the web till applies the tier discount at `TillPage.tsx:122–123` and `autoDiscountRate` appeared **nowhere** in `Plutus.Frontend.AppClient`, so **a Gold member was charged 10% more on MAUI for the same basket**. ✅ **BUILT 2026-08-16 (till 1.56.0)** — `MemberDiscountBasket` (the engine, 16 tests, mutation-checked twice) plus the screen: a customer bar with attach/detach, search by name/phone/email/member number, **scan-to-attach on both add doors**, and the discount rebuilt reactively from `Basket.CollectionChanged` with a re-entrancy guard. ⚠ **🟡 NOT ✅ DELIBERATELY: no person has run it.** The engine is covered by tests; the *screen* cannot be — MAUI bindings fail silently and this row is one grid-row renumber away from a blank customer bar. **Closes at hand-run §G** (`Test Maui.md`). ⚠ Still ⬜ separately: the **create dialog** and the **tier picker** — see their own rows. |
| Member-number scan-to-attach | ✅ | 🟡 | `/api/v1/customers` | ✅ **BUILT 2026-08-16** — `TryRouteMemberScanAsync` sits on **both** add doors (the scan box and Inventory → Add to till). ⚠ **It went on one door first**, which is the identical hole `RefuseIfDayClosedAsync` was extracted to close, made again two commits later — a rule enforced per-entry-point is a rule with a hole in it. Now one method both call. ⚠ 🟡 until a person scans a real card (§G). Original notes: | FE2 `NNNNNNC` barcode. **WP12** — body + DoD added 2026-08-08; the check-digit rule is `SharedKernel.MemberNumbers`, never re-derived. ✅ **The rule is now actually there** (moved 2026-08-13 — it had been in `Plutus.Customers`, a module MAUI cannot reference, so this Notes cell described an intention rather than a fact). `MemberNumbers.LooksLikeMemberScan` is the routing predicate: prefixed + valid check → attach; prefixed + invalid → say the card did not scan, never fall through to an item search. |
| **Add a new member at the till** | ✅ | 🟡 | `POST /api/v1/customers` — **`pos.customers.add` OR `customers.manage`** | ✅ **Backend, permission and the WEB till all landed 2026-08-13** (binding default 20; Matt: *"Till operator to add new loyalty members"*). ⚠ **CREATE-ONLY** — editing a member and setting a tier stay `customers.manage`, pinned by `A_cashier_can_ADD_a_member_but_not_edit_one_or_set_a_tier` (mutation-checked: widening the edit gate is caught). Web till: `pipeline.ts` `mayAddCustomer`/`canAddCustomers` gate **＋ New** on the sale screen and **Add member** on the Loyalty page, while `edit` and the row action stay on `canManageCustomers()`; 4 vitest cases, mutation-checked (OR → AND fails both permissive cases). ⚠ **A cashier gets NO tier picker in the member dialog, deliberately** — the dialog creates the customer and *then* assigns the tier as two calls, so leaving the control visible meant 201 on the create and **403 on the tier**: an error in front of a customer for a member who *had* been added, whose natural retry creates a duplicate. It says who to ask instead. ⚠ **Online-only on every till** — the number comes from a tenant-wide counter, so two offline tills would mint the same one. ⚠ **Needs only a sign-out/in after deploy** — login tokens cache for 12h with the permission set baked in. It does **not** need `SeedMigrator rbac`: `RolePermissionReconciler` delivers new catalogue grants on every boot (I wrote the opposite here first, trusting a stale runbook line, and corrected both). ✅ **LIVE 2026-08-13** — backend 1.17.0, till-web 1.8.0. ✅ **MAUI: on the LOYALTY TAB since till 1.82.0 — and it had never once worked before then.** ⚠⚠ **Two independent faults, both invisible to 598 MAUI tests.** ① It used the **device** client against a `perm:`-gated endpoint, and `PermissionAuthorizationHandler` resolves RBAC by the token's `NameIdentifier` — the *device id* on a device token, which holds no grants. So it answered **403 for every operator whatever their role**; *"member added"* had never appeared on this till (fixed 1.81.0, same fault as the Reports tab in 1.75.0). ② The button was on the **till screen**, where Matt said it did not belong and was right — the web till's sale screen has no customer control at all, a member is attached by **scanning their card**, and add/edit live on its **Loyalty page**. ⚠ Removing it exposed the real gap: the Loyalty tab, the one MAUI screen with a member list, had no add button either — so between the two commits MAUI could not add a member **anywhere**. Moved, not copied: the originals are **deleted** from `TillViewModel` (255 lines), because two divergent add-member paths is the drift C2 exists to prevent. ⚠ On the Loyalty tab it no longer auto-attaches — there is no basket there — and instead searches for who was just added, which is also this screen's only proof the write landed. ⚠ Mandatory field marked `Name *` (Matt: a save that fails silently is a member the operator believes they created). ⚠ **🟡 until hand-run §G38**, which is written on the assumption that nothing here has ever been seen to work. |
| Store credit as tender | ✅ | 🟡 | `POST /api/v1/customers/{id}/credit/redeem` | ✅ **BUILT 2026-08-16 (till 1.58.0).** Offered ONLY when a member is attached with a live balance — it draws down a server-held figure and cannot be verified offline, so a button that could only fail is worse than none (the trap `Offered` was written to fix). ⚠ **NEVER on a refund**, matching the web till and binding default 19: store credit took no money, so refunding INTO it would mint spendable value out of a return — the gift-card cash-out exploit in another hat. Mutation-checked. ⚠ **Capped at the balance through finding Y's existing tender-cap mechanism**, not a second rule about how much a tender may take. ⚠⚠ **REDEEMED BEFORE THE SALE IS RECORDED, and a failure ABORTS** — the server owns the balance, so an overdraw must be refused while the sale can still be abandoned; redeeming after the commit leaves a recorded sale that was never fully paid. Idempotent by entry id. ⚠ 🟡 until hand-run §G11. |
| **Add unknown scan as a new item** | ✅ | ✅ | `POST /api/Item` + `POST /api/Stock` | ✅ **Closed 2026-08-10 (till 1.36.0)** — see the dedicated row below for the ⚠s. Web shipped 2026-08-07. |
| Effective pricing at basket-add | ✅ | ✅ | `GET /api/v1/prices/effective` + the timeline on `catalogue/changes` | **WP5, built 2026-08-08.** The feed carries the item's **whole price timeline** (future points included), its policy, and this till's store overrides; `TillStore.EffectivePricePenceAsync` resolves with the shared `PriceResolution` at the SALE's instant. ⚠ A price write must call `PricingService.TouchItemForSyncAsync` or it never reaches a till — the feed pages by `Item.ModifiedAt` and prices live in their own tables. Pinned by `A_PRICE_change_reaches_the_till_even_though_prices_do_not_live_on_the_item`. |
| Card capture events | ⏸ | ⏸ | `POST /api/v1/payments/events` | Blocked on a provider for **both**. Terminal integration is greenfield — but note the till already *displays* the configured gateway (next row). |
| Payment-gateway awareness at checkout | ✅ | 🟡 | `GET /api/v1/payments/gateway/active` | **WP14** — shared half landed 2026-08-09 (`Client.Core.PaymentGateway.Resolve`, 13 tests); MAUI is 🟡 pending the checkout XAML. ⚠ **The rule is NOT "read `provider`".** Every provider reports `integrated: false` today, so a till switching on the provider name would wait for a terminal that never answers with a customer in front of it. **`integrated` decides the flow; `provider` only decides the wording** — a chosen-but-unwired provider is still the manual flow, named, with "integration pending" shown. ⚠ No answer at all = standalone and **not** "pending": a cosmetic lookup must never block a card sale, and a tenant that never chose a provider must not be told an integration is coming. Web till decides the same three cases inline at `CheckoutDialog.tsx:226-233`. |

## B2. Receipts & hardware

| Capability | Web | MAUI | Backend | Notes |
|---|:--:|:--:|---|---|
| Silent receipt print | ✅ | ✅ | — | Different transports, same outcome: web goes through `Plutus.TillAgent`, MAUI drives OPOS directly. MAUI does **not** need the agent. |
| Cash drawer kick | ✅ | ✅ | — | As above. |
| **Portal-controlled receipt template** | ✅ | 🟡 | `GET /api/v1/stores/{id}/receipt-template` | Shipped 2026-08-06. MAUI's layout is **hardcoded C#** (`PosPrinterManager.cs:138-167`) — it *does* print the store's name/logo/address/VAT from the locally-synced StoreModel, but it ignores the portal template entirely (header/footer lines, toggles), contradicting the "all receipts controlled from the portal" requirement. **WP3** — the receipt renders from the portal template, DoD-gated. |
| Reprint from a past sale | ✅ | ✅ | `GET /api/v1/sales/{saleId}` | ✅ **Closed 2026-08-10 (till 1.34.0)** — Statistics → "Reprint a receipt", from this till's last 20 sales, marked as a copy. See the dedicated row below. 🟡 remaining only for a sale rung up on ANOTHER till, which needs WP11's cross-till lookup. |
| ⚠ **Finding a past sale AT ALL** (list / search recent sales) | ✅ | 🟡 | `GET /api/v1/sales` — already answers | ⚠ **THIS, not the refund rule, is what made refunds unusable.** Returns, the refund cap and the read path all shipped at steps 15–17 — but the "Returns" dialog asked for the **original sale ID**, and `TillStore.FindLocalSaleAsync` takes a `Guid` with **no browse, no search, no "today's sales"** anywhere in the app. The only source of that id was the **barcode on a printed receipt**, so a till with no printer could not refund anything. A complete feature with no door; reported by Matt 2026-08-10 as *"In MAUI I cannot do a refund?"*. ✅ **Closed 2026-08-10**: `TillStore.ListRecentSalesAsync` + a picker in front of the Returns dialog — pick the sale, then give only the reason. ⚠ Reads THIS TILL's own record, so it works with the line down, which is when a shop most needs to hand money back; typing an id remains for goods bought on another till. ⚠ **A QUEUED sale is offered** — the money left the drawer when the goods did, whatever the outbox has managed to deliver. Pinned by `TillStoreReadPathTests` (18). 🟡 until it also covers **cross-till** sales and **reprint** (step 26 — one screen serves both). |
| Barcode on receipt (sale id) | ✅ | ✅ | — | |
| **Show the receipt ON SCREEN** (no paper) | ✅ | 🟡 | — | ✅ **MAUI CLOSED 2026-08-17 (till 1.73.0)** — offered wherever paper fails: when **no printer is set up** (previously a dead end) and when **a print is refused**. ⚠⚠ **IT RENDERS THE SAME `PrintDocument` THE PRINTER GETS**, via `Client.Core/ReceiptPreview.cs` (11 tests) — laying the receipt out a second time for a screen produces a preview that can disagree with the paper, and that is worse than no preview: somebody checks it, sees the right total, and the customer's copy says something else. ⚠ **Not a pixel preview, and it says so** — bold, double-height and barcodes have no honest text equivalent on a thermal printer, so it shows content and layout. A barcode becomes a marker **plus its number**, because the number is the half somebody reads down a phone. ⚠ `Cut` and `Drawer` render **nothing** — they are instructions to a machine, and showing "[cut]" invites *"why didn't the paper cut?"* ⚠ An over-long line is **not truncated**: the printer wraps it, and hiding it would defeat the one thing a preview is for — spotting that a shop's address does not fit the paper. ⚠ Left-aligned lines gain **no trailing padding**, so pasting the receipt into a support ticket is clean. ⚠ It goes through `ReceiptBranding` like every print path, so the portal's layout applies. ⚠ **Known limitation, accepted:** it renders in `DisplayAlert`, which is not monospaced, so columns will not line up perfectly — the figures are what a customer is being shown, and a dedicated monospaced page is screen work for later. ⚠ 🟡 until §G32. Original note: **ROW ADDED 2026-08-17 — Part B did not have one, and A0 found it on its first read.** The web till renders a receipt in the browser (`till/Receipt.tsx`); MAUI has **no on-screen equivalent** — it builds a `ReceiptDocInput` and sends it to paper. ⚠ **This ⬜ needs Matt's decision rather than a WP number**, which is why it has neither yet: the web till's preview exists partly because a *browser* till may have no printer at all, and a Windows till app with a receipt printer attached may not need one. **The question is whether an operator must be able to show a customer their receipt without printing it** — a real request in some shops (email/paperless), and not a request at all in others. ⚠ Recorded rather than assumed away: a missing row is how the July drift happened, and "we decided we didn't need it" is only a decision if it is written down. |

## B3. ~~Tabs the MAUI app has no equivalent for~~ → **Areas the web till had first**

⚠ **This heading was true when it was written and is not now** — corrected 2026-08-17. Every area below
is **built on MAUI**, and on two of them (offline cash, reopening a Z-closed day) **MAUI is ahead of the
web till**. The heading survives, struck through, because a reader who remembered the old shape needs to
see that it changed rather than wonder whether they misread it.

| Area | Web | MAUI | Retrofit |
|---|:--:|:--:|---|
| **Cash** — float, paid in/out, X, Z | ✅ | ✅ | **WP9 / cutover step 23 — MAUI closed 2026-08-10.** Its own tab beside the Till, because a shop cannot OPEN or CLOSE without it. ⚠ **The server was already finished and nothing client-side existed** — no screen, no client method, and the five type names appeared nowhere in the app or the shared libraries. ⚠ **QUEUED, NOT POSTED** (`LocalCashEvents`, schema v4, drained by `CashPushService` on the 60s tick): a shop opens before its broadband does and the money moves whether or not the platform hears, so a float that failed to post is a day whose banking cannot be reconciled at all. ⚠ **ONE Z PER DAY is enforced LOCALLY as well as server-side**, refusing every type after it rather than merely a second Z — the server's guard cannot be consulted with the line down, which is exactly when it matters. ⚠ **The EXPECTED figure is the SERVER's**: only the platform sees the sales half (including sales another device on the same till posted), and a till caching its own would disagree with the banking report with no way to say which was right. ⚠ On the drain, **409 and 400 are TERMINAL** and kept with the server's words — a till that retries them for ever looks healthy while quietly never banking. 10 tests, mutation-checked twice. ⚠ **USER-VERIFY outstanding: does the drawer physically kick?** `POSCashDrawer.InitPOSObject` was missing the `return` on its success path until 2026-08-10, so this is the first build where it can be exercised. ⚠ **AND THE SCREEN HAS TO BE TOLD WHEN THE QUEUE DRAINS** (finding J, till 1.44.0). Matt: *"The open float was 'Waiting' and never updated. I navigated away and back onto the cash tab and it had updated."* The data was right and the drain worked; the screen only ever redrew in `OnAppearing`, so an operator read "(waiting to send)" against money already banked. Now `TillCadence.Ticked`, subscribed while the page is up. ✅ **WEB TILL CLOSED 2026-08-17 (web 1.11.0), §5b W-P5** — `cashOutbox.ts` queues cash events in IndexedDB (store `cashOutbox`, DB v3), drains them on the 60 s beat **and** on the browser `online` event, and shows "waiting to send" per till. ⚠ **Sales drain FIRST**, because a Z waits for its own day's sales and draining cash first would stall on a Z the sales drain is about to unblock. ⚠ **Queued BEFORE sending**, so a browser that dies between the POST and the local write still has the money. ⚠ The three decisions live in `cashRules.ts` — pure, so they test without a browser — and both money mutants are killed. ⚠ **The expected drawer is still never computed locally**: `expectedPence` is set only when the platform ANSWERED, so an offline X/Z shows the count without inventing an expectation. Original note: the web till had no such bug because it had no such feature — `CashPage.tsx` posted online-only, held no queue and showed only the last result, so "(waiting to send)" did not exist there. **MAUI was AHEAD here, not at parity.** ⚠ **AND A COUNTED DRAWER NOW SAYS WHETHER IT BALANCED** (finding I, till 1.45.0). The platform has computed expected-vs-counted since this row was written; the till POSTed the Z, kept the STATUS CODE and threw the response body away — the only place either figure exists — so a drawer £20 short was accepted with a 201 and nobody at the counter was told. Recorded on the local row (schema v6) and shown on the line and in the header, SHORT or OVER, in red. ⚠ **The till still never COMPUTES either figure** — the rule at the top of this row is unchanged and load-bearing; it records what the platform SAID. ⚠ **And the Z now waits for its own day's sales to drain first**, because expected = float + **cash takings** + ins − outs: a Z that overtakes queued sales reports a shortage equal to every penny not yet sent, and — since the Z is terminal server-side — puts the day's real sales into quarantine behind their own close. Held per business day, so one stuck sale from last week cannot block every close from now on; **Pending only, not Failed**, or a refused sale would block this till for ever. |
| ⚠ **Reversing a Z close** (reopen a closed day) | 🟡 | ✅ | `POST /api/v1/cash-events` type `ZReopen` — accepted only on a day that IS closed | ✅ **Till 1.48.0 + backend 1.15.0, 2026-08-11.** Matt: *"I need to be able to override a Z-closed till… A supervisor or above needs to be able to reverse the close (In Web and MAUI)."* He hit it testing: a till closed for a test could do nothing at all afterwards, with no way back until midnight. ⚠⚠ **A COMPENSATING EVENT, NOT A DELETION** — deleting the ZClose would erase that somebody counted and banked the drawer, and the variance computed against it. Both stay: *"closed 17:32, reopened 17:41, by X, because Y"*. A reason is required; "no reason given" in an audit trail is worse than no trail because it looks like a record. ⚠ **SO EVERY GATE ASKS WHICH CAME LAST**, never whether a close exists — `Any(ZClose)` refuses a reopened day for ever. Three places ask (cash guard, sales gate, the till's local copy) and they must agree, so the rule is `CashDay.IsClosed` beside the ENTITY: `Plutus.Sales` cannot reference `Plutus.Cash`, so either home would have forced a second implementation, and a day open for a float and shut for a sale is a disagreement nobody finds until the figures stop matching. ⚠ **Ties go to CLOSED.** ⚠ The reopen is the ONE event a closed day accepts, on both sides, or the escape hatch is locked inside the thing it unlocks. New `pos.cash.reopen`, Owner/Company Admin/Store Manager/**Supervisor**, never Cashier — the person who counted the drawer must not be the only one who can quietly un-count it. Verified live: **8 grants across 2 tenants**, seeded by `RolePermissionReconciler` on boot. ✅ **WEB TILL CLOSED 2026-08-17 (web 1.11.0), §5b W-P5** — a "Reopen this day…" action on the Cash page, gated `pos.cash.reopen` from the W-P2 cached roster (Supervisor and up, never Cashier), with a **mandatory reason**. ⚠ **ONLINE ONLY, deliberately** — it is an audited supervisor action, not drawer money, so it is not queued: if it cannot reach the platform it has not happened. ⚠ The local guard uses `lastMarkClosesDay` (`cashRules.ts`), which takes **the latest mark** and never `some(ZClose)` — the same rule `CashDay.IsClosed` states server-side, and the reason the reopen is a compensating event rather than a deletion. Mutation-checked: the any-Z version, which would shut a reopened day for ever, kills 2 tests. Original note: Matt asked for BOTH surfaces; it is the next piece. |
| ⚠ **A closed till refuses at the DOOR** | ✅ | ✅ | — | ✅ **Till 1.48.0.** Matt: *"with the till closed, I can still add items… I added an item, it let me checkout, but then I got stuck."* ⚠⚠ **MY OWN GATE, IN THE WRONG PLACE.** The Z-closed refusal added that morning lived in `CheckoutCommit` — correct for the LEDGER, far too late for the OPERATOR: it let a basket be built, let the tender loop run, and refused at the moment of recording, with a customer waiting and the screen already behaving as though the sale were happening. `ExecuteItemAdd` now checks first and says **"Till closed"**, naming where to reopen. One indexed read turns a dead end into a sentence before anything is scanned. ⚠ **The commit gate STAYS** — that one protects the books, and a till cannot be the only thing enforcing a rule about the platform's own ledger: an older build or a replayed queue reaches the endpoint without passing through the screen. |
| **Loyalty** — members, tiers, credit | ✅ | 🟡 | ✅ **BUILT 2026-08-16 (till 1.61.0)** — a Loyalty tab listing everyone who is a member OR holds store credit, with tier, rate and live balance, searchable by name/email/member number. ⚠ **A LOOKUP AND THE SIGN-UP DESK, not a configuration screen**: tiers are created in the portal only (default 20), but **Add member** and **Set tier** live here as of **till 1.82.0** — they were on the Till tab, which item 4 correctly removed, and this screen had neither. ⚠ **Set tier asks *"whose tier?"* from the rows on screen** rather than by tapping a row: `TillTable` has no row-tap hook and it is shared with Cash, Statistics and Inventory, so an action sheet — already the till's idiom for *"which one?"* — is the smaller change. Members are labelled **name · membership number**, because two J Smiths are ordinary and putting the wrong one on Gold is money. ⚠ Inactive tiers are not offered (a retired tier still exists so history reads right; putting somebody new on one would resurrect it), and an empty tier list **names the portal** rather than reading as a fault. ⚠ Rows built in CODE, like Cash and Statistics — a blank tier tells a customer they have no discount and a blank balance makes credit look spent. ⚠ **NULL is not EMPTY**: a 403 or unreadable answer says so rather than rendering "no members", which would be a confident wrong statement about the shop. ⚠ **Uses `TillTable`** from 2026-08-16, so it sorts (member · tier · credit, credit NUMERICALLY), page-sizes 25/50/100 and pages — the first MAUI screen ever to do so. ⚠ **Still short of the web till: no EDIT, no renewal date, no lifetime spend, layout not aligned** — §5c item 6, ~1 d, and edit needs a ruling first because `PUT /api/v1/customers` can change an email and an email change silently redirects somebody's account. ⚠ 🟡 until hand-run §G18 + **§G38**. |
| **Users** — employee list/create + set password (legacy `/api/Employee`, `/api/Auth/SetPassword`) | ✅ | ✅ | ✅ **MAUI closed 2026-08-16 (till 1.70.0), WP8 / step 24** — the **people icon on the login screen**, which is where the web till puts it and the only place it is any use: the person who needs it is standing at a till nobody can get into. `Services/People/StaffDirectory.cs`, 28 tests. ⚠ **No client methods existed** — `GetEmployeesAsync` / `CreateEmployeeAsync` / `SetEmployeePasswordAsync` are new, on the shared `GetLegacyAsync` (the `businessId` **header**, and an empty list ≠ a failed call). ⚠⚠ **The password is part of ADDING somebody, not a second errand** — a person created without one cannot sign in anywhere, and stopping after the create leaves a row that looks finished and is not. If the password half fails the create is still reported, because telling somebody it all failed invites them to add the same person twice and the legacy controller will hold both. ⚠⚠ **The 100-row cap is SURFACED**: the legacy list is ordered by `CreatedAt`, so it is the **newest** staff who fall off — exactly who this screen is opened for. ⚠ Leavers are marked `(left)` and sort last, or somebody sets a password for a person who cannot sign in and blames the password. ⚠ A mismatched confirmation is its own message, never "too short" — mutation-checked, and the check found `CanSetPassword` and `PasswordProblem` each holding their **own copy** of the comparison; they are now one rule. ⚠ Passwords compare **ordinally** — case and trailing space are significant. Original note: MAUI's add-user command is a stopgap dialog reading *"not available in this version yet"*. ⚠ `StoreOptionsViewModel`'s `ExecuteAddEmployee`/`ExecuteViewAllEmployees` are **empty methods behind a commented-out menu**, and its command property is misspelt `AddEmployeeCommmand` against a binding to `AddEmployeeCommand` — dead either way; flagged for the removal sweep. | Roles and effective-permissions management are **portal-side**, not till capabilities — the MAUI parity target is this smaller surface. Overlaps WP8 (RBAC sync) but is a distinct screen. |

## B4. Tabs both have, in different shapes

| Area | Web | MAUI | Retrofit |
|---|:--:|:--:|---|
| Inventory CRUD | ✅ | 🟡 | **WP10 / step 25.** ✅ **EDIT (name + price) landed 2026-08-10**, against `PUT /api/Item/{id1}` — the same endpoint the web till uses in production. ⚠ **READ-MODIFY-WRITE, always**: that PUT binds the WHOLE entity, so a bare price change would clear `StockUntracked` or blank `BinnedAtUtc` — **restoring a withdrawn item to sale on every till in the estate**. `PlutusApiClient.UpdateItemFieldsAsync` fetches first and echoes everything back, and the client deliberately exposes no way to construct the payload from scratch. ⚠ **The ex price is DERIVED from the item's existing pair, never typed**: the server guards `|price − exPrice × rate| ≤ 2p` because free-typed ex-prices corrupted 47 live items (a £7.99 item with a £799.00 ex-price) and with them every downstream VAT figure. ⚠ **Needs an OPERATOR token** — the legacy base controller reads an `objectidentifier` claim in its CONSTRUCTOR, so a device token does not merely fail the policy, it 500s before the action runs. 🟡 not ✅ until CREATE, the stock ledger, categories and the Bin land — see below. **Original correction, 2026-08-10:** this row read ✅ / ✅ and was WRONG — MAUI's add/edit/stock screens wrote into the LEGACY local database, and the till client has no item-write endpoint at all (`PlutusApiClient` reads the catalogue and nothing more), so a till-created item reached no report, no other till and no VAT return. Since cutover step 11 the basket resolves items from the v2 catalogue, so it could not even be SOLD on the machine that made it. Both were also gated on the legacy `IsAuthorised`, which **crashed the app** rather than refusing. Hidden 2026-08-10 — see [`MAUI-retrofit.md`](To%20do/MAUI-retrofit.md) §10 L2. A ✅ here is what "reads as built and is not" looks like; the ⬜ is the honest state. |
| **Item search by NAME in the scan box** | ✅ | ✅ | **Closed 2026-08-10.** ⚠ **MAUI had none, and its absence read as an empty catalogue**: `TillViewModel.FindItem` called `FindByBarcodeAsync` and nothing else, so anything TYPED — "BAT" — was tried as an exact barcode, missed, and produced *"We can't find an item with that ID"* against 20,344 synced, sellable items. ⚠ `TillStore.SearchAsync` had been built, correct and tested since 2026-08-09 and was called from **nowhere** — the third finished component found sitting unwired behind a screen that looked broken (after `OutboxPusher.DrainAsync` and the catalogue browse). ⚠ Barcode is tried FIRST and stays exact: a scan is the hot path and must never open a picker in front of a queue. Matching is `SharedKernel.ItemSearch` (C1), never a `LIKE` written at the call site. `matchAllWords` is a per-device preference defaulting to **true on both tills** — two tills that merely *default* differently would disagree about the same query out of the box. |
| **Stock QUANTITY on the till** (read) | ✅ | ✅ | `POST /api/v1/stock/levels/bulk` | ✅ **2026-08-10 (till 1.37.0 + backend 1.9.0).** The column was **BLANK on every row** — it bound `Stock.Quantity`, a legacy EF navigation property nothing populates on a portal till, and a MAUI binding to a null path renders empty without complaint. **An empty stock column reads as ZERO**, so the till was telling anyone who looked that the shop holds none of anything. ⚠ **Three states, three renderings**: a number, **∞** for a deliberately untracked item, **—** for "never counted". Null arrives for BOTH of the last two and only the flag separates them; printing 0 for "never counted" gets the item reordered, and printing 0 for a carrier bag reads as a stock-out. ⚠ **Read after the rows render, never before** — making the list wait on the network would blank the screen exactly when the line is down, which is when browsing matters most; failure leaves the dashes. ⚠ **One bulk call per page, clamped to 200 client-side** because the endpoint TRUNCATES rather than refusing — 500 ids would return 200 answers and 300 silent gaps, each rendering as the honest "never counted". ⚠ Gate widened to accept `pos.reports.view` (third time, same defect — see the row below). Pinned by `StockLevelDisplayTests` + `StockLevelClientTests` (11), mutation-checked four ways. |
| Stock as a movement LEDGER (write) | ✅ | ✅ | `POST /api/v1/stock/movements` — a signed DELTA | ✅ **2026-08-11 (till 1.39.0 + backend 1.9.0).** Tap a row → **Adjust stock…** → write off / add → how many → why. ⚠⚠ **EVERYTHING IS A CHANGE, NEVER A COUNT, AND THE SCREEN SAYS SO.** The server does `level.Quantity += qtyDelta`, so a box labelled "quantity" filled in with what somebody counted ADDS their count to the existing one — 7 on the shelf, operator counts 7, stock becomes 14, nothing errors and nobody finds out until a stock take. Every prompt asks *how many*, in a direction already chosen, with the current figure beside it. ⚠ **The SIGN comes from the choice, never from typing** — a `WriteOff` must be negative or the server refuses, and asking somebody to get a minus sign right on a ledger at a counter is asking for the wrong answer. ⚠ **A REASON IS COMPULSORY** (the server refuses without one): an unexplained stock correction is indistinguishable from shrinkage being hidden. ⚠ **NOT QUEUED when offline, unlike a sale, and the message says so** — a sale is queued because the money moved regardless; a stock correction is a DECISION, and replaying one against a count that has since changed writes a wrong number. ⚠ **Not offered for an untracked item** — its level is meaningless by design. ⚠ **"Correct the count to N" is deliberately absent**: that is `/stock/takes`, which sits under a controller-wide portal gate covering inter-store transfers, so reaching it from a till would grant transfers by accident. A till adjusts; a stock take stays a portal job until it has its own gate. Pinned by `StockMovementClientTests` + `PosStockAdjustSeedTests` (12), mutation-checked three ways. |
| ⚠ **Who may adjust stock from a till** | — | `pos.stock.adjust` | — | **Matt's decision, 2026-08-11.** A new till-side code, seeded to Owner / Company Admin / Store Manager / **Supervisor** — and **never the Cashier**. ⚠ **Why a new code rather than one seed line:** a Supervisor holds NO portal permission, so under `portal.stock.adjust` alone a supervisor at the counter could not write off a damaged box — it would wait for a manager, and stock figures nobody trusts are how that ends. Granting them the PORTAL code instead was one line and the wrong shape: it also carries category create/rename/delete and price-list writes in the portal. ⚠ **The Cashier exclusion is load-bearing** — the person minding the shelf and the person who can alter its count must differ, or shrinkage stops being visible; a named test fails if that ever changes. ⚠ The endpoint accepts EITHER code, so a manager needs nothing new. ✅ **It reaches a live tenant on the next backend BOOT** — `RolePermissionReconciler`, added the same day after Matt asked why a manual step was needed at all. ⚠ Additive only: never removes a grant, never overwrites a ceiling a shop set itself. ⚠ **And the till now accepts EITHER code**, mirroring the server — before that fix an OWNER was refused by the till for something the platform allowed. |
| **The Bin — withdraw an item from sale** | ✅ | ✅ | `POST /api/v1/items/bulk` `{action:"bin"\|"restore"}` | ✅ **2026-08-10 (till 1.37.0).** Tap a row → *Move to the Bin…*, confirmed. ⚠ **NOT A LOCAL TIDY-UP, and the wording says so**: it stamps the PLATFORM, the feed carries a tombstone, and every read path on every till then refuses the item — **including tills that have been offline since**, which is exactly what a recall needs and exactly what somebody clearing clutter off a list does not expect. ⚠ **Nothing is destroyed** — the item keeps its id, so a restore does not split its sales history and past sale lines still resolve; the confirmation says that too, because an operator who thinks it is permanent will not use it when they should. ⚠ **The catalogue is re-synced afterwards** or the row stays on screen and gets binned twice — and the second attempt succeeds silently (`BinnedAtUtc ??=`), teaching the operator the button does nothing. ⚠ Gated on `inventory.bulk` (Owner / Company Admin / Store Manager), refused politely otherwise. **Restore is portal-side** — MAUI has no binned-items view yet. |
| ⚠ **A binned item stops selling on an OFFLINE till** | ✅ | ✅ | the feed's `Removed` tombstone | ✅ **Pinned 2026-08-10** by `CatalogueTombstoneTests` (4) — it was honoured on every read path and **tested nowhere**, which for a recall rule is the wrong way round. The feed sends `Removed: true` rather than omitting the row, because "not in this page" and "withdrawn from sale" are indistinguishable to a client that only receives upserts. ⚠ Now pinned across **scan, search (including the new brand field), and browse** separately — a new read path that forgets the tombstone puts a withdrawn product back on sale indefinitely, and browse is one tap from a basket. ⚠ Restoring returns the SAME id, so history does not split. |
| VAT-band consistency guard | ✅ | ✅ | ✅ **MAUI closed 2026-08-16 (till 1.70.0), WP10** — `Services/Inventory/ItemPricing.cs`, 23 tests, five mutants killed. ⚠⚠ **The interesting part is that the guard itself CANNOT FIRE, and that is correct.** MAUI derives the ex price from the band (`ExPriceFor`) instead of letting it be typed, so the pair is consistent by construction — `A_derived_pair_always_satisfies_the_guard` pins exactly that. `IsBandConsistent` is kept as a **backstop for the day someone re-adds a typed ex-price field**, which is precisely how 47 live items were corrupted. Do not delete it as dead code. ⚠⚠ **Writing the guard exposed a real defect in its own fallback**: when `/api/Tax/Index` doesn't know the band, the ex price carried the item's EXISTING ratio — so the £7.99/£799.00 item (ratio 100) repriced to £9.99 would have written a £999.00 ex price, *reproducing the corruption at a new price point*. Now `CarriedRatioIsUsable` refuses any pair with `ex > inc` (VAT is never negative) and falls back to ex == inc, and the operator is told the item is being saved VAT-free and that a band is the fix. Wrong in the visible direction beats wrong by 100×. Original note: the guard was in WP10's body but gated by no DoD until 2026-08-08. |
| **Portal-published VAT bands** (`GET /api/v1/vat/bands`) | ✅ | ✅ | ✅ **MAUI closed — and this row said ⬜ until 2026-08-11, when the work was already in.** `Client.Core/VatBandCache.cs` stores the **whole effective-dated timeline** (not just today's rate) with 10 tests including a future-dated change applying offline on the day; `Services/Storage/VatBands.cs` is the app's route to it, `TillCadence:283` refreshes it on the 60 s tick, and `TillViewModel:1942` reads it on a **selling** path for the Tax column. ⚠ So step 25e's "remaining ½ day" was already spent. Original note: **WP2c shipped 2026-08-08 (backend + web till).** The web till caches the bands and their whole effective-dated timeline at boot + every 60 s, and its last hard-coded VAT rate (the single-purpose gift-card `/1.2`) is gone. ⚠ **MAUI had NO WP home for this until 2026-08-08** — WP2c asserted "both tills cache it" and only the web till did, while `PlutusApiClient.GetVatBandsAsync` + `VatBandsResult.RateBpAt` sat built and uncalled. Now **WP5**, with a DoD proving a future-dated change applies offline on the day. ⚠ **Caching only "today's rate" is a bug**: the timeline is what lets an offline till apply a future-dated change on the day. |
| **VAT band on the sale line** (`LineMeta.vatBand`) | ✅ | 🟡 | **WP2c-exempt, 2026-08-08.** Zero-rated and exempt both declare `vatRateBp = 0`; the band is the only thing separating them, and it decides whether input tax is recoverable (Notice 706). **MAUI SHOULD send it** — resolve from the item's legacy `TaxId` against each band's published `legacyTaxIds`; `LineMeta.VatBand` exists in `Plutus.Contracts.Client`. **Leave it null rather than guessing** when the portal hasn't mapped the tax row. 🟡 not ⬜ because **the server backfills any line that arrives without one** (`VatBandStamp`, applied in `SalesIngestService` — the single choke point every channel passes through), so MAUI is correct-by-default and only needs to send it for cases the catalogue can't know. A single-purpose gift-card activation/redemption is `"standard"` by the voucher treatment, not its catalogue row — that IS such a case. |
| Category create / rename / reassign / delete | ✅ | ✅ | `/api/v1/categories` — list · create · rename · reassign · delete | ✅ **2026-08-10 (till 1.38.0).** Item list → **Categories**. ⚠ **THE 409 IS THE FEATURE, NOT AN ERROR** — the delete refuses while any item still references the category and says how many, and this flow turns that refusal into the OFFER to reassign rather than into a message. Reporting it and stopping is exactly what left MAUI with no route to a delete, which is what the old 🟡 meant. ⚠ **The refusal guards a LEGACY CASCADE**: `Item → Category` is `OnDelete(Cascade)`, so `DELETE /api/Category/{id}` silently deletes every item in the category — **and their sale lines and stock with them**. A test pins that the till only ever calls the v1 route. ⚠ **Reassign is ALL OR NOTHING and has no cap**, so the COUNT is in the confirmation before it runs — "move its items" sounds selective and is not. ⚠ **The last category cannot go** (`Item.CatId` is required) and the item count includes BINNED items, so a category that looks empty can still refuse — both surfaced in the server's own words. ⚠ **The direction of a reassign is easy to invert** and inverting it empties the wrong category; pinned. Gated on `portal.stock.adjust`. Pinned by `CategoryManagementClientTests` (8), mutation-checked three ways. |
| The Bin (soft delete) + untracked stock | ✅ | ✅ | `POST /api/v1/items/bulk` — `{"action":"bin"}` / `"restore"` | ✅ **MAUI closed 2026-08-10 (till 1.37.0, step 25d).** ⚠⚠ **THIS ROW SAID ⬜ UNTIL 2026-08-11 — a full day after it shipped, and Matt used the feature before the register admitted it existed.** That is the D3 reflex failing in the direction nobody watches for: a row left stale makes parity look WORSE than it is, so the gap list gets re-estimated and re-planned around work that is already done. Original note, for the record: *"FE5 concepts MAUI has no model for. WP10."* ⚠ It is a **soft** delete — `Item.BinnedAtUtc`, keeping the barcode every sale line and stock row keys on — and **restore stays portal-side** (Portal → Inventory → Bin → Restore); MAUI has no binned-items view, which is the honest remainder. ⚠ A binned item stops selling on an OFFLINE till via WP5's tombstones (`CatalogueItem.Removed`), and that rule was honoured on every read path and pinned by NOTHING until 25d covered scan, search and browse separately. |
| Reporting — cross-till, server-aggregated | ✅ | 🟡 | **WP11 / step 26 — first slice landed 2026-08-10.** The Statistics tab now shows **this till's real takings for today from the platform** (`GET /api/v1/reports/summary?level=till`), where a red "these reports read the OLD local database" warning used to be. ⚠ **The figure MUST come from the server**: a till cannot know its own takings — sales posted by the other device on the same till, sales pruned out of local history, and refunds taken at another counter against sales rung up here all belong in the number an operator counts a drawer against. ⚠ **The endpoint was gated on `portal.financials.view` ALONE**, so a Supervisor — who holds `pos.reports.view` and no portal permission — could close a day with a Z-read and be refused the takings they had just counted against. Now carries the `pos.*` alternative, like `/api/v1/sales` since step 19. ⚠ **The test that should have pinned that alternative had been passing for the wrong reason since step 19** — it assigned Store Manager, which holds BOTH portal reporting permissions, under a comment claiming it held neither. Found by mutation, corrected to Supervisor. 🟡 until the fuller reports (items sold, VAT, best sellers) and the cross-till lookup + reprint screen land. ⚠ The two LEGACY reports remain reachable and still warned about — a till migrated from NatApp holds real pre-cutover history in that file and this is the only way to see it. ⚠ **AND IT WAS READ ONCE, AT SIGN-IN** (finding N, till 1.44.0). `AppShell` builds every tab up front, so `LoadToday()` ran from a viewmodel constructor and the figure never moved again — a full day's trading against a total fixed at the moment somebody signed in. ⚠ **Nobody reported it, and nobody would have**: a stuck "(waiting to send)" announces itself, but a takings total eight hours stale looks exactly like a correct one — and it is the number a manager counts a drawer against. Found only by looking for siblings of J. Now refreshed on appearing and on every cadence tick while the screen is up. Original note: WP11. MAUI queries its **own** SQLite, so it can only ever show one till. ⚠⚠ **AND THE REPORTS TAB COULD NOT READ A SINGLE REPORT UNTIL 2026-08-18 (till 1.75.0 + backend 1.17.2)** — reported by Matt: *"it is saying This report couldn.t be read. You may not have permissions to see it. Or the till is offline"*. **Two independent faults, and the message named neither correctly.** ① `ReportsViewModel` built its client with `TillPlacement.TryCreateApiAsync()` — the **DEVICE** token. Every report endpoint gates `perm:*`, and `PermissionAuthorizationHandler` resolves RBAC by the token.s `NameIdentifier`; on a device token that claim is the **device id** (`PlutusTokenAuthHandler`, the `did` branch), so `Guid.TryParse` SUCCEEDS and the lookup then asks "what may this DEVICE do" — nothing, because grants hang off users. **403 on every report, for every operator, whatever their role.** Not a permission that needed granting: the wrong identity was asking. ⚠ `OperatorTokenProvider` exists for precisely this (step 19) and had **one** call site, `PlutusApi.GetOperatorAsync`, which this screen never used — the **fifth** component found fully built, tested and referenced from nowhere, after `OutboxPusher.DrainAsync`, the catalogue browse, `TillStore.SearchAsync` and `NoticesClient`. ② **Five of the six report endpoints lacked the `pos.reports.view` alternative** — only `/reports/summary` got it at step 26, so the step-26 fix was applied **to the endpoint that was reported rather than to the rule**. A **Supervisor** holds `pos.reports.view` and, by design, NOT ONE portal permission, yet can Z-close a day — so they could close a day and be refused the takings they had just counted against, on five of six reports. Both halves fixed; the gate is pinned by `An_operator_with_pos_reports_view_...` extended to all five URLs **as Supervisor**, and the test was **watched going red** against a reverted gate before being trusted — the same test had once passed for the wrong reason by using Store Manager, who holds both portal permissions. ⚠ The refusal message now separates **"no live sign-in"** from **"offline"**; the old single sentence blamed the network for an expired session. ⚠⚠ **AND THE SET IS NOW COMPLETE, INCLUDING DRILL-DOWN (till 1.85.0 + backend 1.17.3, 5c item 5).** The web till had eight report subtabs to MAUI's five. ⚠ *"Custom"* was never a missing report - it is the web till's separate custom-range takings tab, and MAUI's From/To pickers already apply a range to **every** report - so the real gaps were **Stock**, **Negative stock** and **drill-down**. All three are in, and `ReportCatalogue` took **one entry each** with no screen, no viewmodel and no XAML, which is the first time that framework claim (Matt, 2026-08-16) has been tested by a new report. ⚠⚠ **DRILL-DOWN IS THE ONE THAT TURNS A REPORT INTO AN ANSWER**: a takings figure says the day is 40 pounds light, and only the sale behind a row says why. A new **Sales** report lists every sale in the range across **every till**, and tapping a row opens it - lines with their VAT and discounts, how it was paid including change given, what has already been refunded or voided and why, and the totals. ⚠ A refund is itself a sale with a **negative** gross and is shown as one, because offering a refund as something to refund AGAINST cost 13.99 pounds twice on 2026-08-10. ⚠ Read-only, deliberately: refunding stays on the till screen where the ceiling and the cross-till cap are applied, and a second door to the money with none of that behind it is not a convenience. ⚠ `TillTable` gained an **opt-in** row tap (`onRowTap`, default null) so the four tables that predate drill-down cannot change behaviour, and only rows carrying a `DrillSaleId` do anything - a tap on a VAT bucket is inert and silent. ⚠⚠ **AND THE GATE WAS WRONG FOR THE FOURTH TIME**: `GET /api/v1/stock/levels` was `portal.reports.view` only, while the comment recording the THIRD fix sits on `POST /api/v1/stock/levels/bulk` **six lines above the endpoint it missed**. Every fix so far landed on the endpoint in use rather than on the rule. ⚠ Also found on the way: `GetAsync<T>` in the shared client **never caught `JsonException`** - hidden because every report until now deserialised an OBJECT, and `{}` is valid for one. The first caller to ask for a JSON **array** threw, and `No_report_throws_when_the_server_answers_with_nonsense` - a test written for exactly this - is what caught it. ⚠ Fixing it too widely broke `A_dead_network_is_an_outcome_not_an_exception`, whose point is that a dead line must carry its REASON; the catch was narrowed to the deserialisation faults alone. **7 new tests (598 to 605), one mutation-checked.** |
| **Which reports a till OFFERS** (the portal curates the menu) | ✅ | ✅ | `GET /api/v1/reports/catalogue` · `GET /api/v1/reports/published` (till-readable) · `GET/PUT/DELETE /api/v1/reports/publication` (`portal.company.manage`, audited) | ✅ **BOTH TILLS 2026-08-19 — ruling 5b(a).** Matt: *"Portal shows which reports a till can show."* `SharedKernel/ReportCatalogue` is the superset; a `ReportPublication` row is `(TenantId, TillId?) → KeysJson` with the more specific winning; the portal screen is **Locations & tills → Reports on the tills**. ⚠⚠ **THE PUBLISH DECIDES THE MENU, THE PERMISSION DECIDES THE DOOR** — two independent filters, both must pass, and a published-but-unreadable report is **not listed** rather than greyed out, because a greyed row leaks what other roles can see (`Published_does_not_imply_readable`). ⚠⚠ **NO STORED ROW MEANS EVERY REPORT.** A tenant who has never opened the screen sees yesterday's menu; defaulting to "nothing published" would have emptied the Reports tab in every shop on deploy. An **empty** list is a different state — deliberately publishing none — which is why never-chosen is `null` and not `[]`; collapsing them makes one inexpressible. ⚠ **Three fallbacks and the last is "everything"**: server → cached last-known-good → full catalogue. A till that loses its Reports tab to a network blink is worse than one showing a report an owner hid, so `null` from the client is "could not ask" and a malformed answer is never cached. ⚠ Unknown keys are DROPPED on read (old data must not break a till) and REFUSED on write (a typo must not look saved and do nothing). ⚠ Rebuilt **on appearing**, not only in the constructor — `AppShell` builds every tab at sign-in (pitfall 17), so otherwise a change waited for a sign-out, which is the §5c item 7 complaint again — and only when it CHANGED, because re-filling the picker resets the selection. ⚠ Both clients move the operator off a tab that has just been withdrawn; the web till seeds from its cache before the server answers, so without that it would render a report no longer on its own menu. ⚠ The web till sends **no till id** and follows the tenant default: a browser session carries a token, an employee and a name, and has no till id to send. Pinned by `ReportCatalogueTests` (11) + `publishedReports.test.ts` (12) on the same questions — C2 row, three implementations. ⚠ Keys are WIRE VALUES stored in the database: **add, never rename**, and both suites pin the shipped set. **Hand-run §G54; §G54a must pass first.** |
| Store Information | read-only ✅ | local editor ⬜ | WP6 — the two are *inverted*; this is a deletion, not a build. |
| Settings — local prefs | ✅ | ✅ | — |
| Settings — device enrolment & identity | ✅ | 🟡 | **WP4 + WP16a.** MAUI gained a **Plutus tab** 2026-08-08: server address, enrolment code → `POST /api/v1/tills/enrol`, connection state, device status, and buttons that exercise the heartbeat and catalogue feed. Secret goes to platform `SecureStorage`, device id to `Preferences` — ⚠ never the SQLite file, which gets copied off machines during support. 🟡 until the un-enrol *request* flow and the local store are wired. |
| First-time startup | ➖ | 🟡 | **Portal-first since 2026-08-08** (Matt: *"The till is moving to the portal being the 1st place you start, not the local app"*). First run now opens **Connect to Plutus** — server address + enrolment code — because a till is *born in the portal*: company, store, till and code are created there, and the app's only job is to claim that identity. ⚠ **Legacy "Setup" and "Third-party transfer" are retitled `(Legacy)` and pending removal** — see the warning below. 🟡 until they go and WP8 lets a synced operator sign in. |

## B5. Platform citizenship

Most of this exists in the web till and is **absent from MAUI**. None of it was in the original
parity analysis. ⚠ **Two rows now run the other way** — connection status and offline sign-in are
places the *web till* is behind. Parity is not a synonym for "catch MAUI up".

| Capability | Web | MAUI | Backend | Notes |
|---|:--:|:--:|---|---|
| Offline trading + outbox | ✅ | 🟡 | `POST /api/v1/sales` | **Cutover step 11, 2026-08-09: checkout now COMMITS a platform sale.** `CheckoutCommit` assembles an `IngestSaleRequest` and `TillStore.CommitSaleAsync` writes the sale and its outbox entry as ONE row in ONE transaction, allocating `DeviceSeq` inside it. Until then the till wrote a legacy EF graph and called `db.Save()` — the sale existed on that machine only and appeared in no report, no VAT return and no other till's history. ⚠ **COMMIT BEFORE PRINT** is now structural, not incidental: a printer failure is a reprint problem, never a money problem. ⚠ A failed commit does NOT clear the basket — clearing would lose the sale and the evidence together. ✅ **Cutover step 13, 2026-08-09: the outbox now DRAINS.** `OutboxPushService` + `TillCadence` — the till's ONE background clock, ticking every 60s exactly like the web till's `App.tsx`: heartbeat → outbox drain → catalogue (only when the beat says it moved) → notices. Before it, `OutboxPusher.DrainAsync` was referenced **nowhere in the app**, so every sale the till committed sat in the queue for ever while the till looked healthy. ⚠ **One timer, not four** — four drift into each other and fire together behind a rate limit sized for one. ⚠ **Drain runs BEFORE the catalogue pull**: a sale already rung up is worth more than a price nobody has asked for, and on a slow link the catalogue can take the whole interval. ⚠ **The basket owns the catalogue sync** (`TillCadence.BasketIsOpen`) — a sync mid-basket would price one line from before the tick and one from after, in a single sale. The drain and heartbeat are NOT held. ⚠ **Started at app start, not sign-in, and never stopped on sign-out** — a till on its login screen after close still holds the day's takings. ⚠ Quarantine is reported separately and never softened: 202 is terminal, so it is the one outcome that will not fix itself. The Plutus tab's **"Send queued sales now"** is the only place a stuck queue is visible. WP3. |
| **Taking payment at all** (the tender buttons) | ✅ | ✅ | — | ⚠ **Cutover step 13b, 2026-08-09 — a portal-provisioned till could not complete a sale AT ALL.** The checkout read the legacy local `PaymentMethodModel` table, seeded only by `Database.Init()` (the legacy first-run path). A portal till never runs it and has no local database, so the payment sheet rendered **zero buttons** and the `paid != sale.Total` loop could never terminate — the operator was trapped mid-checkout with nothing but Cancel, and nothing threw or logged. Every dev machine hid it: they were migrated from legacy installs that had already seeded Card and Cash. Now `Services.Sales.TillTenders` — the **fixed** shared tender set (binding default 13), not a roster. ⚠ Each name must map back through `Tenders.FromMethodName` to its own byte, pinned by test, because the wire byte is derived from that STRING at commit. ⚠ Change is cash-only, cashback is card-only (the pairing is easy to invert — mutation-checked), and a refund gives neither. ⚠ `Online`/`Credit`/`GiftCard` are deliberately absent: the first is webstore ingest, the other two are online-only and unbuilt, and a button that must fail is worse than no button. |
| **Connection status — network vs server vs revoked** | 🟡 | 🟡 | `GET /api/v1/ping` + `GET /api/v1/tills/devices/{id}/status` | **WP16a.** Shared rule in `Plutus.Client.Core.ConnectivityProbe` (C1). ⚠⚠ **THIS ROW HAD MAUI AT ⬜ AND MAUI WAS BUILT** — corrected 2026-08-21, and `MAUI-retrofit.md` §7 carried the same error in the same words (*"0 references on `LoginView`/`LoginViewModel`"*). **Both came from grepping `ConnectivityProbe`, which MAUI never names directly**: it reaches the probe through `Services.Connectivity.TillConnectionCheck`. `LoginViewModel` has `ConnectionSummary`/`ConnectionDetail`/`ConnectionColour`/`IsCheckingConnection` + `RefreshConnectionAsync`, and `LoginView.xaml` binds all four plus a tap-to-refresh and the clock-skew sentence (72–97). ⚠ **A grep for a shared type is not a check for a capability when a wrapper sits between them.** ⚠⚠ **The real gap was the WEB till's LOGIN screen, which had no indicator at all** — 111 lines of email, password and error, so a dead backend was indistinguishable from a wrong password. ✅ **Closed 2026-08-21** with `connectionCheck.ts`, the C2 twin of the probe: same four states, same sentences, same dot, same tap-to-refresh, `verifyIdentity: false` on both tills. ⚠ **Still 🟡 on both, and honestly so.** MAUI's has never been hand-run; and the web till's APP-WIDE indicator (`App.tsx:64`) is still `navigator.onLine`, which reports the interface — it is the right signal for *"is there a network"* and the wrong one for *"is Plutus there"*, so the login screen is fixed and the header badge is not. Revocation on the web till is `deviceStanding.ts` (W-P1), which is a different question and already correct. |
| **Offline sign-in with an expiry** | 🟡 | ✅ | `GET /api/v1/tills/{id}/operators` | **WP8 + WP16b, built 2026-08-08.** Horizons tiered and shared (`SharedKernel.OfflineCredentials`, C1). ✅ **WEB TILL CLOSED 2026-08-17 (web 1.11.0), §5b W-P4** — `src/offlineLogin.ts`, 30 tests, mutation-checked. All five horizons mirrored exactly (**7d money-out · 30d sell · 3d warn · 15min idle-lock · 12h session**), the three trust tiers, and the **default-deny sell floor** (`pos.sell` + `support.tickets` only). ⚠⚠ **THE FALLBACK FIRES ON A TRANSPORT FAILURE ONLY, NEVER A 401** — a 401 is an *answer* (it is where `AuthController` turns away a deactivated employee, FE9.2), so falling back to the cached hash after one would let a disabled operator sign in offline **past their own refusal**. `api.ts` now marks HTTP answers with `serverAnswered` and `LoginPage` checks it; ⚠ **never `navigator.onLine`** for that decision, which reports the interface and says "online" in a shop whose broadband is down. ⚠ Staleness is measured from the roster envelope's **`asOfUtc`** — the SERVER's clock; MAUI uses `LocalOperator.UpdatedAtUtc` because its store is relational, and the web till's envelope stamp is the same fact. ⚠ A **future-stamped** roster clamps to fresh rather than refusing, because failing the other way stops a shop trading over a wrong clock. ⚠ The offline session carries **no platform token**, so `perm:*` endpoints stay unreachable while queued sales keep flowing on the device token — the same shape as MAUI. ⚠ Session end is the **earlier of the 12 h cap and the business-day rollover**, because a session spanning two business days leaks yesterday's operator into today's X/Z breakdown. Original note: **the web till still cannot sign in offline at all** — a row where MAUI was AHEAD. **Web till ⬜ — WP17.1.** ⚠ It said "needs a WP of its own" until 2026-08-11, which is precisely the "not 'TODO', not 'later'" that D3 forbids: WP17.1 already existed and covered it, and the cell simply did not cite it. A ⬜ whose Notes name no work package is indistinguishable from one nobody has thought about. |
| Device enrolment | ✅ | ✅ | `/api/v1/tills/enrol` | **WP4 · MAUI closed 2026-08-09** (cutover step 4). Enrolment goes through `Client.Storage.EnrolmentFlow`, so the till records **where it is**, not just who it is: `TillId`, `StoreId`, `BusinessId`, `TenantId`, `ServerUrl` into the v2 store's Meta. ⚠ It called `api.EnrolAsync` directly until then — the credential was saved and the store learned NOTHING, so every screen needing a storeId had nothing to read and the failure was silent because enrolment itself succeeded. ⚠ `BusinessId` is the LEGACY business id, not the tenant id: `DeterministicGuid.ForItem` seeds from it, so the wrong one diverges every item id from the web till's, permanently and without a symptom. Placement is re-read on **every start** (`TillPlacement.RefreshInBackground`) because a till can be MOVED between stores in the portal — and that is also what self-heals tills enrolled before this landed. ⚠ The archive gate (D-defaults §9.3) stays OFF until cutover step 21 gives the app a way to archive; turning it on first would refuse enrolment with no way through. |
| ⚠⚠ **A REVOKED TILL STOPS TRADING** (device-status poll on the beat) | 🟡 | ✅ | `GET /api/v1/tills/devices/{deviceId}/status` | ✅ **MAUI 2026-08-16 (till 1.70.0), step 21 — and the WEB TILL is now the one behind.** ⚠⚠ **Why it must be polled at all:** device tokens are HMAC bearer tokens with **no server-side denylist**, so revoking a device in the portal does **not** invalidate the token it already holds — a lost or stolen till kept selling for up to its **12h TTL**. `ConnectivityProbe` knew how to ask, but only ran at **sign-in and on the Plutus tab**, so a till revoked mid-shift was never told. Now on the 60s beat (`TillCadence` step 6 → `TillCadence.DeviceRevoked` → the login screen). ⚠ Asked via device status, **never by minting a token** — that endpoint is 5/min per IP, so polling it makes a healthy till report itself revoked (429), and tills sharing a shop's public IP do it to each other. ⚠⚠ **Only an explicit `Revoked` stops anything.** A failed poll, a 401/403, a **404** and an unknown status all keep trading — stopping a shop on a network blip is a worse outage than the one this prevents, and it would hit the flakiest sites first. `PendingRemoval` trades on purpose: halting a till the moment somebody *requests* it back would make un-enrolment a way to take a shop down. Same load-bearing rule as `OperatorRevocation`, and pinned the same way — `DeviceRevocationTests` (19), mutation-checked in **both** directions (a failed poll stopping the till kills 9; folding `PendingRemoval` into the stop kills 2). ✅ **WEB TILL CLOSED 2026-08-17 (web 1.11.0), §5b W-P1** — `src/deviceStanding.ts`, 21 tests mirroring the .NET file case for case, both mutants killed (a failed poll stopping the till kills 10; folding `PendingRemoval` into the stop kills 2). ⚠⚠ **It polls WITHOUT A SESSION** — its effect deliberately has no `session` guard, because a stolen till is revoked while it sits at a login screen, which is exactly when nobody is signed in. ⚠⚠ **The block is BEFORE the login screen**: gating after login would let somebody sign in to a machine the platform has finished with, and a revocation means the hardware stops being a till, not that one operator stops using it. ⚠ **Latched in IndexedDB (`deviceRevoked`) so it survives a reload** — a revoked till must not come back by pressing F5 — and cleared by any other explicit answer, so re-enrolling in the portal un-blocks it without somebody clearing browser storage by hand ("Check again" on the blocked screen forces that poll). Original note: the web till was on `navigator.onLine` (WP17.3) — the network **interface**, not the server — so a revoked browser till kept trading and it could not tell a revoked till from a dead one. |
| Un-enrol request + manager approval | ✅ | ✅ | `/api/v1/tills/unenrol-request` | ✅ **MAUI closed 2026-08-16 (till 1.70.0), WP4 / step 21** — *"Ask for this till to be removed"* on the **Plutus tab**, beside *Forget this till*. ⚠ There was **no client code at all** — `grep unenrol` returned nothing in the app, so the endpoint (whose gate was fixed in step 19 precisely so a till could call it) still had no caller. ⚠ **On the Plutus tab, not Settings** — the web till puts it in Settings, but MAUI keeps every device-identity action together (enrol, forget), and splitting them would leave two screens that both half-answer "what is this till". ⚠⚠ **The wording keeps it apart from *Forget***: asking is reversible and the till keeps trading; forgetting is immediate, local, and needs a fresh enrolment code from somebody who may not be in the building. It is **not** styled as destructive, because nothing is destroyed. ⚠ It never reports a request that did not reach the platform — a manager would wait for a queue entry that never arrives. |
| Heartbeat / fleet status | ✅ | ✅ | `POST /api/v1/heartbeat` (**built 2026-08-08**) | **WP5.** Presence is in-process (`TillPresence`, 2min ONLINE / 5min STALE) — a fleet beating every 60s must not be a MySQL write per till per minute. ⚠ A failing heartbeat is **silent**: it must never stop a till selling. ✅ **MAUI closed 2026-08-09** — `TillCadence` calls `BeatAsync` on its 60s tick. ✅ **The WEB till closed 2026-08-10, and had never beaten at all**: the endpoint shipped 2026-08-08 with only a MAUI caller, so the surface *most* of the estate runs was permanently absent from the fleet list, and it read as "those tills are switched off" rather than "nobody wrote the caller". `api.ts sendHeartbeat()` + a 60s interval in `App.tsx`. ⚠ **Silent when the browser has no device credential** — an un-enrolled tab has no identity to report, and inventing one would put a phantom till in the list. ⚠ **The reported version is PERSISTED on `Device`** (`AppVersion`, `AppVersionReportedAtUtc`), written only when it CHANGES so the common beat stays a pure read. Presence alone forgot every version on a backend restart — which is exactly when somebody is looking at the fleet list. |
| Hardware-agent telemetry (agent version/health → Locations) | ✅ | ✅ | `POST /api/v1/tills/agent-status` (`TillsController.cs:283`) | ✅ **MAUI closed 2026-08-17 (till 1.71.0)** — `Services/Hardware/AgentReporter.cs` on the 60s beat (cadence step 6b). ⚠⚠ **This row said ➖ — "MAUI drives OPOS directly and has no agent to report on" — and that stopped being true on 2026-08-10**, when MAUI moved onto the agent's print route (see the HARDWARE ROUTE row below, which records the move). The two rows disagreed for a week. **The visible symptom was a portal fleet list where every MAUI till read "agent unknown" beside browser tills that reported properly** — which looks like a missing agent rather than missing reporting. ⚠ MAUI already knew the version all along: `TillAgentPrinting.ResolveAsync()` reads it on the printing path. Only the telling was absent. ⚠ **WHEN to send is a C2 twin** — `Client.Core/AgentReporting.cs`, shared with the web till's `hardware.ts`: on a **change**, or every **6 hours**. MAUI's beat is 60s against the web till's 5 minutes, so sending every tick would be ~360× the useful traffic to overwrite a row with what it already holds — and this is explicitly telemetry the server overwrites in place, writing no `AuditLogs` row. ⚠ A **null** agent version is a *reading*, not silence: "this PC has no agent" is a fleet fact, and skipping it makes that till identical to one that has never checked in. ⚠ `_lastSent` is recorded **only on a successful POST**, or a failed send would suppress the retry and hide an offline printer for six hours. `AgentReportingTests` (12), mutation-checked. |
| **Pick-from-floor notifications** (web sale sold shop-floor stock) | ✅ | ✅ | `GET /api/v1/notifications?unackedOnly=true` + `POST /api/v1/notifications/{id}/ack` | ✅ **MAUI closed 2026-08-16 (till 1.70.0), WP5b.** `Services/Notices/Noticeboard.cs` on the 60s beat + a banner at the top of `TillView`. ⚠ **The rules were never the missing part** — `NoticesClient` shipped 2026-08-09 with 23 tests and was referenced *once* in the whole app, in a comment. What was added is the caller, the screen, and one new rule: **a FAILED poll does not clear the board** (`Noticeboard.Apply`, mutation-checked both ways). `NoticesOutcome`'s own header warned that a caller confusing "no answer" with "nothing to show" would "quietly clear a live incident banner the moment the network hiccups"; the board is marked stale instead, and the stale line shows only alongside something. ⚠ **On the SELLING screen**, because that is the only page an operator is guaranteed to be looking at — a pick note exists to win a race against somebody selling the last one off the shelf, and a notice on the Plutus tab is a notice nobody reads. ⚠ Acking removes the row immediately rather than at the next beat; sixty seconds of it still sitting there reads as "it didn't work". ⚠ Announcements are **not** acknowledgeable and the button is hidden, not disabled. ⚠ Pinned by `NoticeboardTests` (11) **and `NoticeboardBindingTests` (4), which parse the XAML** — MAUI bindings fail silently, and on this banner the failure looks exactly like success, because an empty banner is what a till with no notices is supposed to show. Original note: Phase 6. Banner + acknowledge on the till (`App.tsx:264-272`), polled on the 60s cadence. ✅ **FIXED SERVER-SIDE 2026-08-09 (WP17.4)** — `GET /api/v1/notifications` now filters by the caller's store, derived from the DEVICE TOKEN and never from the query string. That **deletes the C2 twin** rather than pinning it: a per-client filter is a rule every future till has to re-implement correctly. MAUI filtered (`NoticesClient.IsForStore`), the web till did not (`App.tsx:130` set whatever arrived), so in a multi-store tenant staff at one shop hunted stock that was never on their shelves while the shop that had it assumed the other branch had dealt with it. ⚠ Notes with NO store stay visible to everyone — they are tenant-wide by construction. Pinned by two `PickNotesE2eTests`, mutation-checked. ✅ The two mis-gated endpoints were fixed 2026-08-07 (they were gated on `perm:sales.ingest` — a *scope*-policy name used as a *permission* code, which no RBAC role can hold, so every till poll 403'd silently into its `.catch`). ⚠ Their tenant isolation rests entirely on the **global query filter** (`WebstoreNotification` is in `MySqlDbContext.TenantOwned`), not on a predicate in the controller — pinned since 2026-08-09 by `A_till_cannot_see_or_ack_another_tenants_pick_note`, which was mutation-checked against `IgnoreQueryFilters()`. |
| **Announcements banner** (maintenance/incident) | ✅ | ✅ | `GET /api/v1/announcements/active` | ✅ **MAUI closed 2026-08-16 (till 1.70.0), WP5b** — same banner as the pick notes, announcements first (a platform-wide reason something is failing outranks a job). ⚠⚠ **Closing it exposed that the two tills DISAGREED**: `NoticesClient.ShowsOnATill` is a deny-list on `Info`, but the web till had an inline **allow-list** (`severity === "Maintenance" || severity === "Incident"`), so a severity a later backend added would have shown on MAUI and been **silently swallowed** on the web till — the precise failure the shared rule was written to prevent, sitting in the other till the whole time. The web till now calls `showsOnATill` in `api.ts`, pinned by `notices.test.ts` (5, mutation-checked against the old allow-list). See the new C2 row. ⚠ Severity picks a COLOUR only; an unrecognised one still shows and simply gets the maintenance accent. Original note: Shared half done 2026-08-09 — `NoticesClient.ShowsOnATill` is the one home for *which severities reach a till*: **`Info` is portal-only**, and an **unrecognised severity SHOWS** rather than being swallowed, so a severity added by a later backend can't silently blank itself on every till. ⚠ **MAUI is ⬜, not 🟡 — corrected 2026-08-10.** The row said "pending the banner XAML", which
implied the rest was wired. It is not: `NoticesClient` appears in the AppClient **exactly once, in a
comment**, so nothing calls it and nothing puts it on `TillCadence`'s 60s tick. **This is the FOURTH
component found fully built, tested and referenced from nowhere** — after `OutboxPusher.DrainAsync`,
the catalogue browse and `TillStore.SearchAsync`. Remaining work is a cadence step **and** the XAML,
not the XAML alone. |
| **Help / support tickets** | ✅ | ✅ | `/api/v1/support/tickets` | ✅ **MAUI closed 2026-08-16 (till 1.70.0), WP5b** — **Settings → Help and support**: raise a ticket, read the thread, reply. ⚠ **MAUI had no route to support at all**, so a shop on the MAUI till could not ask for help from the till. ⚠ **A dialog flow, not a tab** — every comparable MAUI flow works this way (printer, categories, stock), and a Help *tab* would break the §B7 check that the tab names match the web till, which reaches Help from a button. ⚠⚠ **No `Modal.ShowAsync` at the call site**: `InputAlertHelper` gates on that semaphore internally, and the redundant outer guard is the exact defect that stopped the till taking money on 2026-08-13. ⚠ **Never queued offline** — a ticket is somebody asking for help *now*, and parking it in the outbox would tell them it had been sent; the flow says the connection is down and that nothing was sent. ⚠ The thread lands in a dialog, so it is capped at 12 messages and **says how many are hidden** — silently dropping the older half lets somebody answer a question already answered. Pinned by `SupportDeskTests` (17, mutation-checked) and `SupportLabelsTests` (12). Original note: Closes the `support-heavy` churn signal. |
| **Component version — each till its own** | ✅ | ✅ | `GET /api/v1/ping` returns the **backend's** | Matt, 2026-08-08: *"each till needs a specific version as they will end up diverging when you have specific Windows or Linux challenges."* One file per deployable in **`versions/`**: `till-web.txt` · `till-maui.txt` · `backend.txt` · `portal.txt` · `agent.txt` · `platform.txt` (the shared libraries). **`MAJOR.FEATURE.FIX`** — see [`versions/README.md`](../versions/README.md). ⚠ **A shared number was the first attempt and was wrong**: a Windows-only printer fix would have forced the web till to claim a release it had no changes in, and left "broken on 1.4.2" unanswerable. ⚠ **No surface may hardcode its own** — one nobody bumps names the wrong build in every report against it. Pinned by `ComponentVersionTests` (17). |
| ⚠ **The checkout SAYS which card flow this shop is on** (WP14) | 🟡 | 🟡 | `GET /api/v1/payments/gateway/active` — `Provider` / `Label` / `Integrated` | ✅ **MAUI CLOSED 2026-08-21 (WP14).** It was the last ⬜ on the do-first list that was genuinely a ⬜: **0 references** to `PaymentGateway` anywhere in a MAUI view or viewmodel, against a shared half with 15 tests. ⚠⚠ **THE CASE THAT EARNS IT IS THE MIDDLE ONE.** A tenant who has chosen a real provider with **no wired integration** is still on the manual flow — and *every* provider on this platform reports `Integrated: false` today, so that is not the rare case, it is the only one a real shop meets. A cashier reading only *"Card via Worldpay"* stands waiting for a terminal prompt that is never coming. ⚠ **No extra round trip**: `GatewaySurcharge` already fetched the whole `ActiveGatewayDto` at checkout-open and kept two of its five fields. Renamed `GatewaySettings`, because a name that describes what it *kept* rather than what it *asked for* is how the other three stayed invisible for two weeks. ⚠ **The cache holds the server's ANSWER, not the decision** — `Provider`/`Label`/`Integrated` go to Meta and `PaymentGateway.Resolve` runs on the way out, so the day the rule changes, every till already in a shop changes with it. An upgrading till has no such keys, which reads as `Provider = null` → **standalone**, the flow that works when nothing else does. ⚠ Sentences are `CheckoutAlert.CardHintFor`, **word for word the web till's** and returning plain `HintSpan` records rather than a `FormattedString` — the MAUI type derives from `Element` and throws a `COMException` outside a UI host, which would have left the one screen whose defects all shipped untested untestable again. 7 tests. ⚠ **The web till was ✅ and is now also pinned** — its three cases were an inline ternary that had drifted from .NET in three ways; see C2. ⚠ **🟡 on both: never seen by a person**, and Kapow is on the standalone default, so the only visible line today is the *"take payment on the chip & pin terminal"* one. |
| **Card surcharge** (tenant-set fee on card tenders) | 🟡 | ✅ | `GET /api/v1/payments/gateway/active` carries `surchargeBp` + `surchargeFlatPence`; portal Company → Card payments sets them (typo-capped 10% / £5, audited) | **Built 2026-08-09** on the gateway settings row — the legacy `PaymentMethod.Charge` could never carry it because that table is GLOBAL, one tenant's fee would have been every tenant's. MAUI adds a REAL line against the provisioned `CARD-SURCHARGE` item (`CheckoutCommit.SurchargeItem`): fee = `CardSurchargeVat.FeePence` (percent + flat, the shape of an acquirer's own pricing), VAT = `CardSurchargeVat.PairFor` — **the fee follows the basket** (Bookit C-607/14 / NEC C-130/15: further consideration for the main supply), zero on zero-rated goods, blended on mixed, NEVER a hardcoded rate. Applied once per sale, card tenders only, never on refunds; offline uses the last-known setting from Meta. ⚠ Replaced a legacy path broken twice over: misspelt resx key (`CardChangeNote`) and a money-carrying `BasketNote` the commit guard refuses. ⚠ **Web till ⬜, deliberately**: it reads `charge`/`minimumCharge` off the legacy wire and ignores them (`CheckoutDialog.tsx:54,112`) — no tenant charges a fee today (UK consumer surcharges are BANNED since 2018-01-13; the portal helper text carries the warning), so the web half rides WP15's checkout reshape rather than a one-off TS edit nothing can typecheck on this box. ⚠ **2026-08-11 — one leg of that reasoning is now void.** "Nothing can typecheck on this box" was true and is not any more: the Mac builds the web till on Node 26, and `Plutus.Frontend.WebApp` now carries **vitest and 19 tendering tests** (`src/till/tendering.ts`), so a change here can be typechecked, tested and built like any other. The other reasons still hold — no tenant charges a fee, UK consumer surcharges are illegal, and WP15 reshapes this checkout regardless — so it stays ⬜ **on those grounds alone**. ⚠ Noted because a ⬜ justified partly by a constraint that has since lifted is how a deliberate gap quietly becomes an accidental one. ⚠⚠ **WEB TILL CLOSED 2026-08-17 (web 1.11.0), §5b W-P7** — and the note above is exactly why it got built rather than waiting for WP15: the reasons for deferring had been decaying one at a time, which is the pattern this register exists to catch. `src/till/surcharge.ts` is the C2 twin of `CardSurchargeVat` + `CheckoutCommit.SurchargeItem`, **32 vitest cases using the .NET test's own vectors**, six mutants (one of them instructive — see C2). `CheckoutDialog` builds the fee when a **card row holds money** (the web equivalent of MAUI picking a card method), folds it into what is owed exactly as `TenderLoop` does (`total += surcharge`), sends it as a real `CARD-SURCHARGE` line and itemises it on screen and on the receipt. ⚠ **Built at checkout, never stored in basket state** — the ONE place the web till deliberately differs from MAUI's mechanics: MAUI adds the line to `Basket`, and a fee line in the web till's *persisted* basket would survive a cancelled checkout, a park/recall and a switch to cash. A phantom fee on a cash sale is money nobody authorised. ⚠ Last-known-good in `localStorage`, mirroring MAUI's Meta cache, so the fee does not vanish offline and reappear online. ⚠ Still DORMANT for Kapow (rate zero → no line, no behaviour change), which is precisely how it could have sat unnoticed until the first tenant that charges a fee. |
| **Receipt off the COMMITTED sale** | ✅ | ✅ | — | **Cutover step 14, 2026-08-09.** `PosPrinterManager.SetUpSalePrint` now takes `Printing.ReceiptSale`, built from the payload the platform accepted (`CommitOutcome.Request`), not the legacy `SaleModel`. ⚠ **The barcode had been EMPTY since step 11**: the footer printed `SaleModel.Id`, and step 11 deleted the legacy `db.Save()` that assigned it — nothing threw, and the barcode is the ONLY way a customer's receipt finds its sale again for a reprint or a receipt-led refund. It now carries the platform `saleId`, which is what step 15's read path keys on. ⚠ Totals are the committed ones, never a re-sum of the basket — two opinions about one sale means the paper and the record can differ by a penny with no way to tell which was charged. ⚠ Timestamp is LOCAL (the payload is UTC); a receipt an hour out gets queried at a chargeback. |
| ⚠ **How a receipt reaches paper — the HARDWARE ROUTE** | ✅ | ✅ | `POST http://127.0.0.1:9123/print` — the **Plutus Till Agent**, a tray app on the till PC | ✅ **Closed 2026-08-10 (till 1.33.0).** Matt: *"I still cannot see a printer, it says wifi is turned off, but I do not understand what this means? The webtill can see the receipt printer fine."* Both halves were true and had ONE cause — **the two tills were on different hardware routes**. The web till has always POSTed a rendered document to the agent, which prints through the **ordinary Windows print queue**, so every driver-installed printer is available to it. MAUI asked Windows for a `PointOfService` device: a specialist driver profile almost no receipt printer ships, so the list was empty — and the picker Windows shows for that selector is the **generic device chrome**, which fills an empty list with its stock advice about Bluetooth and Wi-Fi Direct radios. ⚠ **"Wireless is turned off" was never about the printer.** It is the picker answering a question nobody asked, which is exactly why the screen was impossible to act on. MAUI now speaks the same route: `Client.Core/TillAgentClient.cs` + `ReceiptDocumentBuilder`, paired from Settings → Receipt printer with the agent's own code. ⚠ **The drawer rides WITH the print job** — one round trip, and it opens as the paper starts moving, as it did on OPOS; the checkout resolves which route it is on BEFORE dispatching anything, or agent and OPOS would both kick it. ⚠ **Graceful degradation is structural**: every method returns false rather than throwing, so no agent / wrong code / printer off all leave a committed sale alone. OPOS survives one level down for a till with a genuine POS device. Pinned by `TillAgentClientTests` (9) + `ReceiptDocumentTests` (11), mutation-checked four ways. |
| ⚠⚠ **EVERY control that prints a receipt uses that route** (D6b) | 🟡 | ✅ | — | ⚠⚠ **REPORTED BY MATT 2026-08-21 AND FIXED THE SAME DAY.** *"If you complete a sale and print no receipt, the receipt is shown (do not change) but if you try to print it from the next screen, it comes out on the A4 printer."* The web till's receipt dialog called `window.print()` **unconditionally** — the browser path, which goes to the **OS default printer**, and in a shop that is the office A4. So an operator who declined the ask and then changed their mind sent a customer's receipt to another room. ⚠⚠ **THE ROW ABOVE WAS ✅/✅ AND STAYED TRUE THE WHOLE TIME** — the *route* existed and worked; what was missing was every control USING it. **A capability row cannot see a caller that skipped it**, which is why this is now its own row. ⚠⚠ **AND THE FIX ALREADY EXISTED TWICE.** W-P6 closed the identical fault in `SaleDetailDialog` on **2026-08-17** (its header still reads *"this dialog could already reprint, but only through `window.print()`"*), and `TillPage` had the agent-first logic from FE3.3 — **three inline copies of one rule, one of them wrong, nothing pointing between them.** The wrong one survived four days after the right one was written. ✅ The rule is now `till/receiptPrint.ts`, called by all three, with **7 vitest cases** including *never throws* and *never kicks the drawer*. ⚠ MAUI 🟡: it routes through `TillAgentPrinting` on both checkout and reprint, and has **no equivalent afterthought-print screen** — a declined receipt shows nothing to print from — so it never had this bug; 🟡 because that has not been hand-run. ⚠ **The membership card is the documented EXCEPTION** and must keep using the browser (CR80 card to a page printer, §5d WP-L1). **Hand-run §G72, and §G72e is the regression check.** |
| **Searching by BRAND** | ✅ | ✅ | `GET /api/v1/catalogue/changes` now carries `Brand` | ✅ **Closed 2026-08-10 (till 1.36.0 + backend 1.9.0).** ⚠ **A silent parity gap, not a missing nicety.** `SharedKernel.ItemSearch` matches name, barcode **and** brand — that is the shared rule (C1) — but the till's catalogue row had no brand column, so `TillStore.SearchAsync` passed `null` and matched TWO fields where the server and the web till matched three. Searching "Marvel" found nothing on a MAUI till and everything on the web one: same query, same shop, two answers, and nothing anywhere to say which was right. ⚠ Brand had to be added to the SQL PREFILTER as well as the in-memory matcher — the prefilter must stay a superset, and adding it to only one half filters out rows that were never fetched. ⚠ `"-"` (the legacy "no brand" placeholder both tills write) is normalised to null at the server's projection, once, rather than in each client. Pinned by `CatalogueUpsertTests` (6), mutation-checked three ways. |
| **Item BRAND, DESCRIPTION and COST on a till** | ✅ | ✅ | `GET /api/v1/catalogue/changes` (local schema **v5**) | ✅ **2026-08-10 (till 1.36.0 + backend 1.9.0).** All three were already on the `Item` entity — one `.Select` line each, no join — and simply never selected, so the item list could not show them and the editor could not write cost back without zeroing it. ⚠ **`CatalogueItemDto` EXISTS TWICE**, hand-copied into `CatalogueChangesController`; both changed. ⚠ **Cost travels as PENCE** though the column is decimal — money is integer pence everywhere and the conversion happens once, at the projection. ⚠ **THE CURSOR TRAP**: the feed is keyset pagination over (ModifiedAt, IdOne), so a new FIELD reaches an existing till only for items that change afterwards — the rest keep nulls indefinitely and the symptom is a search that works for three items and not twenty thousand, with nothing in any log. Schema v5 clears the cursor to force one backfill, and because that only helps if the SERVER already sends the fields, the Plutus tab gained **"Re-download the whole catalogue"** so deploy order stops mattering. ⚠ `TillStore` has **two** hand-written upsert branches and a field copied into one and not the other reaches only brand-new items — the `StockUntracked` bug; `CatalogueUpsertTests` includes a reflection guard that fails when a field is missing from either. |
| **Add an unknown scanned barcode as a new item** | ✅ | ✅ | `POST /api/Item` + `POST /api/Stock` — the same legacy routes the web till uses (binding default 10) | ✅ **2026-08-10 (till 1.36.0).** An unknown barcode used to end at *"we can't find an item with that ID"*, so selling something newly delivered meant leaving the counter — which is how shops end up ringing new stock through as a miscellaneous line, lost to every stock figure and category report it belongs in. Now the till OFFERS to add it. ⚠ **The barcode travels with the offer**: reading it off the packaging and retyping it is where a digit gets dropped, and the result is an item nothing will ever scan to. ⚠ **An OFFER, not an automatic jump** — a mistyped search is far commoner than a new product. ⚠ **The clash is NAMED, not just refused**: the barcode is half the composite primary key, so a duplicate is a database error; the web till showed a raw "API 500" before it learned to check, and the useful answer is *which* item already has it. ⚠ **An empty band list REFUSES the whole operation** — unlike an edit, a new item has no existing pair to fall back on, and defaulting to 20% would be a VAT decision made by a default. ⚠ Creation lives in ONE place (`ViewAllViewModel.ExecuteCreateItem`), reached by both the till and the inventory screen; a second copy on the till would be exactly the drift C2 exists to prevent. |
| ⚠ **Roster + permissions re-read ON THE HEARTBEAT** | 🟡 | ✅ | `GET /api/v1/tills/{tillId}/operators`, every 60s tick | ✅ **2026-08-11 (till 1.41.0).** Matt: *"MAUI getting updates from the portal should happen all the time. E.g. if an account is disabled, or new permissions added, should refresh immediately."* ⚠ **NOTHING REFRESHED THE ROSTER ON ANY CADENCE.** It was fetched by a manual button on the Plutus tab, and by a login path that **only fires when the roster is EMPTY** — so a normal daily sign-in never touched the network for staff data, and a till left signed in through a shift never re-read permissions at all. A role change made in the portal reached it whenever somebody happened to press a diagnostics button. ⚠ Now on every beat, so a permission change lands **within 60 seconds**. ⚠ **Web till 🟡** — it fetches its roster differently, but it also **throws the entire heartbeat response away** (`api.ts` posts and discards), so `Locked`, `SyncNow` and `CatalogueCursor` are already dead there. That is its own row and its own bug. **WP17.4** — same work package as the sign-out row below, because they are one change: a web till that re-read its roster on a cadence would get both. |
| ⚠ **A disabled operator is signed OUT, not just refused** | 🟡 | ✅ | the roster's absence | ✅ **2026-08-11 (till 1.41.0).** Matt: *"If a user is disabled, the user needs immediately logging out with an information message saying 'Your account has been disabled, please speak to your manager'."* A deactivated employee **drops out of the roster** server-side (`TillOperatorsController` filters on `e.Active`, and again on holding at least one `pos.*` permission), so one absence covers both "disabled" and "no longer allowed at a counter". ⚠⚠ **A ROSTER THAT COULD NOT BE FETCHED IS NOT AN EMPTY ROSTER** — `OperatorRevocation.Check` treats a null fetch as "carry on", because the alternative signs the **whole shop out mid-sale every time the broadband hiccups**, accusing the operator of being disabled. That would be a worse outage than the one this prevents, and it would hit the flakiest sites first. ⚠ An **empty** roster the server *did* send DOES revoke — both halves are pinned, and mutating either fails a named test. ⚠ The message names the ACCOUNT, not the till: *"you have been logged out"* sends somebody to reboot the machine. ⚠ The basket is abandoned — a revoked operator continuing to ring a sale is an unattributable sale; committed sales are untouched and keep draining. ⚠ The platform token is cleared too: it is a bearer token with no server-side denylist. Pinned by `OperatorRevocationTests` (7), mutation-checked three ways. ⚠⚠ **Web till ⬜ — WP17.4, ADDED 2026-08-11, and its cell was EMPTY until then.** That is my own D3 violation on the day I shipped this: *"A ⬜ needs a reason, not just a number… An empty Notes cell is how the July drift happened."* I left one on a row about revoking access. ⚠ **The gap is not cosmetic.** The web till signs out only REACTIVELY — `api.ts:50`, `if (res.status === 401) signOut()` — and login tokens are cached **12 hours** carrying their permission set (runbook pitfall 10). So where MAUI drops a disabled operator inside 60 seconds with Matt's wording, the browser keeps a working session until something happens to 401. ⚠ **How long that actually is, I have NOT traced** — `perm:*` policies resolve live from RBAC by userId rather than from token scopes (pitfall 5), so a refusal may well arrive on the next permission-gated call; what does not exist either way is a PROACTIVE check. **WP17.4 is the roster re-read on the web till's own cadence**, and it shares its answer with the row above. ✅ **WEB TILL CLOSED 2026-08-17 (web 1.11.0), §5b W-P2** — `src/roster.ts` caches the **whole `TillOperatorsResult` envelope** in IndexedDB and re-reads it on the 60 s cadence; a signed-in operator missing from a roster **the server actually sent** is signed out with Matt's sentence, verbatim. ⚠⚠ **The null≠empty rule is mirrored exactly and is the reason this is a C2 twin**: `refreshRoster` returns null on any failure and `isStillPermitted` treats null as *carry on*, or every broadband hiccup would sign the whole shop out mid-sale accusing the operator of being disabled — worst-connected sites first. An **empty** roster the server did send revokes, correctly. Pinned by `roster.test.ts` (11), mutation-checked. ⚠ **The envelope, not rows** — `asOfUtc` is roster-level and is the SERVER's clock, which W-P4's offline horizons measure from. ⚠ It is also **the spine**: W-P3's ceiling and W-P4's offline sign-in read this same cache. ⚠ The 12 h token cache is unchanged — this makes the check PROACTIVE, which is what was missing. |
| **Reprint a receipt for a past sale** | 🟡 | ✅ | — (reads this till's own record, so it works with the line down) | ✅ **Cutover step 26, 2026-08-10 (till 1.34.0).** Statistics → **Reprint a receipt** → pick from this till's last 20 sales. A customer who has lost their receipt could not be refunded: the barcode on the paper is what the refund flow keys on. ⚠ **The copy is MARKED "REPRINT — not a new sale", above the first rule.** That is a money rule, not a courtesy — the refund flow accepts a sale found by a receipt barcode, so two indistinguishable papers for one purchase is the shape of a double refund, and this till has already let £13.99 out twice from a different cause. ⚠ **The barcode is the SAME** as the original's: marking distinguishes the paper, never the sale, or the copy could not find the sale at all, which is the whole point of asking for one. ⚠ **The drawer does NOT open** — no money is moving, and a drawer that opens when nothing is happening teaches operators to ignore it. ⚠ **Refunds ARE reprintable** (`purchasesOnly: false`), unlike the refund picker: refunding a refund is invalid, reprinting one is not. ⚠ Every figure is the STORED payload's; re-deriving from today's catalogue would hand back a different total after a price change. Item NAMES are looked up locally because the wire carries only ids — a withdrawn item falls back to its barcode rather than a blank. Pinned by `ReceiptReprintMarkingTests` (5), mutation-checked on the marker position and the barcode. ⚠⚠ **WEB TILL CLOSED 2026-08-17 (web 1.11.0), §5b W-P6** — and it was **half there already**, which is why the ⬜ was wrong rather than merely stale: Reporting → a sale → **Print copy receipt** has existed for months and marks the copy (`${id} (COPY)` on the sale id), but printed only via `window.print()`. So the capability read as absent while the *hard* part — finding the sale, marking the paper — was done, and the missing part was the printer. It now goes through `agentAvailable()` + `printDocument(receiptToDocument(…, columns, false))`, with the browser dialog as the fallback: **a reprint must never be held up by hardware**. ⚠ `openDrawer: false`, the third argument — same rule, stated the same way, for the same reason. ⚠ **The web till reprints CROSS-TILL sales**, which MAUI cannot: its list is `GET /api/v1/sales` (server-side, whole business), not this device's own record — so a sale rung up on the MAUI till reprints from the browser. The trade is the mirror of MAUI's: MAUI's works with the line down, this one needs the server. Original note: **Web till ⬜ — WP11**, where it joins the reporting screen that already lists sales. |
| **Item edit — the FULL field set** | ✅ | ✅ | `GET`/`PUT /api/Item/{id1}`, `/api/Tax/Index`, `/api/Category/Index` | ⚠ **Reworked 2026-08-10 (till 1.34.0) after a second report** — Matt: *"Maui edit items is missing category and tax e.g. 20%."* Both readings were true. The sheets came AFTER the form, so opening "Edit item" showed name/brand/price and no tax or category anywhere; and **an empty list SKIPPED the sheet in silence**, so a failed `/api/Tax/Index` call made the capability vanish without a word — indistinguishable from one never built. Now: the three choices come FIRST, the form then shows the chosen **tax band, category and stock setting as read-only rows** beside the price, and an empty list SAYS SO and keeps the item's existing value. ⚠ **The band shows its PERCENTAGE** (`Standard — 20%`) via `Client.Core/TaxBandLabel`, because a band called "Standard" or "T1" cannot be checked against a price — and checking is the only reason to show it. See C1. ✅ **Extended 2026-08-10 (till 1.33.0).** Matt: *"I can now edit, but it seems to be missing a lot of options compared to the webtill."* Correct — the web till offers barcode, name, brand, description, cost, price, tax band, category and stock tracking; MAUI offered **name and price**. It now offers all of them: text fields in the input alert, and action sheets for the three a text prompt cannot ask for (tax band, category, stock tracking). ⚠ **The barcode stays read-only, as it is on the web till** — it is half the composite primary key, so changing it is a DELETE plus an INSERT that orphans every stock movement, sale line and webstore listing pointing at the old one. ⚠ **The ex price is DERIVED from the chosen band and `rate` is a MULTIPLIER** (1.2 = 20%), so it divides; multiplying makes a £10 item's ex price £12 and the server's `|price − exPrice × rate| ≤ 2p` guard rejects the write with a message that never mentions units. ⚠ **Zero-rated and Exempt both have rate 1.0 and are different bands** — the sheet shows NAMES and writes the id, never a rate comparison. ⚠ Still read-modify-write through `UpdateItemFieldsAsync`: the PUT binds the whole entity. |
| **Parked baskets** (park / recall) | ✅ | ✅ | — (local only, binding default 15 — the web till parks locally too) | **Cutover step 18, 2026-08-09.** Moved onto the v2 `SavedBasket` table, which had existed, been mapped and been indexed since the store was built while **nothing ever touched it** — parking wrote to the LEGACY database, which on a portal-provisioned till is an empty file the app creates on first use. ⚠ Serialised as **contract JSON with a plain `kind` discriminator**, never Newtonsoft `TypeNameHandling.Auto`: that wrote .NET type names (`NatApp.Plutus.Models.BasketItem, NatApp.Plutus`) into the blob, which stop resolving the moment a namespace, assembly or class is renamed — and this codebase has renamed all three, which is why a discounted parked basket crashed the app on recall. ⚠ **Read before delete** on recall: the old code deleted the row and *then* parsed its copy, so an unreadable blob destroyed the basket as well as losing it. ⚠ **The parked price is kept, not re-resolved** — a basket is a quote made to a customer who walked away. ⚠ Loading moved OFF the UI thread (the constructor read SQLite synchronously inside `BeginInvokeOnMainThread`). Pinned by `ParkedBasketTests` (15), mutation-checked on the return discriminator. |
| **Operator platform token** (for `perm:*` routes) | ✅ | ✅ | `POST /api/Auth/Login` — the SAME endpoint the web till uses (binding default 11) | **Cutover step 19, 2026-08-09.** The roster still AUTHENTICATES (offline, against synced PBKDF2 hashes); this is a different need — the till's `perm:*` routes are gated on what the OPERATOR may see, and a device token identifies the machine, not the person. Fetched in the background after a successful roster sign-in; failure is silent, because a till with no network must still open. ⚠ **Held in memory only**: it is a bearer token with no server-side denylist, so a copy on disk would outlive every revocation the platform can perform. ⚠ Expiry is checked on READ, not by a timer — a token that lapsed overnight must not be handed out because nothing woke up. |
| **A till can request its own removal** | ✅ | ✅ | `POST /api/v1/tills/unenrol-request` | ⚠ **The caller it was written for could not call it.** Its doc comment has always said *"a till asks to be un-enrolled"*, and it was gated on `portal.tills.enrol` — a scope a DEVICE token cannot carry — so only a portal admin could ever raise the request and the till's own button had nothing to talk to. Now `TillsUnenrolRequest` (portal admin OR device). ⚠ A device may only un-enrol **itself**, enforced against the token's `did`: otherwise one enrolled till could start the removal of every till in the estate, and approval is a portal queue where plausible-looking requests get waved through. Mutation-checked. |
| **A till can read its own takings** | ✅ | ✅ | `GET /api/v1/sales`, `GET /api/v1/cash-events` | Both gained `pos.reports.view` alongside their portal permissions (step 19). ⚠ `cash-events` **is** the X/Z drill an operator runs at the end of their own shift, and it was reachable only with `portal.financials.view`, which no till operator holds — the till could write cash events and never read them back. |
| **Store Information** (name, address, phone, VAT number) | ✅ | ✅ | `GET /api/v1/stores/{id}/info` — READ-ONLY; the portal is the source of truth (WP6.1) | **Cutover step 20, 2026-08-09.** MAUI's five local-write commands are DELETED. They edited the shop's name, address, logo, phone and VAT number into the LEGACY local database — so the company's own VAT number could differ on every till in the estate, and the one printed on a receipt was whichever machine printed it. Nobody finds that until an inspection. They were also unusable on a portal till three times over: gated on `IsAuthorised` (a legacy `AuthActions` table that does not exist there), through `EmployeeId` (null for every roster operator), falling back to `RequestAuthorisedUserInput` (which never terminates). ⚠ **Last-good when offline, "unavailable" when never fetched** — never the legacy local record, which is the thing this step exists to stop being authoritative. ⚠ A null answer does NOT clear the cache: a blip would otherwise leave the next receipt with no shop on it. ⚠ **The logo is dropped** (binding default 18) — no logo field on the contract, so a local one was only ever this machine's opinion. ⚠⚠ **MAUI's SCREEN WAS REBUILT 2026-08-17 (till 1.74.0), and the ✅ above had been hiding how bad it looked.** Matt: *"MAUI looks nothing like the webtill"* — and the capability register cannot see that, because a row that asks *"can the till show its store details"* is answered by data arriving, not by anybody being able to read it. What was on screen: **every field label was `Colors.LightGray` on a near-white surface**, so the information was all present and none of it legible; a `LightGray` top bar bound to the **legacy `StoreModel`**, which step 20 made non-authoritative and which is EMPTY on a portal till, so it rendered as a grey strip above the same four facts fetched properly; a "Region" panel printing `FirstDayOfWeek` and a currency **format string** (`£###,###.##/-£###,###.##`) that the web till does not show and no shopkeeper needs; a bare button labelled **"Bag"** under an invisible heading, with nothing saying it asks for a barcode; and a two-column Grid reflowed from `OnSizeAllocated`. It is now the web till's **three cards** — Business / Store / Opening hours — same order, same field labels, same words, wrapping via `FlexLayout`, with **Store id and Till id** below (the web till showed both; MAUI showed neither, and the till id is the first thing anybody is asked for on a support call). ⚠ **Every colour is a DYNAMIC resource** so a portal theme still repaints it; the old `TextColor = Colors.X` assignments captured a colour once and could not follow a theme change. |
| App-update prompt | ✅ | ✅ | — | ✅ **MAUI closed — and this row said ⬜ until 2026-08-16, when the work was already in.** `TillCadence` sets `UpdateAvailable` from the heartbeat every tick, `ConnectionViewModel.HasUpdate` exposes it, and `ConnectionView.xaml:96` shows it on the Plutus tab. ⚠ **It tells you to fetch the build rather than offering a button**, because there is no self-update for MAUI (Matt's decision) — a button that cannot work is worse than a sentence that explains. ⚠ It clears itself the moment the till is updated, being recomputed from the beat rather than latched. Original note: Web polls a build stamp; MAUI needs the `426 Upgrade Required` path in WP5. |
| ⚠ **Remote lock of a lost or stolen till** | ⬜ | ⬜ | ⬜ **`Device.Locked` exists but is inert** | ⚠ **Reads as built and is not — traced 2026-08-09.** The column ships (WP5's `AddDeviceSyncSignals`), `HeartbeatResult` carries `Locked`/`LockReason`, and `SyncClient` surfaces `HeartbeatOutcome.Locked`. But **nothing sets the flag** (no endpoint, no portal control — `Locked =` appears nowhere outside the migration) and **nothing enforces it**: `IssueDeviceTokenAsync` refuses a device only when `Status == Revoked`, so even once set the lock is *advice to the till* and older or modified software ignores it. ⚠ **`Revoked` is the only thing that actually stops a device today** — reach for that in a real incident. Enforcement must land **before** any control that sets it, or the switch stays fake. Retrofit plan §7 risk 4. |
| **Portal-controlled theming** (colour schemes pushed to stores / tills / groups) | ✅ | 🟡 | `GET /api/v1/themes/effective` + `/api/v1/themes` CRUD | Shipped 2026-08-07. Built-in light/dark + custom schemes, assigned per tenant/store/group/till from Locations. ✅ **MAUI CLOSED 2026-08-17 (till 1.73.0), step 22 / WP7.** ⚠⚠ Matt: *"Changing the theme in the portal needs to be consistent across the web and MAUI till."* So the parse-and-validate rule lives in `Client.Core/ThemeSlots.cs` — a **C2 twin** of the web till's `theme.ts`, 41 tests, two mutants killed — and **neither till decides anything about colour**: the server resolves till > group > store > tenant > default. ⚠ **The same seven slots, by name** (`accent`, `accentInk`, `surface`, `surface2`, `ink`, `inkMuted`, `line`) as XAML keys `Theme*`, rather than remapped onto the legacy palette keys — remapping would have worked once and then diverged the first time either palette was touched. ⚠ **Six-digit hex ONLY** — the web till's *"last line of defence against a bad value in the blob"*, and `colorsJson` is opaque with no server-side validation. `#abc` and 8-digit alpha are both refused; an accepted alpha channel could render a till's text at zero opacity. ⚠⚠ **A bad slot is ABSENT, never substituted** — the stock palette key is left alone, which is what makes clearing an override restore the built-in palette *exactly*, and what stops a malformed blob producing white-on-white. ⚠⚠ **A malformed blob still applies the BASE MODE** — `theme.ts` says so outright, and a shop set to dark must not be thrown back to light by one bad hex. ⚠ **7a landed too**: ClientUI's palette is ported **verbatim** under the same `x:Key` names, in place before [L10] drops that project — AppClient had **no theme resources at all** before this. ⚠ Cached in `MetaKeys.EffectiveTheme` and applied **before the first screen**, so an assigned scheme survives an offline restart without the till flashing the stock palette; a failed fetch keeps the last-known theme. ⚠⚠ **RECEIPTS ARE IMMUNE** (C1) — verified, no print path reads a `Theme*` key; printing from a dark scheme once put near-white ink on paper. ⚠ 🟡 until §G31. Original note: MAUI: consume the same effective endpoint and map the slots onto XAML resources — **WP7b**, added 2026-08-08. ⚠ WP7 previously said "port the palette" only and would have closed with a hardcoded scheme. |
| Operator RBAC + offline login | ✅ | ✅ | `GET /api/v1/tills/{id}/operators` (**built 2026-08-08**) | **WP8.** A till syncs its roster — credential hashes + **raw** permission windows — and verifies offline with `SharedKernel.Pbkdf2`, byte-identical to the server. ⚠ **Windows ship RAW, never pre-evaluated**: resolving "allowed right now" at sync time would leave a Saturday-only supervisor synced on a Wednesday with **no permissions until the next sync**, silently. The staleness tier applies at the gate, so past 7 days the same person keeps selling and loses refunds. |
| ⚠ **Enforcing those permissions at the action** (sell, refund ceiling, price override) | ✅ | ✅ | — | **Cutover step 12, 2026-08-09** — `AppClient/Services/Security/TillGate.cs`, 16 tests. Delegates every decision to `SignedInOperator.Can`, so the ceiling, the window and the staleness tier are the shared rule. ⚠ It replaced a gate that **could not work**: the legacy `Authorisation.IsAuthorised` looked up action names in a local `AuthActions` table a portal-provisioned till does not have, and `RequestAuthorisedUserInput` **never assigned the id it returned** — so a supervisor typing correct credentials was re-prompted for ever and only Cancel escaped, while the caller re-tested the ORIGINAL operator anyway. ⚠ **A null operator BLOCKS** (`SignedInOperator` is null after a legacy local login) and is NOT offered an override — another person's password does not answer "nobody is signed in". ⚠ The refund ceiling now counts **quantity**: the legacy `Sum(bRI => bRI.Price)` summed UNIT prices, so five £30 returns tested as £30 and sailed through a £100 band. Both pinned by mutation. |

---

# Part C — Where the rules live

Part B answers *can this till do X*. Part C answers **would every till get X right** — which is a
different question, and the one that produces silent money bugs.

## C1. The rules register

For each cross-cutting rule: where the one implementation lives, who else implements it and why,
and what stops them drifting.

### Money and VAT

| Rule | The implementation | Second implementation | Pinned by |
|---|---|---|---|
| **Money is integer pence** | Everywhere; `SharedKernel/Money.cs` | — | `No_module_declares_decimal_or_double_money_members` |
| **A line's VAT figures** (rate from the price pair, VAT = gross − ex, discount scaled by ex/inc, returns negated with the discount dropped) | `SharedKernel/VatLineMath.cs` | Web till `api.ts` checkout — **deliberate**, it's TypeScript | `VatLineMathTests` (.NET side only — see C2) |
| **Which VAT rate applies** | The portal. `GET /api/v1/vat/bands` publishes the whole effective-dated timeline | None — **no till may hold a rate** | `Till_libraries_stay_platform_neutral_and_hold_no_VAT_rates_of_their_own` |
| **Which VAT band a line was sold under** | `LineMeta.vatBand` on the wire; backfilled server-side by `Plutus.Entities/VatBandStamp.cs` when a channel sends none | Client may state it (and then wins) | `VatBandsE2eTests` |
| **How much of a sale is still refundable** | `SharedKernel/RefundRules.cs` (2026-08-09) | None yet — ⚠ the server does **not** enforce the remainder when a refund is posted, so today this is a *client* gate only | `RefundRulesTests` (23, mutation-checked) |
| **The members' auto-discount — whether it applies, and to which lines** | `SharedKernel/MemberDiscount.cs` (2026-08-13). Expired membership grants nothing; a line is eligible only if it is **not a return, not already discounted (NO STACKING), and not a gift card**; `Label` fixes the receipt wording | ⚠ Web till `basket.ts` `applyMemberDiscount` + `TillPage.tsx:122` — **deliberate**, it's TypeScript, and it is the ORIGINAL. Default 10: if they disagree, the web till wins | `MemberDiscountTests` (18, all four exclusions mutation-checked) |
| ⚠ **Members' discount ARITHMETIC** | Already shared — `LineDiscounts.Percentage` (whole-line rounding, capped at line value, refuses a fraction > 1) then `VatLineMath.ForLine` | Web till `basket.ts lineDiscountPence` | `LineDiscountTests`, `VatLineMathTests` |
| ⚠⚠ **Whether a SCHEDULED discount is live, and which lines it lands on** | `SharedKernel/ScheduledDiscounts.cs` (2026-08-20) — `IsLiveAt` mirrors `PermissionGrant.IsActiveAt` **expression by expression** (day mask bit 0 = Sunday; window LOCAL and inclusive at BOTH ends; validity UTC; **midnight wrap matches nothing, copied on purpose**), `Targets` unions all/category/barcode, `IsWellFormed` **fails towards not discounting** — and its empty-target case means NOTHING, never everything | Web till `till/scheduledDiscounts.ts` — **deliberate**, it's TypeScript. See C2 | `ScheduledDiscountsTests` (29) + `scheduledDiscounts.test.ts` (29), the SAME vectors, **both sides RUN**. Three mutants killed: empty-target→everything, day-mask ignored, tie flipped |
| ⚠⚠ **Which automatic discount a line takes when TWO apply** (decision D2) | `SharedKernel/AutoDiscounts.cs` (2026-08-20) — the tier or the rule, **the LARGER, never both**; ties to the MEMBER; an operator's own discount stops both. ⚠ It exists because the alternative is each till deciding for itself, and "10% here, 15% there on the same Wednesday" is a money defect nothing downstream can detect | Web till `till/autoDiscounts.ts` — **deliberate**. See C2 | `AutoDiscountsTests` (16) + `autoDiscounts.test.ts` (16), same vectors, both RUN |
| ⚠ **What a rule is worth, in the units it travels in** | Split at the wire: `PercentFraction` (a RATIO, legitimately decimal) and `FixedAmountPence` (INTEGER PENCE). ⚠⚠ **It was one ambiguous `decimal Amount` until the architecture guard `No_module_declares_decimal_or_double_money_members` refused it — and the guard was right**: a field that is money in one branch and a ratio in the other is a unit error waiting for its first reader. The legacy `Discounts.Amount` column is still decimal pounds; the collapse happens ONCE, in `DiscountRulesController` | Web till reads the same two fields | The architecture suite itself, plus the `takes its pence off every unit` vector on both sides |
| ⚠⚠ **Which lines a members' discount may LAND on, in MAUI's shape** | `AppClient/Services/Storage/MemberDiscountBasket.cs` (2026-08-16) — composes the shared rule into a `BasketAlteration` that **names its eligible items explicitly**. ⚠ Not a second rule: eligibility and arithmetic are still `MemberDiscount`'s. ⚠ **The association is load-bearing** — `CheckoutCommit.TargetsOf` excludes returns but NOT already-discounted or gift-card lines, so a whole-basket alteration puts the member's money exactly where the shared rule forbids | — (the web till needs none: it stores the discount ON the line, so its exclusions are per line for free) | `MemberDiscountBasketTests` (16). ⚠ Mutation-checked twice: removing the association kills 3 including the end-to-end commit test; ignoring the member's own prior alteration on re-compute kills the silent-collapse test |
| **Gift-card CODE format, check character and scan routing** | `SharedKernel/GiftCardCodes.cs` — **moved there 2026-08-16** from `Plutus.Customers`, a server module MAUI cannot reference. Exactly the `MemberNumbers` move, for exactly the reason: the till must route a scan **without a server round trip**. ⚠ Random, not sequential — a gift card is a **bearer instrument**, and a guessable code is a licence to print money. ⚠ `LooksLikeCard` checks the **check character**, so a mis-scanned product barcode starting with G is not treated as a card | ⚠ Web till `TillPage.tsx:35` `/^G[0-9A-Z]{13}$/i` — see C2 | `GiftCardCodeTests` (15, incl. the two-prefix collision with member cards) |
| **What identifies a gift-card ACTIVATION line** | `SharedKernel/GiftCards.cs` — `ItemIdOne = "GIFT-CARD"`, compared case-insensitively. It decides a **zero-VAT treatment** and a discount exclusion, so it is a rule, not a magic string | ⚠ Server `SalesIngestService.cs:579` still holds a **private const of the same value** — see C2 | `GiftCardConstantTests` |
| **What a discount must record before it may happen** (binding default 22(c)) | `SharedKernel/DiscountAudit.cs` (2026-08-14) — the reason is **mandatory** and normalised; a step-up with no authoriser is refused; **self-approval is refused a second time**, on the RECORD rather than the act | ⚠ Web till `basket.ts normaliseReason` — **the normalisation half only**, deliberately, because it is TypeScript. See C2 | `DiscountAuditTests` (19, both refusal guards mutation-checked) + `discountReason.test.ts` (5) |
| **How a discount authority reaches the wire** | `Client.Core/DiscountAuthorityWire.cs` → `LineMeta.discountAuthority[]`. ⚠ Ids written `"D"`, `Guid.Empty` written as ABSENT — a zero id would render as a real user in a report | Web till `api.ts` checkout builds the same object literal | `DiscountAuthorityWireTests` (13, incl. the legacy-bridge FK tripwire) |
| ⚠⚠ **What a CARRIER BAG is, and which VAT band it takes** | `SharedKernel/CarrierBags.cs` (2026-08-19) — the category (`"Carrier bags"`) is the marker, the id is `BAG-<pence>` so **price is the identity**, and `StandardRatePreferred` picks the band. ⚠⚠ It picks the **HIGHEST** rate (preferring an exact 20%), the deliberate **opposite** of `GiftCardSaleItem`'s zero-band pick — a bag is an ordinary retail supply, a gift-card activation is not a supply at all, and reaching for that helper here would silently under-declare VAT on every bag a shop ever sold. ⚠ **No statutory price is hardcoded** — the portal sets it, because England's minimum moved 5p → 10p on 21 May 2021 and the four nations differ. ⚠ Returns **null** rather than guessing when a business has no bands yet, exactly as gift-card provisioning does. | Two client twins — see C2 | `CarrierBagTests` (24) |
| **Legacy tax row → band** | `SharedKernel` `VatBandResolution` + the portal's `VatBandTaxMap` | — | `VatExemptBandTests` |
| **Band snap tolerance** (25bp) | `VatAccounting.BandSnapToleranceBp` — one constant | — | `VatExemptBandTests` |
| **Output tax on a return** (VAT fraction × takings) | `SharedKernel/VatAccounting.cs` — **server only**, a till never computes a return | — | `VatAccountingTests` |
| **Was the rate legal at the time of sale** | `SharedKernel/VatRates.cs` `VatRateHistory.Assess` — server-side at ingest | — | `VatRateChangeE2eTests` |
| **Which HMRC rules the platform applies** | `SharedKernel/VatGuidance.cs` — served to the portal's VAT → Rules tab | — | Rendered from code, so it cannot drift from behaviour |
| **Caching the published bands on a till** | `Client.Core/VatBandCache.cs` — ⚠ stores the WHOLE effective-dated timeline, and prices at the SALE's instant | Web till `api.ts` band cache | `VatBandCacheTests` (10) — including a future-dated change applying on the day to a till that never reconnects |
| **Legacy tax row → band, on a till** | `VatBandCache.BandKeyForTaxIdAsync` — ⚠ **null on a TIE**, never first-wins | ⚠ Web till `api.ts:786` DOES take the first match — see C2 | `A_tax_row_claimed_by_TWO_bands_is_ambiguous_not_first_wins` |
| **What a drawer SHOULD hold** (float + cash takings + ins − outs) | `Plutus.Cash/CashModule.ExpectedDrawerAsync` — **server only** | ⚠ **None, and there must never be one.** Only the platform sees the takings half, including sales another device on the same till posted. A till holds the ANSWER (`LocalCashEvent.ExpectedPence`/`VariancePence`, schema v6) and never the calculation — two numbers for one drawer and nobody can say which is right | `DrawerVarianceE2eTests` (4, mutation-checked twice) + `LocalCashEventTests` (6, mutation-checked four times) |
| **A Z close waits for its own day's sales** | `CashPushService` + `TillStore.PendingSalesForDayAsync` — **till only**, because only the till knows what it still holds | — | `A_days_unsent_sales_are_visible_to_the_drain_that_holds_its_Z`, `A_sale_the_platform_REFUSED_does_not_block_the_Z_for_ever` |

### Identity and sale shape

| Rule | The implementation | Second implementation | Pinned by |
|---|---|---|---|
| **Item id from a barcode** | `SharedKernel/DeterministicGuid.cs` `ForItem(businessId, itemIdOne)` | Web till `pipeline.ts` `itemGuid()` — **deliberate** | Frozen golden vector in `LegacySaleBridgeTests` — see C2 |
| ⚠ **The barcode is the real invariant**, `ItemId` rides along | `SaleLine.ItemIdOne`, carried in `LineMeta.itemIdOne` | — | Retrofit plan §10 |
| ⚠⚠ **What may be an item's ADDITIONAL barcode** | `SharedKernel/ItemBarcodeRules.cs` (2026-08-20) — `Normalise` trims and never case-folds; `WhyRefused` returns ONE SENTENCE per refusal and the portal shows it verbatim. Refuses: over 20 chars (`Item.IdOne`'s own `[MaxLength]`), interior whitespace, and the **reserved shapes that could never scan as an item anyway** — member cards and gift cards (both tills route those BEFORE any item lookup), carrier-bag ids, `GIFT-CARD`, `CARD-SURCHARGE` | None — server-enforced, and the clients deliberately do NOT restate it: they show `WhyRefused`'s sentence verbatim. ⚠ That is the point — a copy of a reserved *shape* in a client would go stale and refuse a code the server accepts | `ItemBarcodeRuleTests` (25, sentences asserted verbatim; the member-shape mutant was watched dying) + `ItemBarcodesE2eTests` |
| ⚠ **What an operator is TOLD while typing a barcode** — the live check | Web till `src/barcodeProblem.ts` (2026-08-20) — uniqueness against the whole-tenant alias list, and a leading/trailing-whitespace **warning** that still saves because the server trims | Portal `src/barcodeProblem.ts` — **byte-for-byte identical**, because the two apps cannot share an npm package | `barcodeProblem.test.ts` (11 vectors) for the rule, **`FrontendTwinTests`** for the twin — see C2 |
| ⚠⚠ **Which item a scanned code belongs to, when an item has several** | Three doors, each returning the CANONICAL item: `ItemController.FindById` (server, the till's exact-scan endpoint) · `TillStore.FindByBarcodeAsync` (MAUI + shared storage) · `offline.ts cachedItemByAlias` (web till, offline). **`Item.IdOne` is always what comes back**, never the scanned alias | Three by necessity — server, .NET local store, browser store. See C2 | `ItemBarcodesE2eTests` · `TillStoreTests` (6 alias cases + the replacement mutant) · web: hand-run §G66c (no IndexedDB harness exists) |
| ⚠ **Which code was actually scanned** | `LineMeta.barcodeScanned` — written by `SaleAssembler` (both .NET tills) and `api.ts` checkout (web), **omitted when it equals `itemIdOne`**. A snapshot for debugging a supplier's barcode migration; every reader still keys on `itemIdOne` | Web till `api.ts` object literal — deliberate, it is TypeScript | `SaleAssemblerTests` (3, incl. "an ordinary line's json gains no barcode key") + `scannedBarcode.test.ts` (5, the real reducer) |
| **Entity ids are UUIDv7** | `SharedKernel/Uuid7.cs` | Web till `pipeline.ts` `uuidv7()` — **deliberate** | `No_module_mints_entity_ids_with_Guid_NewGuid` (.NET side) |
| **The four sale invariants** | `SaleV2.Validate()` — enforced at ingest, so a client cannot diverge undetected | — | `SalesV2Tests` |
| **Ingest status policy** — 201 recorded · 200 duplicate · 202 quarantined (never retry) · 400 skip | `Plutus.Client.Core` `OutboxPusher` | Web till `pipeline.ts` | `TillOutboxSoakE2eTests`, `ClientCoreE2eTests` |
| **A member number's shape and check character**, and **whether a scan is a member card at all** | `SharedKernel/MemberNumbers.cs` (moved out of `Plutus.Customers` 2026-08-13 — a till must answer it **offline at the scanner**, and MAUI may not reference a backend module) | None — ⚠ and there must never be one. A card that validates on one till and is refused on the next is indistinguishable from a damaged card | `MemberNumberTests` (38) — round-trip through the printed barcode, and the ceiling pinned |
| ⚠ **A member number is ALLOCATED server-side only** | `Plutus.Customers/MemberNoAllocator` — needs a tenant-wide counter row, whose value is its own concurrency token | **None possible.** Two offline tills would mint the same number, which is why adding a member is online-only on **every** till (binding defaults 20/21) | `MemberNumberTests` allocation + backfill sections |
| **Reaching the customer endpoints from a till** | `Client.Core/PlutusApiClient` customer methods (2026-08-13 — there were **none**, which is why MAUI has no attach). Search/detail/tiers are open to any authenticated principal so a **device token** works; create and set-tier are `perm:*` and need an **operator** token, because `perm:*` resolves from RBAC by userId and a device token has none | ⚠ Web till `api.ts:309–418` — **deliberate**, it's TypeScript, and it is the reference | `CustomerClientTests` (17) — URL and payload shapes, plus the refusal wording |
| ⚠ **A till never types a tier's name or rate** | `SetMembershipAsync` posts **`tierId` only**. The tier owns the discount and the renewal length, so re-rating "Gold" in the portal moves every Gold member at once instead of leaving a snapshot on whichever till assigned it | — | `SetMembership_posts_only_a_tier_id` (mutation-checked: adding a rate to the payload fails) |
| ⚠ **The member-number ceiling is `SequenceDigits`** (1,000,000/tenant) | Past it a number **formats but cannot be parsed back** — the card prints and resolves to nobody. Widening the one constant is the fix; both halves read it and widen in step | ⚠ **Never loosen the parser instead** — arbitrary lengths make a bare **EAN-8** canonicalise as a member number ~3% of the time | `Past_the_sequence_ceiling_a_number_formats_but_cannot_be_read_back` |

### Connection and offline trust

| Rule | The implementation | Second implementation | Pinned by |
|---|---|---|---|
| **What "connected" means** — no network / no server / revoked / not enrolled / online, and the two-step order that tells them apart | `Plutus.Client.Core/Connectivity.cs` `ConnectivityProbe` | ⚠ Web till still on `navigator.onLine` — **WP16a moves it here** | `ConnectivityProbeTests` (14), `ConnectivityE2eTests` (5) |
| **Reachability is asked without a database** | `GET /api/v1/ping` — anonymous, no DB, so it answers during a MySQL blip and for an un-enrolled or revoked till | — | `ConnectivityE2eTests` |
| **Revocation reaches a till by POLLING, never by token expiry** | `GET /api/v1/tills/devices/{id}/status`. ⚠ Tokens are bearer tokens with **no server-side denylist** — a revoked till otherwise trades for up to 12h | — | `ConnectivityE2eTests` |
| ⚠ **Never probe by minting a device token** | `POST /api/v1/tokens/device` is rate-limited 5/min/IP; polling it makes a healthy till report itself revoked, and tills sharing one public IP do it to each other | — | `ConnectivityProbeTests` |
| **How long a cached login is trusted** — tiered: money-out 7d, selling 30d, warn from 3d, idle 15min, session ≤ min(12h, business-day rollover) | `SharedKernel/OfflineCredentials.cs` | — | `OfflineCredentialsTests` (23) |
| **Whether a permission is live right now** (validity dates, day mask, time window) | `SharedKernel/PermissionGrant.IsActiveAt` | The server's `EffectivePermissionsService.InWindow` now **delegates** to it — WP8 gave the same question to every till, and two implementations would be a permission meaning one thing centrally and another on a counter | `OperatorLoginTests`, `TillOperatorsE2eTests` |
| ⚠ **Permission windows travel RAW to a till, never pre-evaluated** | `GET /api/v1/tills/{id}/operators` ships the dates/mask/window; the till judges them against its own clock at the moment of the action | — | `A_Saturday_only_grant_is_refused_on_a_Sunday_and_allowed_on_the_Saturday` — resolving at sync time would strand a Saturday-only supervisor with no permissions all week |
| **Offline password verification** | `SharedKernel/Crypto.cs Pbkdf2` on both sides — the till verifies the same hash the server stores | — | `The_payload_carries_a_VERIFIABLE_hash_and_the_RAW_window` asserts the hash actually verifies with the till's KDF |
| **Splitting one discount across the lines it applies to** | `SharedKernel/DiscountApportionment.cs` — proportional, largest-remainder, ties to the earlier line | — (web till reshapes in WP15) | `DiscountApportionmentTests` (13) |
| **How much of a sale has ALREADY been given back** | `TillStore.AlreadyRefundedPenceAsync` over the `LocalRefunds` table — written in the SAME transaction as the refund, from that refund's own payload | The SERVER's `SaleAdjustments` rows, written by `SalesIngestService` (step 17) and summed by `SaleDto.AlreadyRefundedPence` — the till knows only what IT has seen, so the two are halves of one cap, not copies | `TillStoreReadPathTests` (13), mutation-checked twice: overwrite-instead-of-accumulate, and keeping the sign. ⚠ A **table**, not a column: one basket can refund lines from two different original sales, and a single origin column would drop one — leaving it refundable again in full. ⚠ Counts sales still QUEUED (the cash left the drawer when it was handed over) and SURVIVES pruning (deleting it would reset the running total to zero). |
| ⚠ **A REFUND CANNOT ITSELF BE REFUNDED** | The till never offers one: `TillStore.ListRecentSalesAsync(purchasesOnly: true)` excludes negative-gross sales | The SERVER refuses independently: `ValidateRefundCapAsync` quarantines any refund whose origin has `GrossPence < 0` | `Refunds_are_NOT_offered_as_sales_to_refund_against` + `A_refund_cannot_itself_be_refunded` (E2E), both mutation-checked. ⚠ **This cost £13.99 twice on a £13.99 sale, 2026-08-10.** A refund is stored as its own sale with a NEGATIVE gross, so it appeared in a recent-sales picker — newest first, exactly where an operator taps — and the second refund was recorded against the FIRST REFUND's id. The cap then compared it against nothing. ⚠ The server's half took `Math.Abs(origin.GrossPence)`, so a −£13.99 refund read as a £13.99 sale with nothing returned: **the cap was working perfectly on a premise nobody had checked.** |
| ⚠ **A refund still IN THE OUTBOX counts against the next one** | `TillStore.UndeliveredRefundedPenceAsync` sums refunds whose sale is not yet `Pushed`; `ReturnLookup` ADDS it to the server's figure | The two do not overlap — the server counts DELIVERED refunds from any till, this counts UNDELIVERED ones from this till | `A_refund_still_QUEUED_counts_against_the_next_one`, `A_refund_the_platform_has_TAKEN_is_no_longer_counted_locally`, `A_refund_the_platform_REFUSED_still_counts_against_the_next_one`, mutation-checked. ⚠ **The drain window is a real hole**: the two real double-refunds were **97ms apart** and drained together, so the platform answered "nothing refunded" both times — correctly. ⚠ **Added, never `max()`'d**: taking the larger would miss another till's refund landing while this one holds something queued. ⚠ A **REFUSED** refund still counts — the money left the drawer whatever the platform later decided. |
| ⚠ **The refund cap itself** — never give back more than was paid (binding default 12) | `SharedKernel.RefundRules.Authorise`, run at BOTH gates | **The till** caps per ORIGIN SALE (step 16); **the server** re-runs the same rule per sale AND **per item** (step 17) and quarantines 202 past it | `ReturnLookupTests` + `SaleAssemblerE2eTests` (5 E2E), mutation-checked. ⚠ **Per-item is the half only the server can do**: a till's local history is per origin sale, so refunding a £10 line twice inside a £210 sale passes a sale-level test comfortably — and that is the shape refund fraud actually takes. ⚠ **Recording the refund was missing entirely** until step 17: only the WEBSTORE wrote `SaleAdjustments`, so every "how much has been refunded" question read a table till refunds never populated and always answered "none". ⚠ An origin the platform has NOT seen is **accepted, not quarantined** — 202 is terminal and a refund can legitimately outrun the sale it refunds when another till's outbox drains later. |
| ⚠⚠ **A price ADJUSTED at the till, and the VAT it keeps** | `SharedKernel/PriceAdjust.cs` — `ExFromInc`: the operator types the INC price, the ex half is derived from the CATALOGUE pair's proportion, products-before-division, rounded once away from zero | ⚠ **NOW A TWIN — `till/priceAdjust.ts`, 2026-08-18.** The web till always worked this way (its `adjust` reducer); MAUI asked for **ex AND inc in one dialog and wrote both**, so a mistyped pair BECAME the line's declared VAT rate — £10.00 ex against £10.50 inc declared 5% on a 20% item, with nothing to validate it. See C2 | `PriceAdjustTests` (8) + `priceAdjust.test.ts` (9), the same vectors. ⚠ Derived from the CATALOGUE pair, never the line's current pair, so adjusting twice cannot drift a penny per edit; an impossible catalogue pair (ex ≥ inc, or ex ≤ 0) is clamped rather than propagated into a VAT return |
| **Card-surcharge money** — the fee (percent bp + flat) AND its VAT (follows the basket, one blended ratio, multiply-before-divide, round once away from zero) | `SharedKernel/CardSurchargeVat.cs` — `FeePence` + `PairFor`; the `CARD-SURCHARGE` natural key is the shared constant both the provisioner and the tills use | ⚠ **NOW A TWIN — `src/till/surcharge.ts`, 2026-08-17 (§5b W-P7).** See C2 for what pins the pair | `CardSurchargeVatTests` (17), mutation-checked against a hardcoded 20% — the plausible shortcut that reconciles perfectly and is wrong on every non-standard-rated basket. ⚠ `PairFor` found its own trap: `fee × (ex ÷ gross)` turns the exact midpoint 3 × 10000 ÷ 12000 = 2.5 into 2.4999… — products first, always. |
| ⚠ **A basket whose lines don't add up to its total is REFUSED at the till, not sent** | `CheckoutCommit.CommitAsync` compares `request.GrossPence` against `BasketMoneyPence` — the same sum the till's own `sale.Total` uses — before anything is queued | — | `CheckoutCommitTests`. ⚠ The alternative is not "a slightly wrong sale": the server answers **202 Quarantined** and `OutboxPusher` treats 202 as **terminal**, so the sale looks successful at the counter and is destroyed hours later with the customer long gone. This guard is what makes any future money-carrying basket record fail **loudly and locally** instead. |
| ⚠ **A till receives only its OWN tenant's operators** | `TillOperatorsController` joins through `Employee` (tenant-filtered). `WebCredentials` is a GLOBAL table with no query filter — login is by email before a tenant is known | — | `A_till_NEVER_receives_another_tenants_operators_or_their_hashes` |
| **Catalogue freshness — what a till has and when** | `GET /api/v1/catalogue/changes`, keyset cursor over `(ModifiedAt, IdOne)`; client loop in `Client.Core/SyncClient.cs` | — | `CatalogueSyncE2eTests` (9), `SyncClientTests` (13), `CatalogueCursorTests` |
| ⚠ **A withdrawn item reaches a till as a TOMBSTONE, never as an absence** | `CatalogueItemDto.Removed`, set from `Item.BinnedAtUtc` | — | `A_binned_item_arrives_as_a_TOMBSTONE_not_as_an_absence` — without it a binned item stays sellable on every offline till indefinitely |
| **The change cursor is stamped where it cannot be forgotten** | `Item.ModifiedAt`, stamped for every `IAuditable` in `RepositoryContext.SaveMethods()` — a single choke point every write already passes. ⚠ The plan originally specified a counter each write path bumps; a path added later that forgot would leave every till silently stale | — | `Paging_a_bulk_edit_loses_nothing_even_when_every_row_shares_a_timestamp` |
| **Till presence** — ONLINE <2min · STALE 2–5 · OFFLINE >5 | `Plutus.Tenancy/TillPresence.cs`, in-process. ⚠ NOT MySQL: a fleet beating every 60s would be the busiest write path in the system, storing data that expires in five minutes | — | `TillPresenceTests` (6) |
| ⚠ **A failing heartbeat is SILENT** | `SyncClient.BeatAsync` swallows everything. If a 500 ever read as "locked", one backend hiccup would stop every till in the estate selling | — | `A_failing_heartbeat_is_SILENT_and_never_locks_the_till` |
| **Which TRADING DAY a till is on** | `SharedKernel/BusinessDay.cs` — local wall-clock, not UTC. `CheckoutCommit` and the cash screen both call it | The web till's `pipeline.ts businessDay()` — **C2 twin** | ⚠ Not yet pinned by a test. **Why it is a rule and not an expression:** the drawer reconciles against the day's TAKINGS, so if a cash event and a sale disagree by one day about "today", the Z balances against the wrong takings and the variance is fiction — with nothing in the numbers to show the dates were the problem. ⚠ UTC would file a till trading past midnight under tomorrow and split one shift's banking across two days. ⚠ A rollover hour ("the day starts at 04:00") belongs HERE when it comes, not in each caller |
| **The cash vocabulary** — the five event names | `Contracts.Client/CashContracts.cs` `CashEventTypes`, with `NeedsCount` / `NeedsReason` | The server's `CashEventType` enum in `Plutus.Entities` — **C2 twin by construction**, since a till may not reference a backend project | `LocalCashEventTests.The_wire_vocabulary_is_exactly_the_five_the_server_parses` |
| **The print-op wire contract** — what `POST /print` binds | `tools/Plutus.TillAgent.Core/PrintOps.cs`, referenced (not copied) by `Plutus.Client.Core` | The web till hard-codes the NUMBERS in `receiptDoc.ts` (`OP_TEXT = 0`) — **C2 twin**, since a browser cannot reference a C# project | `TillAgentClientTests.Op_kinds_go_on_the_wire_as_NUMBERS`, and `ConventionTests.The_print_wire_contract_stays_a_pure_contract` — which is what pays for Client.Core being allowed to reference a project outside `src/` |
| **What a receipt SAYS** — the document layout | `Client.Core/ReceiptDocument.cs` `ReceiptDocumentBuilder` | The web till's `till/receiptDoc.ts` — **C2 twin, unavoidable**: the browser cannot run the C# and MAUI cannot run the TypeScript | `ReceiptDocumentTests` (11). ⚠ Two shops printing two different receipts for the same basket is the failure this describes, and it is invisible until someone compares paper |
| **A reprinted receipt is marked as a copy** | `ReceiptDocument.cs` `ReceiptDocInput.IsReprint` → "** REPRINT — not a new sale **", above the first rule | ⚠ **The web till gained a reprint on 2026-08-17 (§5b W-P6) and marks it DIFFERENTLY** — `reporting/SaleDetailDialog.tsx` puts `(COPY)` on the sale id rather than a banner above the rules. Same purpose, weaker signal: the id line is small print, and the rule exists so nobody mistakes the paper for a second sale. ⚠ **Not converged, and deliberately recorded as unconverged** — the honest fix is `receiptDoc.ts` taking an `isReprint` flag and emitting MAUI's banner, which is a WP15 job because it touches the shared document builder. ⚠ The BARCODE is identical on both, which is the half that must not diverge: marking distinguishes the paper, never the sale, or the copy could not find the sale at all | `ReceiptReprintMarkingTests` (5), mutation-checked by moving the marker into the footer and by changing the copy's barcode. ⚠ **The web half has no equivalent test** — the marking there is a template literal in a component, not a rule in a module |
| **What a VAT band says to an operator** | `Client.Core/TaxBandLabel.cs` — `(rate − 1) × 100`, because `Rate` is a MULTIPLIER | The web till shows the band NAME only (`InventoryPage.tsx`), so no twin yet — ⚠ it should adopt this, since a name alone cannot be checked against a price | `TaxBandLabelTests` (7), mutation-checked four ways. ⚠ Zero-rated and Exempt both read 0% and stay distinguishable by NAME — pinned, because merging them on screen is the first step to merging them on a VAT return |
| ⚠ **A failed list is NOT an empty list** | `Client.Core/PlutusApiClient.GetLegacyAsync` returns `(Value, Problem)` — the status is named, never flattened to null | — | `LegacyListFailureTests` (12), mutation-checked four ways. ⚠ **This is the rule Matt's *"missing category and tax"* report was really about**: the client returned an empty list for a 403, a 500, an unreachable server and an unparseable body alike, so the item editor skipped its prompts and a NETWORK fault presented as a fact about the shop. "There are none" is about the tenant; "I could not ask" is about the till, and only one of them is somebody's job to fix. ⚠ A 500 here means the legacy base controller's CONSTRUCTOR `.First()`-ing the `objectidentifier` claim — so it is reported as *"nobody is signed in"*, which is the actionable reading |
| ⚠ **The item editor's ex-price comes from the LEGACY TAX ROW, never the cached VAT band** | `Services/Inventory/ItemPricing.cs` — `ExPriceFor` divides by `TaxBandDto.Rate` from `/api/Tax/Index`; `ViewAllViewModel.ExecuteOpenEditItem` calls it | The till DOES hold a full band cache locally (`VatBandCache`, refreshed every 60s) and it is **deliberately not used here** | ✅ **Pinned 2026-08-16** by `ItemPricingTests` (23, five mutants killed) — the arithmetic moved out of the 1,300-line screen so it could be tested without a device. ⚠ `Rate` is a **MULTIPLIER** (1.2 = 20%), so the ex price DIVIDES by it; the pinned theory covers 20%, 5%, zero-rated and the multiply-instead-of-divide mistake, which the server rejects with a message that never mentions units. The server validates the saved pair against the specific legacy `Tax` row's rate (`ItemController`: `|price − exPrice × rate| ≤ 2p`), while band↔tax-row matching runs on a **25bp tolerance** (`VatAccounting.BandFor`). So a band and its tax row may legitimately differ, and an ex-price derived from the cached band would be rejected with a message about ex-prices that never mentions where the rate came from. ⚠ The consequence is that item editing needs the network — recorded here rather than "fixed" by an offline path that would write wrong money |
| ⚠ **A Z-close ENDS the business day** — nothing may be recorded against it afterwards | `TillStore.RecordCashEventAsync` refuses once `IsDayClosedAsync`; the server 409s independently | Two gates, one rule — the local one is the only one available offline | `Nothing_can_be_recorded_against_a_day_that_has_been_Z_closed`, mutation-checked |
| **Which build this is** | `versions/<component>.txt` → `Directory.Build.targets` → `SharedKernel/PlutusVersion.cs`. ⚠ **`.targets`, not `.props`** — props is imported before the project body, so a per-project `<PlutusVersionFile>` would be ignored and every component would silently report the platform version | Each `vite.config.ts` reads **its own** file into `__APP_VERSION__` | `ComponentVersionTests` — each file exists and parses X.Y.Z, each project points at its own, no surface holds a literal, and the wiring has not moved back to `.props` |
| **Every HTTP endpoint is gated unless deliberately listed** | `[Authorize]` per action or per controller; ⚠ there is **no global fallback policy**, so a missing attribute is an open door | — | `AnonymousEndpointTests` — a reviewed allow-list of anonymous entry points, mutation-checked |
| **Which permissions survive staleness** | `OfflineCredentials.SellFloor` — an **allow-list**, so a permission added later is withdrawn until someone says otherwise | — | `OfflineCredentialsTests` |
| **Supervisor override** — a second operator authorises ONE action | `Client.Core/OperatorLogin.AuthoriseOverrideAsync` | — | `OperatorLoginTests`. ⚠ It may never become an escalation path: **self-authorisation refused**, the supervisor's **own ceiling applies**, and it does **not bypass staleness** — a roster too old to be trusted with refunds is too old to authorise one, or every staleness restriction has a trivial workaround. The record names **both** people, because "a refund was authorised" is worthless and naming only the supervisor re-attributes the sale. |
| **Offline password verification is byte-identical to the server's** | `SharedKernel/Crypto.cs` `Pbkdf2` — PBKDF2-HMAC-**SHA1**, 101,010 iterations, 64-byte output | MAUI's legacy `Helpers/Security/Password.cs` — **retired at WP8** | ⚠ Nothing pins the two against each other yet — see C2 |

### Screens that must never trap the operator

A till has no back button and no task switcher — it is a full-screen app on a counter with a queue
in front of it. A screen that stops responding is not a bug report, it is a shop that cannot trade,
and the two entries below are the two ways it has actually happened here. Both were reported as
*"it's just hung"*, on screens that were not at fault.

| Rule | The implementation | Second implementation | Pinned by |
|---|---|---|---|
| ⚠ **The loading overlay is driven by the DIFFERENCE between wanted and actual, one awaited operation at a time** | `AppClient/Services/Loading/OverlayGate.cs`; `LoadingViewService` is now only the MAUI half (push, pop, marshal to the UI thread) | — (the web till has no modal overlay) | `OverlayGateTests` (5), mutation-checked by collapsing the reconcile loop to a single `if` — caught by `Hide_requested_while_the_show_is_still_in_flight_still_ends_hidden` and `The_overlay_still_works_on_the_next_screen_after_that_race` |
| ⚠ **Every modal dialog has a way out — a Cancel button, the background, or Escape** | `CustomViews/AlertDialogBase.cs` — `OnBackButtonPressed` now CANCELS rather than swallowing; `OnBackgroundClicked` completes the task when interruptible. The cash-payment dialog passes a real `cancelText` | — | ⚠ **Nothing. Held by review** — a caller that omits `cancelText` on a non-interruptible dialog rebuilds the trap |
| ⚠ **A cancelled prompt UNWINDS the operation — it never falls through** | `Client.Core/TenderLoop.cs` — `TenderOutcome.Abandoned` carries an EMPTY payment list by construction, so a caller cannot mistake it for a partial success | — | ✅ **Pinned 2026-08-10 (step 11b)** — `TenderLoopTests` (19), mutation-checked three ways. `Backing_out_of_the_AMOUNT_takes_nothing_and_abandons`, `Backing_out_of_the_METHOD_…`, `Abandoning_midway_through_a_split_payment_takes_nothing` |
| **Taking payment — how a basket is settled** | `Client.Core/TenderLoop.cs`. Written in terms of what is still OUTSTANDING, so a refund is the same algorithm with the sign flipped rather than a second code path | — (the web till has its own in TypeScript — **C2 twin, PINNED ON BOTH SIDES since 2026-08-11**: `till/tendering.ts` + `tendering.test.ts`, 19 vitest cases deliberately mirroring `TenderLoopTests` — the £4-then-£6 split, a refund that gives no change, an overpay only cash can cover, and the last-tender-absorbs-the-penny rule. ⚠ Before that the WebApp had **no test runner at all**, so the arithmetic that decides what a customer is charged existed twice and was checked once. ⚠ The first version of the penny test was worthless — it used £3.33 + £3.34, whose shares round identically with or without the remainder rule, so deleting the rule left the suite green. **Equal tenders are what discriminate**: £5 + £5 against £9.99 gives two exact 0.5p shares, and `Math.round(0.5)` rounds up, so independent rounding pays 2p of change for a 1p overpay) | `TenderLoopTests` (19). Mutation-checked: accepting a zero/wrong-way tender, charging the surcharge per-tender, and letting a card give change each fail named tests |
| ⚠ **A tender that cannot settle anything is REFUSED, not accepted** | `TenderLoop` — zero, and any amount pointing away from the outstanding balance | — | `A_zero_tender_is_refused_and_the_operator_is_asked_again`, `A_tender_pointing_the_wrong_way_is_refused`. ⚠ Both were **infinite loops**: `paid += 0` never moves `paid` toward the total, and `paid > total` cannot express "wrong way" for a refund, where both numbers are negative |
| ⚠ **A card surcharge is charged ONCE PER SALE, not once per tender** | `TenderLoop` applies `TenderChoice.SurchargePence` only while none has been applied | — | `The_surcharge_is_applied_ONCE_across_a_split_card_payment`. ⚠ It used to be the CALLER's job (re-checking the basket each pass); a split card payment charged the flat fee twice if the caller forgot |
| ⚠⚠ **A tender's cap is on the TENDER TYPE across the WHOLE loop, not on one pass of it** | `TenderLoop` sums what that type has already taken (net of change, grouped by `Tenders.FromMethodName`) and subtracts it before comparing. `TenderChoice.CapPence` arrives **GROSS every pass** and a caller must NOT decrement it — a caller's number is a stored balance or an origin capacity, and neither knows what this loop has taken | `till/tendering.ts` — `refundSplitRefusal` aggregates `asked` per tender type across rows before comparing to capacity | ⚠⚠ **THE DEFECT OF 2026-08-19, and the .NET half was the wrong one.** The cap was compared per PASS, so picking one method twice took the cap twice. A £4.40 sale paid £2.00 cash + £2.40 card, refunded Card £2.40 then Card AGAIN £2.00 — each inside the £2.40 cap — put the whole £4.40 onto a card that took £2.40. ⚠ The server catches it only by **QUARANTINE** (202, terminal in `OutboxPusher`): the money has left the card machine, the till has said "complete", and the sale is destroyed afterwards. ⚠⚠ On **store credit and gift cards this was live at a counter** — £5 of credit answered a £10 basket twice, then the redeem refused the overdraw and ABORTED the sale with everything already taken. ⚠ The web till could not express it (one row per method) which is exactly why the TS half was right and the .NET half wrong. Pinned by `A_tender_cannot_take_its_cap_twice_by_being_picked_twice`, `Store_credit_cannot_be_spent_twice_over_on_one_basket`, `The_prompt_offers_what_is_LEFT_of_a_part_spent_cap`, and **`Whatever_split_the_loop_accepts_the_refund_RULE_would_also_authorise`** — which asserts the AGREEMENT with `RefundRules` rather than a behaviour, so the next drift goes red. Two mutants killed (drop the accumulation → 4 red; restore 0-means-uncapped → 1 red) |
| ⚠⚠ **Null caps nothing; ZERO means this tender may take nothing** | `SharedKernel/RefundRules.CapacityFor` — the three-way answer for one tender: `null` = ignorance (not a refund, or an origin this till could not read), `0` = a real limit of nothing, otherwise the remainder | `till/tendering.ts` — `capacityFor`, **decision for decision**, and the TS half had this right first | ⚠⚠ **The same overload caused a second live hole.** A card an earlier refund had used up reports 0 remaining and is **still offered by the picker** (`OriginTenderTypesAsync` returns every type the origin used, spent or not) — so under "0 means uncapped" the next refund put the money back onto it again, uncapped. ⚠ It was a line of LINQ inside `ExecuteCheckoutTransaction`, i.e. inside a ~200-line `async void` that nothing can test; it is now a tested rule with one home. ⚠ Note the two sides reach 0 differently — C# keeps a spent tender in the list with `RemainingPence` 0, TS drops it and falls through to 0. Same answer; a difference anything that ITERATES capacities would see. `An_empty_capacity_list_caps_nothing_because_it_knows_nothing`, `A_tender_absent_from_a_populated_list_may_take_nothing`, `A_spent_tender_answers_zero_rather_than_null` + the vitest twin |
| ⚠ **A popup is pushed once, awaited once, and popped in a `finally`** | `InputAlertHelper.ShowAsync`, `SliderAlertHelper`, `InputMultiSelectAlertHelper` | — | ⚠ Not pinned — needs `MopupService`, which needs a UI host |
| ⚠ **A screen may not hold the shared store gate without a deadline** | `TillStoreAccess.UseAsync` serialises every caller behind ONE semaphore. Callers pass a `CancellationTokenSource` deadline — `TillCadence`'s beat uses 30s, `ViewAllViewModel`'s browse 20s | — | ⚠ **Nothing. This is a convention held by review, and the next caller written without a deadline restores the fault in full.** Pinning it means an architecture test over `UseAsync` call sites requiring a token that is not `default` — worth doing when a third caller appears |

⚠ **Why the overlay rule is worth a class of its own.** The straight-line version — flip a `bool`,
then `await PushModalAsync` — strands the overlay **permanently** the first time a screen finishes
loading before the push completes, which a local SQLite read does routinely. The hide sees the flag
already set, clears it and pops a modal stack the overlay has not landed on yet; the push then lands
with the flag reading "hidden", so every later hide returns at its first line. The overlay covers
the app **for the rest of the process**, on every screen after it, and nothing is logged because
nothing threw. Fail open, always: if the gate cannot tell what is on screen it assumes the overlay
is DOWN, because a missing spinner is recoverable and a locked screen is not.

⚠ **The cash-payment dialog had no exit at all, and that is the shape to watch for.** It was raised
with no `cancelText` (so no Cancel button was built), `interuptable: false` (so clicking the scrim
did nothing) and an `OnBackButtonPressed` that returned `true` (so Escape was swallowed). Three
independent decisions, each defensible alone, combining into a screen an operator could only leave
by killing the process — mid-sale, with a customer at the counter. Worse, the two helpers behind it
looped `while (result.Count == 0) { push; await; pop; }` over a `TaskCompletionSource` created ONCE,
so the moment cancelling became possible it would have span the UI thread for ever. **Check the
combination, not the pieces.**

⚠ **Never raise the global overlay across a navigation.** It is a MODAL page, so
`App.SetLoading(true)` immediately before or during a `PushAsync` issues a modal push and a
navigation push against one window at once. MAUI does not serialise the two stacks and the WinUI
handler resolves the collision into a corrupted layout — the Inventory list drew over the tab bar,
at the wrong size, with no way back. ⚠ It was also a CROSS-SCREEN contract: the opening screen
raised the overlay and expected the opened screen to lower it, so a screen that failed to load left
the overlay over the whole app. **A screen owns its own spinner, or has none.**

⚠ **Why the store-gate rule is here rather than in "Connection".** One semaphore for the whole store
is the right call (EF contexts are not thread-safe, and two overlapping outbox writes is a lost or
duplicated sale) — but it means a wedged *screen* silences the *heartbeat*, and the till vanishes
from the fleet list while still selling perfectly well. The symptom appears on the platform, not on
the till, so it gets diagnosed as a network or enrolment problem. Anything on that gate needs a
deadline, and the heartbeat needs one most.

### Portal decides, till obeys

Each follows the same shape: the portal owns it, the till caches it on the sync cadence and renders
it. **A till that computes one of these locally is a bug.**

| Rule | Contract | Notes |
|---|---|---|
| Receipt layout | `GET /api/v1/stores/{id}/receipt-template` | The house exemplar for this pattern. ⚠ Receipts are deliberately immune to theming. |
| Colour scheme | `GET /api/v1/themes/effective` | Resolves till > group > store > tenant > default server-side. |
| VAT bands | `GET /api/v1/vat/bands` | See above. |
| Store details | `GET /api/v1/stores/{id}/info` | ⚠ Carries the **legacy `businessId`**, which is not the tenant id. |
| **Which extra barcodes an item scans under** | `GET /api/v1/items/barcodes` (any authenticated caller — the web till pulls the whole set during its catalogue sync) → the portal writes via `POST`/`DELETE api/v1/items/{itemIdOne}/barcodes` on `portal.prices.manage`'s sibling `portal.stock.adjust`; MAUI receives them on `CatalogueItemDto.Barcodes` | ⚠⚠ **AN ALIAS WRITE TOUCHES `Item.ModifiedAt`, OR NO TILL EVER HEARS ABOUT IT.** The catalogue feed's cursor pages by the ITEM, so a row in the alias table is invisible to it — exactly the trap `PricingService.TouchItemForSyncAsync` exists to carry for prices, and `An_alias_write_reaches_the_till_feed` is the test that keeps it true. ⚠ **THE WHOLE SET PER ITEM, EVERY TIME, AND THE TILL REPLACES ITS ROWS WITH IT** — that is what makes a REMOVED barcode stop scanning with no tombstone table: the item simply arrives with a smaller array. Nothing in history references an alias row (a sale line stores the scanned code as a string, no FK), so a hard delete is safe. ⚠ **The writer is STRICT, the readers TOLERANT** — the portal refuses reserved shapes, clashes and over-long codes with a sentence each; the tills just resolve what they were given. ⚠ MAUI needed a **local schema bump (v7) AND a catalogue-cursor reset**, because adding a wire field changes no item's `ModifiedAt` and an existing till would otherwise only ever see aliases for items somebody edited afterwards. |
| **Which discounts apply by themselves, and when** | `GET /api/v1/discounts/rules` (any authenticated caller — a device token works, like `carrier-bags` and `payments/gateway/active`) → the portal writes via `PUT`/`DELETE` on `portal.prices.manage`, and reads `…/rules/manage` for the paused ones | ⚠⚠ **THE SCHEDULE IS NOT PRE-EVALUATED, and that is the contract's whole shape** — see C1. The server ships the day mask, the local window and the UTC validity RAW; the till answers *"is it Wednesday"* against its own clock at the moment of the sale. Anything else strands an offline till on the day the promotion was last synced. ⚠ **A PAUSED RULE IS SIMPLY ABSENT** rather than present-and-flagged: a till that received inactive rules would have to filter them, which is a second place for the answer to be wrong. ⚠ **The writer is STRICT and the readers are TOLERANT** — the opening-hours lesson applied deliberately rather than learned again: the portal refuses a midnight-wrapping window, a rule that targets nothing, and a fraction above 1, each with a sentence a person can act on. ⚠ Fails to **last-known-good**, then to nothing: a rules feed that cannot be reached charges the shelf price, which the operator can fix at the counter — the opposite trade from the VAT bands, where an uninformed till must never render 0%. ⚠ Paused, **never deleted** (`DELETE` sets `Active = false`): every sale that took the discount points at its id, so the row is what the record of what you charged resolves against. |
| **Which carrier bags a till sells** | `GET /api/v1/carrier-bags` (any authenticated caller) → the portal writes via `PUT`/`DELETE` on `portal.company.manage` | ⚠⚠ **Ruling 2026-08-19.** Matt: *"That creates the 5p and 20p bags at the back and that pushes down to the tills… This would be cleaner than creating a bag at each till."* It replaced a **per-device preference on both tills** (`DefaultBagId` / `prefs.bagBarcode`) — free text, validated by nothing on the web side — so a five-till shop configured it five times and one till held `"001"`, a barcode no item has. ⚠⚠ **A LIST, NOT A PAIR**, and that is the answer to *"is there a time when you would have to charge 5 and 20p for a bag? Or is it one or the other?"*: a shop normally sells BOTH a statutory-minimum single-use bag and a dearer bag for life, and may add a paper or jute one. ⚠⚠ **NO STATUTORY PRICE IS HARDCODED ANYWHERE** — England's minimum went from 5p to **10p on 21 May 2021** and the four nations have moved at different times, so a figure baked into a build is wrong the next time Parliament moves; the portal asks and does not prefill. ⚠ **Price is the identity** (`BAG-<pence>`), so a shop cannot accumulate three 10p rows that split one receipt line across three report rows — changing a price means adding the new bag and withdrawing the old, which is correct because the old one is on historical receipts. ⚠ **Withdrawn = binned, never deleted**, for the same reason. ⚠ **Standard-rated (20%)** — the deliberate opposite of the gift-card item's zero band; reusing that helper would have under-declared VAT on every bag a shop ever sold. ⚠ **The one cache that fails towards NOTHING**: server → last-known-good → *no buttons*. Every other pushed setting fails towards a default, but a guessed bag price charges a customer money the shop never set. ⚠ Hidden from both inventory lists — see C2. |
| Prices | `GET /api/v1/prices/effective`, and the **timeline** on `GET /api/v1/catalogue/changes` | Resolution is `SharedKernel/PriceResolution.cs` — **store override → central list → legacy baseline** — and the server's `PricingService` delegates to it. ⚠ A price write must call `PricingService.TouchItemForSyncAsync`, or it never reaches a till: the feed pages by `Item.ModifiedAt` and prices live in their own tables. |
| Permissions | Token carries the user's full effective set; `perm:*` resolves from RBAC by userId | ⚠ `"perm:x"` and `PlutusPolicies.X` are different namespaces — a typo between them fails closed and silently. |
| Gift-card VAT treatment | `GiftCardSettings` — single- vs multi-purpose | Locks at the first card sale. Absence 409s. |
| **Which announcements reach a till** | `GET /api/v1/announcements/active` → `Client.Core.NoticesClient.ShowsOnATill` (MAUI) and `api.ts showsOnATill` (web till) | ⚠⚠ **"Exactly one home" was NOT TRUE until 2026-08-16** — this row said so while the web till carried an inline allow-list that behaved differently. It is a **C2 twin**, not a C1 single home; see the drift register. ⚠ The server ships **all three severities** and applies only the time window and tenant targeting — the *till-vs-portal* split is a CLIENT rule. **`Info` is portal-only**: the banner interrupts someone mid-transaction, and filling it with release notes trains operators to ignore the incident too. ⚠ An **unrecognised severity SHOWS** — a later backend's new severity will be at least as urgent as maintenance, and defaulting to "hide" would blank exactly the messages worth reading, silently, until someone shipped a build. |
| **Which card flow the operator runs** | `GET /api/v1/payments/gateway/active` → `Client.Core.PaymentGateway.Resolve` | ⚠ Decided by **`integrated`**, never by the provider name — every provider is `integrated: false` today, so a till reading the name would hang waiting for a terminal. A chosen-but-unwired provider is the manual flow *with the provider named*; no answer is the manual flow *with nothing pending*. The `"standalone"` key comes from `SharedKernel.PaymentProviderCatalogue`, which the server compares against too, so a rename can't leave the two sides disagreeing. |
| **Which pick notes reach a till** | `GET /api/v1/notifications` → `Client.Core.NoticesClient.IsForStore` | ⚠ The server does **not** filter by store — it returns the tenant's 50 most recent, so the addressing is the client's job. `StoreId` null = every store's tills (fulfilment undecided: everyone, rather than nobody); non-null = only that store's. ⚠ A till that doesn't yet know its own store sees everything — showing a note that isn't its problem costs a walk, hiding one that is costs an oversell. ⚠ **The web till does neither** — WP17.4. |
| **A till's very existence** | Portal → Locations → Tills → enrolment code → `POST /api/v1/tills/enrol` | ⚠ **The portal is where a till is BORN, not just what it obeys** (Matt, 2026-08-08). Company, store, till and code are created centrally; the app claims that identity and holds no opinion about who it is. |
| **When an added unit joins an existing basket line** | `SharedKernel.BasketMerge.CanMerge` — MAUI's `TillViewModel.Mergeable` translates, the web till's `basket.ts addLine` is the TS twin | ⚠⚠ **A MONEY RULE, and the two tills disagreed about it until 2026-08-18.** MAUI had **two** merge paths with **two different rules**: the search path compared the price pair, but the *selected-line fast path* checked only the item id and "not a return". So: ring a £5 item, adjust it to 50p, **leave the line selected**, scan the same item again — and the second unit joined the adjusted line **at 50p**. The shop sold it for a tenth of its price, silently, with the receipt as the only evidence. The web till has never done this (`!l.adjusted`). ⚠ The fault was **two paths, not a missing check**, so both call sites now ask the one question and there is no fast path left to be a different rule. ⚠ The `adjusted` flag is tested **before** the price pair, because adjusting a line to exactly its catalogue figure would otherwise pass the arithmetic and re-merge — the accident the old search path was relying on. ⚠ **Both halves of the price pair** are compared: the sale line's declared VAT rate is derived from that pair (C1 rule 2), so merging rows that agree on inc and differ on ex puts two VAT answers on one row. Found while doing §5c item 3 by reading the web till's reducer rather than ours. |

> ### ⚠ A till may not invent itself — and the legacy MAUI "Setup" screen does exactly that
>
> `Views/FirstTimeStartUp/SetupView` builds a **standalone** till: a locally-invented store and
> admin employee in a local SQLite file, with **no tenant, no till record and no device credential**.
> Its "Cloud" option was never implemented (`DatabaseProvider.Cloud` throws), so the only path that
> completes is the local one.
>
> **It still works, and that is precisely the danger.** It produces a till that looks fully
> configured, lets someone sign in, and can never post a sale to the platform — there is nothing to
> post it *as*. Nothing on that screen tells the operator so.
>
> Retitled `(Legacy)` and demoted below Connect-to-Plutus on 2026-08-08; **deletion is the intent**,
> once WP8 lets a portal-synced operator sign in. Third-party transfer goes the same way for the
> same reason — importing someone else's data is a central concern that already belongs to the
> NatApp translation agent, not to one device's local database.

## C2. The drift register — where the same rule exists twice

**This is the section that decays invisibly, and the one to read before writing anything that
computes money on a client.**

The web till is TypeScript and everything else is .NET, so some rules genuinely exist twice. That is
a deliberate cost. What matters is being honest about which twins are tested and which are trusted.

| Twin | Status | The honest position |
|---|---|---|
| ⚠⚠ **WHICH CARD FLOW THE CHECKOUT ANNOUNCES** — `Client.Core.PaymentGateway.Resolve` ↔ `till/cardPayment.ts resolveCardPayment` | ✅ **PINNED ON BOTH SIDES 2026-08-21, AND BOTH SIDES RUN** | ⚠⚠ **THIS ROW EXISTED AS A COMMENT AND NOTHING ELSE UNTIL TODAY.** `PaymentGateway.cs`'s own header said it *"mirrors the web till's `CheckoutDialog.tsx`, where the same three cases are decided inline"* — so one side was a named function with 15 tests and the other was a ternary buried in JSX, with a sentence between them doing the work of a pin. ⚠⚠ **AND THE TERNARY HAD ALREADY DRIFTED, IN THREE WAYS.** `gateway.provider === "standalone"` is case- and whitespace-sensitive, so a wire value of `"Standalone"` or `" standalone "` took the NAMED branch and rendered *"💳 Card via Standalone (integration pending…)"*; an **empty** provider rendered *"💳 Card via "* with nothing after it; and a provider with a blank label did the same instead of falling back to the provider key. The .NET side handled all three and had a `[Theory]` for the first. ⚠ **The rule is not "read `Provider`"** — every provider on the platform reports `Integrated: false` today, so a till switching on the name alone sits waiting for a terminal that will never respond, with a customer in front of it. What decides the flow is `Integrated`; the provider only decides the wording. ⚠ Pinned by `cardPayment.test.ts`, **case for case against `PaymentGatewayTests.cs`**. ⚠ The **sentences** are deliberately NOT shared — C1's split: the rule returns a verdict, the client composes the English, so nothing in `Client.Core` carries a translatable string. MAUI's live in `CheckoutAlert.CardHintFor` (7 tests) and the web till's in `CheckoutDialog.tsx`, and both are pinned to the same words. |
| ⚠⚠ **WHICH OF THE THREE CONNECTION FAULTS A TILL REPORTS** — `Client.Core.ConnectivityProbe` ↔ `connectionCheck.ts` | ✅ **PINNED ON BOTH SIDES 2026-08-21** | ⚠⚠ **THE WEB TILL'S LOGIN SCREEN HAD NO CONNECTION CHECK AT ALL**, so there was nothing to drift *from* — which is why this row is new rather than overdue. MAUI has had the badge since WP16a; `LoginPage.tsx` was 111 lines of email, password and error. A shop whose backend was down met a failed sign-in it could not tell from a wrong password. ⚠⚠ **"OFFLINE" IS THREE FAULTS WEARING ONE WORD** and the operator is the one who has to act on the difference: no network (their cable), no server (nothing they can do at the till), or this till revoked (a manager's job, and no router reboot fixes it). A single red badge sends shops to reboot routers over a portal setting. ⚠ **The login surface asks only the FIRST question** — `verifyIdentity: false` on both tills — because whether *this* device is still enrolled belongs where there is something to do about it (`deviceStanding.ts`, the W-P1 twin). ⚠ Same four states, same four sentences, same ordering, same 2-minute clock-skew tolerance and same 5s timeout; `connectionCheck.test.ts` mirrors `ConnectivityProbeTests.cs` for the cases this surface reaches. ⚠ **A 500 is still "connected"** on both sides — a sick server is not an absent one, and *"check your cable"* would waste the one person who could ring support. ⚠ `navigator.onLine` is used for step 0 ONLY, the same role `MauiNetworkAvailability` plays in .NET; it reports the interface and would say "online" in a shop whose broadband is down. |
| **VAT line arithmetic** — `VatLineMath` ↔ `api.ts` | 🟡 **half-pinned** | `VatLineMathTests` fixes the .NET side to the numbers the web till produces, including the JS-vs-.NET midpoint-rounding trap. Nothing executes the **TypeScript** against those numbers, so a change to `api.ts` would not fail a test. |
| **Item id derivation** — `DeterministicGuid.ForItem` ↔ `pipeline.ts itemGuid` | 🟡 **frozen vector** | `LegacySaleBridgeTests` asserts a hardcoded GUID the TS produced in a 2026-07-24 smoke test. It will catch .NET drift. It will **not** catch TS drift. Getting this wrong corrupts item ids silently — stock still moves, because lines key on the barcode. |
| **Basket totals / ex-VAT apportionment** — `VatLineMath` ↔ `till/basket.ts basketTotals` | ⚠ **third copy, unpinned — on the WEB till only** | `basketTotals` re-implements the same discount apportionment for the on-screen total. It must agree with the checkout payload or the screen and the receipt disagree, and nothing enforces it. ✅ **MAUI does not have this problem** (2026-08-09): `SaleAssembler.Total` and `.Assemble` run the SAME `VatLineMath.ForLine` calls, so its display total is a projection of the payload rather than a second calculation. The web till's copy remains — WP15/WP17. |
| **Basket → `IngestSaleRequest`** — `Client.Core.SaleAssembler` ↔ `api.ts` (~1062) | 🟡 **twinned, .NET side pinned END-TO-END** (2026-08-09) | Deliberate twins: TypeScript and .NET. The .NET half is pinned by 19 unit tests **and** `SaleAssemblerE2eTests`, which posts an assembled mixed-rate basket to the real `/api/v1/sales` and requires **201 Recorded, not 202 quarantined** — the first client-assembled payload the endpoint has ever accepted. ⚠ Three rules are load-bearing and each has a named test: the header is the SUM of its lines (the server's reconcile invariant rejects anything else), `LineMeta.itemIdOne` is refused when missing (the server would accept the sale and silently skip its stock movement), and the item id is RE-DERIVED via `DeterministicGuid.ForItem` rather than trusted. ⚠ The TypeScript half is still unexecuted by any test. |
| ⚠⚠ **WHEN A SCHEDULED DISCOUNT IS LIVE, AND WHERE IT LANDS** — `SharedKernel.ScheduledDiscounts` ↔ `till/scheduledDiscounts.ts` | ✅ **PINNED ON BOTH SIDES 2026-08-20, AND BOTH SIDES RUN** | 29 vectors each, deliberately the same ones: the Wednesday/Thursday pair, the Sunday-first day mask, the window inclusive at **both** ends (a 09:00–17:00 rule is live AT 17:00:00), the midnight wrap matching nothing, category-vs-barcode targeting as a UNION, and the four exclusions. ⚠⚠ **THE MONEY VECTOR IS `a rule that targets nothing lands on nothing`** — the carrier-bag "fail towards NONE" rule, and the mutant that makes it fail towards EVERYTHING was watched dying in .NET. ⚠ Three .NET mutants killed in total (empty-target→everything; day mask ignored — 4 tests; the tie flipped). ⚠ **THE TS SIDE WAS EXECUTED** on the build Mac (`npx vitest run`, 335 green across 21 files) — not mirrored and trusted, which is the failure the card-surcharge row records. ⚠ One asymmetry stated in both files: the .NET arithmetic additionally caps at the line's value and THROWS above a fraction of 1; neither is reachable from a well-formed rule, because `IsWellFormed` refuses `> 1` and for any fraction ≤ 1 `round(v × f) ≤ v`. Provably identical for every input a rule can produce. |
| ⚠⚠ **WHICH AUTOMATIC DISCOUNT WINS** — `SharedKernel.AutoDiscounts` ↔ `till/autoDiscounts.ts` | ✅ **PINNED ON BOTH SIDES 2026-08-20, AND BOTH SIDES RUN** | ⚠⚠ **A MONEY TWIN, and the dangerous direction is silent**: a Gold member buying Warhammer on a Wednesday must be charged the same on both tills, and two tills each inventing their own tie-break differ by real money on a basket nothing downstream can flag. Mirrored exactly: **the LARGER wins, never both** · a **tie goes to the MEMBER** (`>=`, fixed rather than left to comparison order) · several rules resolve **biggest pence, then lowest id** (the only stable key offline) · manual beats automatic · the four exclusions settled before either source is asked. 16 vectors each. ⚠ The .NET mutant that flips the tie to the rule was watched killing `A_tie_goes_to_the_member` by name. ⚠ **`memberLabel` is twinned here too** and carries the discriminating vector `12.5% → 13%`: `Math.round` is half-up and the .NET side asks for `AwayFromZero` explicitly, where .NET's DEFAULT (banker's) would answer 12 — a 10% rate cannot tell those apart. |
| ⚠⚠ **WHETHER AN AUTOMATIC DISCOUNT IS THE TILL'S OR THE OPERATOR'S** — web till `LineDiscount.auto` ↔ MAUI `BasketAlteration.Automatic` | ✅ **twinned 2026-08-20, the REDUCER itself under test on the web side** | ⚠⚠ **TWO FAULTS LIVE HERE AND NEITHER IS VISIBLE TO A UNIT TEST OF THE RULE.** ① The resolver re-runs on every basket change; if its own output counted as "already discounted" the discount would **vanish the moment a second item was scanned**. ② If clearing an automatic discount did not WAIVE it for that line, it would **reappear on the next scan** and full price would be unreachable — the promise "you can always charge full price" would last one scan. ⚠ The members' discount could be recognised by its sentinel id alone; a scheduled rule **cannot**, because its id is a real catalogue id an operator can pick by hand off the same list. So the distinction is RECORDED, never inferred. ⚠ Pinned by `autoDiscountBasket.test.ts` (10) — which drives **`reduceBasket`, the real reducer, now exported for exactly this reason**, closing the honest weakness the `BasketMerge` row below still records — and by `AutoDiscountBasketTests` (MAUI, 22, including the id-collision case where a rule's id equals a hand-applied discount's). ⚠ On MAUI the waiver is detected by a removal seen while the rebuild flag is DOWN: ours all happen inside it, so that is the one reliable way to tell "somebody took this off" from "we are rebuilding". |
| ⚠⚠ **WHICH ITEM A SCANNED CODE RESOLVES TO** — `ItemController.FindById` ↔ `TillStore.FindByBarcodeAsync` ↔ web `offline.ts cachedItemByAlias` | ⚠ **THREE COPIES, equivalent BY RULE; two of the three pinned by executable tests** (2026-08-20) | Three because the resolution has to happen where the data is: on the server for an online scan, in SQLite for MAUI, in IndexedDB for an offline web till. All three answer identically: try the item's OWN `IdOne` first, then the alias table, return the **CANONICAL item**, and refuse a binned one either way. ⚠⚠ **THE SHARED PROPERTY IS WHAT MATTERS, NOT THE CODE**: the alias string must never leave the resolver. A leak causes two silent faults — a phantom `StockLevel` from `StockProjectionConsumer`, a null VAT band from `VatBandStamp` — and `SaleAssembler`'s GUID/IdOne mismatch throw is the tripwire that catches it. ⚠ Matching is exact-after-trim on all three, deliberately: MySQL collation, a case-sensitive SQLite `==`, and a case-sensitive IndexedDB key. That asymmetry already existed for `IdOne` and has never bitten because scanners emit exact strings; folding only for aliases would make one match where the item's own barcode would not. ⚠ **The web copy is unexecuted by any test** — `offline.ts` has never had a harness (node has no IndexedDB), which is the honest position and why **§G66c** hand-checks the offline scan. |
| **Manual line discount** — `SharedKernel.LineDiscounts` ↔ `till/basket.ts lineDiscountPence` | 🟡 **half-pinned** (2026-08-09) → ⚠ **and the WEB side is now ONE expression, not two** (2026-08-20) | Fixed-per-unit × qty, or a FRACTION of the whole line rounded once. ⚠ `Percentage` **throws above 1.0** rather than obeying: the legacy MAUI till computed `item.Price * decimal.Parse(operatorInput)`, so entering `10` for "10%" multiplied the price BY TEN and the basket charged it. ⚠ Returns take no discount on either till — a refund returns what was actually paid. ⚠⚠ **2026-08-20: `lineDiscountPence` now DELEGATES to `scheduledDiscounts.discountPence` — a copy REMOVED, not added.** The auto-discount resolver has to predict what a candidate rule is worth in order to pick the largest, and predicting it with a second expression is how a "largest wins" comparison chooses a different winner from the one the customer actually pays. The `ItemSearch` precedent: the fix for a duplicated rule is one home, not two careful copies. |
| **Basket-level discount → per-line discounts** — `SharedKernel.DiscountApportionment` ↔ `till/basket.ts` | ⚠ **one copy today — MAUI only** (2026-08-09) | ⚠ **This closed a P0 shipped by cutover step 11.** A MAUI discount is a separate `BasketAlteration` record holding a NEGATIVE price; `ExecuteAlterTransaction` never writes it back onto `BasketItem.Price`. Step 11's `LinesFrom` skipped alterations *under a comment asserting the money was already in the line price* — it was not. The till's `sale.Total` sums every record, so the tenders settled against the discounted figure while `GrossPence` was built from undiscounted lines, and the server's `Σ tender − Σ change == GrossPence` invariant would have answered **202 Quarantined** on every discounted sale — which `OutboxPusher` treats as terminal and never retries. Nothing was lost only because the outbox does not drain until step 13. ⚠ The platform model has nowhere else to put it: no basket-level discount field, and `GrossPence` must equal Σ line gross, so "£5 off these three" MUST be apportioned before it can be sent. ⚠ Largest-remainder, because 3 × 333.33p loses a penny and a lost penny fails the invariant. ⚠ Ties break on the **earlier line** so two tills put the odd penny in the same place. Pinned by 13 tests + `CheckoutCommitTests`, mutation-checked against the step 11 behaviour. The web till reshapes its basket in WP15; when it grows a basket-level discount this becomes a twin. |
| **Gift-card scan ROUTING** — `SharedKernel.GiftCardCodes.LooksLikeCard` ↔ web till `TillPage.tsx:35` `GIFT_CARD` regex | ⚠ **two copies, and they DISAGREE — recorded 2026-08-16** | The regex is `/^G[0-9A-Z]{13}$/i`. It accepts **I, L, O and U**, which are not in the Crockford alphabet, and it **never checks the check character** — so the web till routes mis-scans to a gift-card lookup that must 404 before it falls through to the item search. ⚠ **It fails SAFE** (a wasted round trip, then the right answer), which is why this is a discrepancy to record rather than a defect to rush: MAUI uses the real rule and a mis-scan never leaves the till. ⚠ Converges when the web till adopts the shared rule — it cannot today, being TypeScript, so this is the same standing gap as every other C2 twin on that side. ⚠⚠ **Do NOT "fix" the regex by tightening it to the alphabet alone**: `Crockford32.Normalise` folds O→0 and I/L→1 *deliberately*, so a human typing O for 0 still finds their card. A tightened regex would refuse a valid code read down the phone. |
| **The gift-card activation id** — `SharedKernel.GiftCards.ItemIdOne` ↔ `SalesIngestService`'s private `GiftCardItemIdOne` ↔ the web till's `giftCardCode` marker | ⚠ **three copies, one pinned** (2026-08-16) | `"GIFT-CARD"` decides that an activation line posts **zero VAT** (VAT falls due when the card is spent) and takes **no members' discount** (stored value is a liability, not a supply). ⚠ Get the identification wrong and *both* silently stop applying, on a line that looks entirely ordinary. The shared constant went in when MAUI needed it for the members'-discount exclusion — a third hardcoded literal was the alternative. ⚠ The server's private copy is **deliberately left for now**: pointing it at SharedKernel is a one-line, zero-behaviour change that would still oblige a backend version bump and redeploy, which is not worth doing on its own. `GiftCardConstantTests` pins the value; **the duplicate goes with WP13**, when gift cards reach MAUI and the server can reference it as part of a change that ships anyway. |
| **How a table orders, filters and pages** — `SharedKernel.TableSort` ↔ `DataTable.tsx` `cmp()`/filter/`PAGE_SIZES` | 🟡 **half-pinned, and NEW on MAUI** (2026-08-16) | Numeric-aware ordering (`"Store 2"` before `"Store 10"`), case- and accent-insensitive to match `sensitivity: "base"`, case-insensitive `contains` search, page sizes 25/50/100. ⚠ **MAUI had NONE of this** — `table-standard.md` said "three surfaces" and MAUI is the fourth, so a till claiming to follow the standard had no sortable table anywhere. ⚠ The .NET half is pinned by `TableSortTests` (27, mutation-checked on the digit-length rule — removing it kills the headline "Store 2 / Store 10" case). The TypeScript half is unexecuted by any test, the usual position on that side. ⚠⚠ **Equivalent BY RULE, not proven byte-identical**: the web till delegates to ICU's `localeCompare`, this implements the documented behaviour, and exotic input could differ. Ordinary shop data — names, barcodes, "Till 3" — is pinned by the tests above. |
| **Discount reason normalisation** — `SharedKernel.DiscountAudit.NormaliseReason` ↔ `till/basket.ts normaliseReason` | ✅ **twinned and pinned on BOTH sides** (2026-08-14) — the first twin in this table that is | Trim, collapse interior whitespace to one space, truncate at **200**. ⚠ The collapse is what makes a report *usable*: without it "damaged    box" and "damaged box" are two distinct reasons, and the most common reason in a shop fragments across every spacing an operator types. ⚠ Whitespace-only must normalise to *nothing* — `"   "` is present to a SQL query and blank to a human, which is the empty column default 22(c) exists to prevent. ⚠ **This one IS executed on both sides**: `DiscountAuditTests` in .NET and `discountReason.test.ts` in vitest, deliberately over the SAME cases, and both mutation-checked (removing the collapse kills a named test in each). **Add a case to one, add it to the other — a twin pinned on one side only is not pinned.** ⚠ The 200 is asserted as a literal in the TS test precisely so a change on the .NET side fails there rather than drifting. |
| **Discount authority → wire** — `Client.Core.DiscountAuthorityWire` ↔ `api.ts` checkout `discountAuthority` | 🟡 **half-pinned** (2026-08-14) | Same field names, same per-line-share meaning. ⚠ The .NET half is pinned by `DiscountAuthorityWireTests` including the **legacy-bridge FK tripwire** — the test that fails if anyone moves these fields onto `LineMeta.discounts[]`, which would FK-fail the projection of every discounted sale. The TypeScript half is an object literal no test executes, the usual position on this till. ⚠ **The two are NOT identical in what they can express, and that is honest rather than drift**: the web till never writes `authorisedBy` because it has no step-up to write one from (Part B, default 22(b) ⬜ there). If that row ever goes ✅ without this cell changing, one of them is wrong. |
| **Tender name → wire byte** — `SharedKernel.Tenders` ↔ `Plutus.Entities.Models.TenderType` ↔ web till `api.ts tenderTypeFor` | 🟡 **three copies, all pinned** (2026-08-09) | The bytes sit on a byte column and every payment-split report groups by them, so a renumber does not break a build — it silently re-labels history. ⚠ The enums could NOT simply move to SharedKernel: 69 backend files import both namespaces (CS0104), and renaming the backend's copy would move the EF model snapshot. So SharedKernel holds **constants** and `TenderTypeParityTests` pins all three — including reading `tenderTypeFor` out of `api.ts` and asserting **gift is tested BEFORE credit**, because "Gift card" matches neither cash nor online and a naive chain files it as store credit: different liability, different reconciliation. ⚠ The TypeScript pin is a TEXT assertion, which is coarse — but the alternative is no pin at all until WP15 lands a JS runner. |
| **Effective price PAIR** — `TillStore.EffectivePricePairAsync` ↔ web till's price resolution | 🟡 **half-pinned** (2026-08-09) | The line's declared rate is derived FROM the pair (C1 rule 2), so both halves must come from the SAME published point — pairing an inc price from the timeline with an ex derived from the snapped baseline rate produces a rate nobody set. Pinned on the .NET side by `TillStoreTests`; the web till's resolution is not executed by any test. ⚠ Recorded here because the wobble is **price-dependent**: 1990bp at £5.00 against the 1993–2004bp the docs quote for £10–£20. Any future range-check on a declared rate must scale with the line. |
| **Item search matching** | ✅ **fixed 2026-08-08 — one implementation now** | Was THREE copies: the server's `ItemParameters.Tokenise`, the web till's `offline.ts searchTokens`, and a fourth about to be written for MAUI. Both existing copies carried a *"keep in sync"* comment, which admits the problem rather than fixing it. Now `SharedKernel/ItemSearch.cs`, and **the server's `Tokenise` delegates to it** — a copy removed, not added. Pinned by `ItemSearchTests` (15) including the `batman one` → *Batman Year One* vector. ⚠ Word-vs-phrase mode remains a per-device **preference** and the two are *supposed* to differ; what must not differ is two tills with the same setting. ⚠ The TypeScript copy is still unexecuted by any test — see the root-cause note below. |
| **What a support ticket's status and severity SAY** — `Client.Core.SupportLabels` ↔ `api.ts SUPPORT_STATUS` / `SUPPORT_SEVERITY` | 🟡 **twinned 2026-08-16, .NET side pinned** | ⚠⚠ **The web till maps BY POSITION** — `SUPPORT_STATUS[t.status]`, a literal array indexed by the server's enum byte. The day `SupportStatus` gains a member anywhere but the end, every label after it shifts by one and a **Closed** ticket silently reads as **Open**, with nothing to notice. MAUI maps **by value** and reports an unknown one as `Status 3` rather than borrowing its neighbour's word. ⚠ Not converged, because the web till's array is correct *today* and rewriting it is a change to a screen nobody asked about; recorded here so the next person to touch that enum knows there are two readers and only one of them is safe. ⚠ `SupportLabelsTests` asserts against `Plutus.Entities.Models.SupportStatus` **itself**, not against literal numbers, so a renumber fails a test instead of mislabelling a ticket on a shop floor. ⚠ MAUI says "Waiting on you" where the web till says "Waiting on client" — deliberate: from a till, *the client* is the person reading the screen. |
| ⚠⚠ **When a queued cash event may be sent, and when a day is closed** — `Services/Sync/CashPushService` ↔ `cashRules.ts` | ✅ **twinned 2026-08-17, BOTH sides pinned** | ⚠⚠ **Three decisions, and every one of them is a way to lose or hide money.** **(a)** `lastMarkClosesDay` takes the **latest** Z mark, never `some(ZClose)` — the any-Z version shuts a reopened day for ever, which is why the reopen is a compensating event rather than a deletion (the ZClose stays: somebody counted and banked that drawer, and a variance was computed against it). Three places ask this question — cash guard, sales gate, local copy — and they must agree, or a day open for a float and shut for a sale is a disagreement nobody finds until the figures stop matching. **Ties go to CLOSED.** **(b)** `zMayGo` holds a Z until its own day's **pending** sales have drained: the expected drawer is float + cash takings + ins − outs, so a Z that overtakes queued sales tells the person who counted correctly that they are short by every penny not yet sent — and the Z being terminal server-side then quarantines those very sales behind their own close. ⚠ Pending only, never parked, or one rejected sale blocks the close for ever; and per business **day**, so last week's stuck sale cannot block today. **(c)** `terminalRefusal` is **409 and 400 only** — everything else retries, because a till that retries a terminal refusal for ever *looks healthy while quietly never banking*, which is the failure that hides money instead of losing it. ⚠ The expected drawer is **never computed by either till** — both record what the platform said. `cashRules.test.ts` (17), both money mutants killed. |
| ⚠⚠ **How long a cached credential is trusted** — `SharedKernel.OfflineCredentials` (+`Crypto.Pbkdf2`) ↔ `offlineLogin.ts` | ✅ **twinned 2026-08-17, BOTH sides pinned** | ⚠ Five horizons (**7d · 30d · 3d · 15min · 12h**), three trust tiers, and a **default-deny sell floor** — mirrored exactly, because the .NET header's shape is the whole point: *selling stays alive for a long time, money-out expires quickly, and the till NEVER hard-locks.* A till that stopped selling over an old roster would close a shop for a connection problem. ⚠⚠ **The floor is an ALLOW-LIST** on both sides, so a permission added to the catalogue later is withdrawn when stale until somebody deliberately adds it — a new permission that silently survived staleness is precisely the bug the tiering prevents. ⚠ **A future-stamped roster clamps to fresh** on both sides rather than refusing: failing the other way stops a shop trading over a wrong clock. ⚠ Session end is the **earlier of the cap and the business-day rollover** — a session spanning two business days leaks yesterday's operator into today's X/Z breakdown, silently, in the records HMRC would ask about. ⚠⚠ **`Pbkdf2` is twinned too and SHA-1 is DELIBERATE** — it preserves the legacy NatApp hash byte-for-byte, so a "modernised" SHA-256 browser twin would refuse **every valid password ever set** and would present as a wrong-password bug rather than a hash mismatch. Pinned by name on both sides (`OfflineCredentialsTests` · `offlineLogin.test.ts` 30). |
| ⚠⚠ **What an operator may do, and up to how much MONEY** — `SharedKernel.PermissionResolution` (+`PermissionGrant.IsActiveAt`, `EffectivePermission.Merge`) ↔ `permissions.ts` | ✅ **twinned 2026-08-17, BOTH sides pinned** | ⚠⚠ **A MONEY twin** — it decides how much a cashier may take off a basket, so the two tills disagreeing means one shop's cashiers have a limit and another's do not. And the dangerous direction is **silent**: an over-generous ceiling looks exactly like a correct one until somebody audits the discounts. Mirrored exactly: **union-merge** (per code, unlimited beats any ceiling, otherwise the LARGEST wins — *not* intersection, which would silently demote every manager who also holds a cashier role) · **inclusive** boundary (`amount <= max`) · **fails closed** on an unknown or mis-cased code (`"perm:x"` and a policy name are different namespaces, and a typo between them already caused one silent outage here) · a **null amount against a capped grant is ALLOWED**, because *"may discount at all"* and *"may discount £40"* are different questions · valid-from/to in **UTC**, day mask and time window in **local** · day mask **Sunday = 0**, matching .NET's `DayOfWeek`. ⚠⚠ **The midnight-wrap limitation is mirrored ON PURPOSE** — a 22:00–02:00 window rejects everything on both tills, because .NET left it *"deliberately unhandled rather than half-handled"*; fixing it on one till alone would make them disagree. ⚠ A malformed window is treated as **absent**, not as a closed door — a bad portal value must not lock a shop out. `permissions.test.ts` (22), mutation-checked twice. |
| ⚠⚠ **Whether a disabled operator stays signed in** — `Client.Core.OperatorRevocation` ↔ `roster.ts isStillPermitted` | ✅ **twinned 2026-08-17, BOTH sides pinned** | ⚠⚠ **One catastrophic way to be wrong, and it looks identical to the right one at a glance**: treating *"the roster could not be fetched"* as *"you are not on the roster"*. That version signs **the whole shop out, mid-sale, every time the broadband hiccups** — with a message accusing the operator of being disabled — and it fails first on the worst-connected sites. So **null means carry on**, always, and only a roster the server ACTUALLY SENT can revoke. ⚠ An **empty** roster the server sent is a real answer and does revoke — nobody is assigned to this till any more. Both halves are pinned on both sides (`OperatorRevocationTests` 7 · `roster.test.ts` 11), each mutation-checked. ⚠ Matched **case-insensitively** on the user id: a Guid can arrive in either case from either end, and a casing mismatch would sign out every operator in the shop while the roster was perfectly correct. ⚠ The operator-facing sentence is Matt's, verbatim, and identical on both tills — it names the **account**, not the till. |
| ⚠⚠ **Whether a revoked device may still trade** — `Client.Core.DeviceRevocation` ↔ `deviceStanding.ts` | ✅ **twinned 2026-08-17, BOTH sides pinned** | ⚠⚠ **The most dangerous twin on this list**, because the two wrong answers fail in opposite directions: too lax and a lost or stolen till keeps selling for its whole 12 h token life (device tokens have **no server-side denylist**); too strict and a dropped connection closes a shop mid-sale, on the worst-connected sites first. **Only the literal status `Revoked` stops a till** — a failed poll, a 401, a 403, a **404** and any unrecognised status all keep trading, and `PendingRemoval` trades on purpose so that *requesting* a removal cannot be used to take a shop down. ⚠ 404 is deliberately not terminal: approving a removal sets `Status = Revoked` rather than deleting the row (`TillsController.DecideRemoval`), so the explicit answer always arrives, while a wrong device id or a tenant-filter miss would otherwise close every till at once. ⚠ **Pinned on both sides against the same cases** — `DeviceRevocationTests` (19) and `deviceStanding.test.ts` (21), each mutation-checked in both directions. ⚠ The wording an operator reads is identical on both tills by design; it names the **till**, never the account. |
| ⚠⚠ **What the store's OPENING HOURS mean** — `Client.Core.OpeningHours` ↔ `openingHours.ts` ↔ the portal's `hoursProblem` (the writer) | ✅ **three copies, all pinned, and DELIBERATELY ASYMMETRIC** (2026-08-17) | ⚠⚠ **THE DEFECT WAS THAT ALL THREE AGREED TO SAY NOTHING.** Matt: *"webtill does not show the opening hours set in the portal."* Both tills printed **one sentence — "Not set — add opening hours in the management portal" — for three different states**: nothing stored, stored-but-malformed, and stored-in-a-shape-the-reader-did-not-expect. So the report could not be diagnosed from the screen, and the advice it gave was to retype hours that were already there. ⚠⚠ **The portal made that reachable**: its "Advanced (JSON)" textarea sent whatever was typed straight to the server with **no validation of any kind**, and its simple editor's `catch { return {} }` then showed a malformed value back as *every day unticked* — so the portal looked *set* to the person who typed it while every till read *not set*. ⚠ **Now: the tills are TOLERANT, the portal is STRICT.** The readers stretch to what is already in the field (case-insensitive and long day names, a bare span object, `from`/`to`, `"09:00-17:30"` including an en dash, `9:00`→`09:00`, seconds dropped, `closed`/`[]`/`false` as an explicit CLOSED, two spellings of one day MERGED); the writer refuses to create anything a reader would have to stretch for, gates Save on it, and forces the section open when the stored value is unreadable. A writer as lax as its readers guarantees the next odd shape reaches production. ⚠ **`unreadable` is a distinct state on both tills**, carrying the parser's own message, and it is what the register now claims — not "shows opening hours". Pinned by `OpeningHoursTests` (26) and `openingHours.test.ts` (27), the SAME VECTORS, plus one mutant: making the parse failure return "unset" (the original bug) kills two tests. ⚠ These are STRING vectors, so unlike the surcharge row below the mirroring genuinely pins both languages — there is no `decimal`/`double` asymmetry hiding in them. |
| ⚠⚠ **What a price ADJUSTMENT does to the VAT** — `SharedKernel.PriceAdjust` ↔ `till/priceAdjust.ts` | ✅ **twinned 2026-08-18, BOTH sides pinned** | ⚠⚠ **MAUI WAS GETTING THIS WRONG IN A WAY NOTHING COULD SEE.** It asked the operator for the **ex-VAT price AND the inc-VAT price** in one dialog and wrote both onto the line. The sale line's declared rate is derived from that pair (C1 rule 2), so two hand-typed numbers WERE the VAT figure on the sale: £10.00 ex against £10.50 inc declared **5% on a 20% item**, no validation, nothing downstream able to tell. The web till has always asked for one number and scaled. Now one rule, one number, ex derived from the catalogue proportion. ⚠⚠ **AND THE MUTATION RESULT DIFFERS BY LANGUAGE, which is the lesson to carry:** the ratio-first mutant (`newInc × (ex ÷ inc)`) **SURVIVES in C#** — `decimal` carries 28–29 digits, so 999 × (1000÷1200) still rounds to 833 — and **DIES in TypeScript**, where a double cannot hold 70/100 and `45 × 0.7` returns 31 instead of 32. Both suites carry the vector; the mutation was run in both and watched failing in the one that can detect it. Second time this pattern has appeared (see the card-fee row) — **a mirrored test proves only what the host language can distinguish.** |
| ⚠⚠ **The card fee, and the VAT it carries** — `SharedKernel.CardSurchargeVat` + `Storage/CheckoutCommit.SurchargeItem` ↔ `till/surcharge.ts` | ✅ **twinned 2026-08-17, BOTH sides pinned** | ⚠⚠ **THE TYPESCRIPT SIDE WAS "VERIFIED" BY COPYING THE .NET VECTORS AND WAS STILL UNPINNED, and this is the lesson for every other row on this list.** `CardSurchargeVatTests` forces products-before-division with `3 × 10000 ÷ 12000 = 2.5` — `decimal` computes `10000m/12000m` as `0.8333…3`, so ratio-first gives 2.4999… and rounds DOWN, and the test goes red. A **double** rounds `10000/12000` **up**, to `0.8333333333333334`, so ratio-first gives 2.5000000000000004 and the same vector **passes**. Mutation-tested and the mutant SURVIVED: the TS suite was mirroring the other language's tests and inheriting none of their protection. `45 × 70 ÷ 100 = 31.5` (ratio-first: `45 × 0.7 = 31.499999999999996`) is the vector that actually discriminates in JS, and it is now in `surcharge.test.ts`. ⚠ **A copied vector is not a copied guarantee** — the arithmetic is only pinned where the *host's* rounding can go wrong, and that is a different set of inputs in each language. ⚠ Searched exhaustively afterwards: over gross 50p–£50, flat fee 15–60p and every ex/gross ratio between pure-20% and pure-zero-rated there is **no** divergence, so nothing was ever mispriced — the rule was unprotected, not wrong. ⚠ Six mutants total; the other five died on the first run (truncating the percent half, letting the fee ride on returns, counting a gift card as a card, dropping the once-per-sale guard, and drifting the `CARD-SURCHARGE` spelling between `surcharge.ts` and `basket.ts`). ⚠ **`FeePence`/`PairFor` throw on both tills** for a negative setting or an impossible ex total; the web till catches it, refuses the sale and says why (fail closed on money) rather than blanking the till. |
| ⚠⚠ **How a portal theme is read** — `Client.Core.ThemeSlots` ↔ `theme.ts` | 🟠 **twinned 2026-08-17, .NET side pinned — but the MAUI half is barely CONNECTED (checked 2026-08-18)** | ⚠⚠ **Matt asked for this one by name**: *"Changing the theme in the portal needs to be consistent across the web and MAUI till."* `colorsJson` is an **opaque blob the server does not validate** (`EffectiveThemeResult` says so), which makes these two the only thing between a bad value and an unreadable till — and they must agree, or one assignment paints two shops differently and the portal's preview is a lie about at least one of them. **Every rule is mirrored deliberately:** the seven slot names and their order · **six-digit hex only** (`#abc` and 8-digit alpha refused — an alpha channel could render text at zero opacity) · **a bad or absent slot falls back to the stock palette rather than a substitute** (the web till `removeProperty`s it; MAUI leaves the `Theme*` key alone) · ⚠⚠ **a malformed blob still applies the base mode** (*"opaque blob was bad — base mode still applies"*) · an **unknown** slot is ignored so a later portal's eighth cannot break an older till · slot names matched **case-sensitively**, because the blob is written against the web till's camelCase `ThemeColors`. ⚠ The stock accent `#2c698d` is the seam: it is the MAUI palette's `Quinary` **and** the web till's `DEFAULT_ACCENT`, so "clear every override" lands on the same colour on both. ⚠ **Resolution order is NOT twinned and must never be** — `ThemeResolution` does till > group > store > tenant > default **server-side**, so there is nothing for a till to get wrong. ⚠ `ThemeSlotsTests` (41) is written against the cases where the two could differ, mutation-checked twice (drop the hex gate; throw on a bad blob). ⚠ **The TS side is unexecuted by any test** — the same root cause as the other twins on this list: converged by review, not by CI.  ⚠⚠ **AND PARSING IT IS NOT THE SAME AS PAINTING WITH IT — checked 2026-08-18, and the answer was worse than this row implied.** The slot PARSING is twinned and the .NET side is pinned, which is what this row claimed and all it ever claimed. But the web till has **92 CSS rules** consuming the variables while **MAUI has ONE file** (`StoreOptionsViewModel`, and only since Store Information was rewired that day) — and its six `Colors.xaml` brushes are built with **`StaticResource`**, so they capture the colour at parse time and cannot follow a theme change. **No MAUI XAML uses `DynamicResource` for a slot at all.** ⚠ So a scheme set in the portal repaints the web till and one MAUI screen. ⚠ The lesson for this register: a C2 row proves two implementations AGREE ABOUT A RULE; it says nothing about whether either one is WIRED TO ANYTHING. Sized as §5c item 10.|
| **Which announcements reach a till** — `Client.Core.NoticesClient.ShowsOnATill` ↔ `api.ts showsOnATill` | ✅ **converged 2026-08-16 — the web till followed** | ⚠⚠ **They had disagreed since WP15.1, and C1 claimed there was only one copy.** MAUI denies `Info` and shows everything else; the web till had an inline **allow-list** — `severity === "Maintenance" \|\| severity === "Incident"` — so any severity a later backend adds would appear on the MAUI till and be **silently dropped** on the web till, in the same shop, for the same incident. That is the exact failure the MAUI rule's header describes ("defaulting to *hide it* would silently blank exactly the messages worth reading") and it was live in the other till the whole time. ⚠ The allow-list also broke on `"incident"` and `" Incident"`; both are now trimmed and case-folded, because whether a shop hears about an outage must not depend on a backend's capitalisation. ⚠ Pinned on BOTH sides now — `notices.test.ts` (5) is written against the cases where the two answers differ, and was mutation-checked by restoring the old allow-list (3 tests die). ⚠ The `.catch(() => undefined)` on both web-till polls is **correct** and matches `Noticeboard.Apply`: a failed poll leaves the banner alone. The web till does **not** show a stale marker, and does **not** tell the operator when an ack fails — MAUI does both. Neither is a money rule, so they are recorded here rather than chased. |
| **Legacy tax row → VAT band** — `VatBandCache` ↔ `api.ts vatBandForTaxId` | ✅ **converged 2026-08-09 — the web till followed** | Both now require **exactly one** claiming band and return **null → unclassified** otherwise. Previously the web till returned the **first match**, so when two bands claimed one tax row the answer was decided by serialisation order. That is the dangerous case, not a benign one: zero and exempt are **both 0%**, so first-match silently moves exempt takings into zero-rated (or the reverse) while every total still balances, and nothing downstream can detect it. ⚠ Its doc comment had described the strict behaviour for some time while the code did the loose thing — worth remembering that a comment is not a pin. ⚠ Still unexecuted by any test on the TS side (see the root cause below); the two are now identical **by review**, not by CI. |
| ⚠⚠ **ONE-SCREEN SETTLEMENT — what is paid, what remains, what change is due** — `Client.Core.TenderSettlement` ↔ `till/tendering.ts` | ✅ **PINNED ON BOTH SIDES 2026-08-19** (§5c item 2) | The .NET half arrived **second**, and that is the point: `tendering.ts` has carried the one-screen rules and 40 vitest vectors since 2026-08-17 while MAUI had only the sequential `TenderLoop`, which cannot answer *"does this whole screen balance"*. Matt's look-and-feel ruling required MAUI's checkout to become the web till's screen, and a screen that looked the same while computing money differently would be the worst of both. ⚠ **Ported expression by expression, not reinterpreted**, and `TenderSettlementTests` runs the SAME cases as `tendering.test.ts` — a £10 basket settled £4 then £6, a card overpay that cannot give change, a refund that must not, "rest" recomputing a filled row. **Add a case to one, add it to the other.** ⚠⚠ **Three mutants killed**: deleting the remainder-absorption rule, swapping `MidpointRounding.AwayFromZero` for .NET's default banker's rounding, and dropping the id ordering. The middle one is the documented JS-vs-.NET midpoint trap — JavaScript's `Math.round` is half-up and .NET's default is not, so on a 1p overpay across two equal cash tenders the shares are exactly 0.5p and the two languages would disagree by a penny. ⚠ `ParsePence` is a twin too, and deliberately STRICTER than the `NumberStyles` MAUI used to parse amounts with: that accepted a leading sign, so `-5.00` read as a negative tender. ⚠ `TenderLoop` STAYS — it owns the sequential question (*may this ONE tender be taken*) and remains the C1 home of the caps, refusals and surcharge rules. |
| **Taking payment** — `Client.Core.TenderLoop` ↔ the web till's checkout | ⚠ **NEW TWIN, .NET side pinned** (2026-08-10) | Extracting the tender sequence out of `TillViewModel` created a second home for a rule the web till already implements in TypeScript. Deliberate, and the same trade as every other row here: the .NET half is now 19 tests and three mutation checks; the TypeScript half is **unexecuted by anything**. ⚠ The rules most worth diffing by hand until WP15 lands a JS runner: **zero and wrong-way tenders are refused** (both were non-terminating loops on MAUI — assume the web till has at least one of them), **the surcharge is once per SALE**, and **change is only given by a method that can give it**. ⚠ Refunds are the same algorithm with the sign flipped, NOT a second branch — that is what let over-refunding past MAUI's old `paid > total` guard. ⚠⚠ **2026-08-19 — THIS TWIN DID DRIFT, AND THE DRIFT WAS MONEY. This row called it a year early and the warning was not acted on.** Both halves enforce a per-tender cap; the TypeScript half aggregates per tender **type across rows** before comparing, and the .NET half compared **one pass at a time**. So the same £4.40 basket the web till refuses, the MAUI till accepted — picking Card twice put £4.40 back on a card that took £2.40 — and on store credit and gift cards the same hole overdrew a live balance and aborted the sale after the goods were bagged. ⚠⚠ **The lesson is not "diff the twins by hand", which is what this row already said and nobody could sustain. It is that a twin of a rule that ALREADY EXISTS in `SharedKernel` should call it rather than re-derive it.** `RefundRules` had both missing rules right and tested (`Two_requests_against_the_same_tender_are_summed_before_the_cap_is_applied`, `A_tender_with_nothing_left_is_refused_rather_than_capped_at_zero`) since 2026-08-13; the loop was a weaker third implementation of a rule with a correct shared home. The fix therefore includes a test that pins the loop's verdict **against the rule** rather than against a remembered behaviour — `Whatever_split_the_loop_accepts_the_refund_RULE_would_also_authorise` — because that is the only kind of pin that survives somebody editing one side. |
| **The cash event vocabulary** — `Contracts.Client.CashEventTypes` ↔ the server's `CashEventType` enum | 🟡 **twinned by construction, .NET side pinned** (2026-08-10) | The five names exist twice because they must: the enum lives in `Plutus.Entities`, a backend project a till may not reference, and the contracts project has no references *because it ships onto tills*. ⚠ **Better than the TenderType twin in one specific way**: these travel as NAMES, so a rename fails loudly as a 400 rather than silently re-labelling history the way a renumbered byte would. ⚠ **The failure mode is still nasty** — a till that cannot post a cash event cannot bank the day, and it finds out at closing time. Pinned on the .NET side by `LocalCashEventTests`; the server's parse is `Enum.TryParse`, so adding a sixth name server-FIRST is the only safe order. |
| **The trading day** — `SharedKernel.BusinessDay` ↔ the web till's `pipeline.ts businessDay()` | ⚠ **twinned, NEITHER side pinned against the other** (2026-08-10) | Both return the LOCAL wall-clock Y-M-D, and they must: a sale and a cash event that disagree by one day make the Z-read balance against the wrong takings, and the variance is fiction with nothing in the numbers to explain it. ⚠ The day this grows a rollover hour it must grow one on BOTH tills in the same commit, or two tills in one shop will bank different days. |
| **Money representation** | ⚠ **divergent today** | Web is integer pence throughout; MAUI is `decimal` across 11 model files. WP2. |
| **UUIDv7** — `Uuid7` ↔ `pipeline.ts uuidv7` | ➖ **low stakes** | Only has to be a valid, time-sortable v7. Divergence costs ordering, not money. |
| **Business day** — the wire value ↔ `pipeline.ts businessDay` | ➖ | A till's day is deliberately wall-clock, not UTC. The server takes what it is given. |
| **Which reports a till OFFERS** — `SharedKernel.ReportCatalogue` ↔ `reporting/publishedReports.ts` ↔ MAUI's `Services/Reporting/PublishedReports` | ✅ **PINNED ON BOTH SIDES 2026-08-19** | Ruling 5b(a). **THREE implementations of one rule**, because the server decides, the MAUI till caches and the web till caches — and all three must answer the same two questions. ⚠⚠ **What does SILENCE mean?** No stored row = **every report**, not none: a tenant who has never opened the portal screen must see the menu they saw yesterday, and defaulting to "nothing" would empty the Reports tab in every shop the moment this deployed. ⚠ An **empty** stored list is a different thing — a shop deliberately publishing nothing — which is why the never-chosen case is `null` and not `[]`; collapsing them makes one impossible to express. ⚠⚠ **What happens to a key we do not recognise?** Dropped on the way OUT (old data must not break a till) and REFUSED on the way IN (a typo must not look saved and then do nothing). ⚠ Order is the catalogue's, not the stored order, or the menu reshuffles by the order somebody ticked the boxes. Pinned by `ReportCatalogueTests` (11) + `publishedReports.test.ts` (12), the same cases both sides. ⚠⚠ **The keys are WIRE VALUES stored in `ReportPublications.KeysJson`** — renaming one silently unpublishes it for every tenant that had it ticked, so both suites pin the shipped set. **Add, never rename.** |
| ⚠⚠ **What makes an id a CARRIER BAG, and what to do when its price contradicts it** — `SharedKernel.CarrierBags` ↔ web till `till/carrierBags.ts` ↔ MAUI `Services/Sales/CarrierBags` | ✅ **PINNED ON BOTH SIDES 2026-08-19** | Ruling 2026-08-19. Three copies because the web till is TypeScript, and all three answer the same two questions. **(1) What is a bag id?** Exactly `BAG-` + a positive whole number of pence — so `BAG-0` (a free bag is not a receipt line), `BAG--5`, `BAG-abc` and `BAGGY-10` are all refused, and a shop's real product barcode is never mistaken for one. **(2) What if the stored price disagrees with the id?** ⚠⚠ **THE ROW IS DROPPED, NEVER REPAIRED.** A `BAG-10` selling at 25p means somebody edited the item behind the portal's back and there is no way to know which figure is right — so it does not become a button. Refusing to sell is recoverable; selling at a made-up price is a refund and a complaint. ⚠ **Both sides RUN**: `CarrierBagTests` (24) in .NET and `carrierBags.test.ts` (11) in vitest, deliberately over the same vectors. **Add a case to one, add it to the other.** ⚠ The MAUI copy re-filters on the way OUT of its cache, not just on the way in, so a cache written by an older build cannot sell something this build would refuse. ⚠⚠ **The ONE deliberate asymmetry with `publishedReports.ts`**, stated in all three files: reports fail towards MORE (a missing Reports tab looks broken), bags fail towards NONE (a guessed price charges a customer money the shop never set). If somebody ever "harmonises" those two fallbacks, this is the row that says not to. |
| **Keeping carrier bags OUT of the inventory lists** — server `ItemParameters.IncludeCarrierBags` ↔ MAUI `ViewAllViewModel`'s local filter | ⚠ **two mechanisms, same outcome, and deliberately NOT the same mechanism** (2026-08-19) | Matt: *"This could just be a unique item that doesnt show in the Inventory."* ⚠ **The web till and portal hide them SERVER-SIDE** because both lists are paged by the server: a browser-side filter would show 24 rows on a page of 25 and an "X of N" that never agrees with itself. ⚠ **MAUI hides them CLIENT-SIDE** because its list is a capped local SQLite browse, not a server page — filtering it there is exact. ⚠⚠ **The bags must stay in BOTH offline catalogues regardless**, or the Bag button works online and fails silently offline — the worst version, because it looks fine every time anybody tests it. That is why `syncCatalogue` passes `IncludeCarrierBags=true` and MAUI's `/api/v1/catalogue/changes` feed is untouched. ⚠ Pinned by `A_carrier_bag_is_hidden_but_an_untracked_item_beside_it_is_not`, which also stops the rule collapsing into "untracked items are hidden" — that would take gift wrapping and every other service line out of inventory too. ⚠ **The provisioned `GIFT-CARD` item is deliberately still visible**: the same argument applies to it, nobody has asked, and hiding something an owner is used to seeing is not a change to make unasked. |
| ⚠⚠ **WHAT A TYPED MEMBER NUMBER MEANS, AND ITS CHECK CHARACTER** — `SharedKernel.MemberNumbers` ↔ web till `memberNumbers.ts` | ✅ **PINNED ON BOTH SIDES 2026-08-19** (WP-T2) | The web till had **no member-number logic at all** before this — only the `MEMBER_CARD` shape regex — so a typed bare number answered *"Nothing found"*. Matt: *"How do I get the credit though? I have people with credit. But there is no way to select them?"* ⚠⚠ **THE CHECK CHARACTER IS WHY THIS IS A PORT AND NOT A REGEX**: `000482P` verifies and `000482Q` does not, and refusing the second is the only thing between a mistyped digit and attaching a DIFFERENT member — with their discount and their store credit — to somebody else's sale. The 7/3/1 weights are what make a **transposition** change the sum, and `482`↔`824` is the classic counter mistype. ⚠⚠ **LENGTH IS TESTED BEFORE "all digits", on both sides, and swapping those two branches is a silent wrong-member bug**: a check character is often itself a digit (sequence 1 → `0000011`), so a full number can be entirely numeric and must not be read as a bare sequence. ⚠ Ambiguous characters are FOLDED, not refused (O→0, I/L→1, U→V) — a person reading a card down the phone says "oh oh oh four eight two". ⚠ The six-digit ceiling is pinned on both sides too, because the fix if a tenant nears it is to widen the constant in both, **never** to loosen the parser: arbitrary lengths would make a bare EAN-8 canonicalise as a member number roughly 3% of the time. ⚠ Both sides RUN: `MemberNumberTests` and `memberNumbers.test.ts` (10) over the same vectors. ⚠ **The STRICT scan test stays strict on both tills** — `LooksLikeMemberScan` / `MEMBER_CARD` still demand the `C` prefix, and the loose parser is reachable only *after* an item lookup has failed. |
| **Wire records** — `Plutus.Contracts.Client` ↔ the server's own copies | 🟡 **twinned by convention** | `EnrolResult`, `DeviceTokenResult`, `StoreInfoResult`, `PingResult` and now `HeartbeatRequest/Result` + `CatalogueItemDto/ChangesResult` are declared on BOTH sides. Deliberate: the contracts project has no references and no packages *because it ships onto tills*, so no backend module references it. The E2E tests deserialise the server's JSON into the CLIENT record, which is what actually pins the two shapes together — a field renamed on one side fails `CatalogueSyncE2eTests` or `ClientCoreE2eTests`. |
| ⚠⚠ **THE PORTAL ↔ WEB TILL TWINS** — `DataTable.tsx`, `Ask.tsx`, `DialogX.tsx`, `Barcode39.tsx`, `barcodeProblem.ts` | ✅ **MECHANICALLY PINNED 2026-08-20 — the only row on this list that is** | ⚠⚠ **THIS AXIS WAS NEVER IN THIS REGISTER, AND THAT IS WHY IT DRIFTED.** Every other row here is .NET ↔ TypeScript. But the portal and the web till are **two separate npm apps that cannot share a package**, so a handful of files exist twice *in the same language* — and the only thing asking them to stay identical was a comment in each one: *"keep the two files byte-for-byte identical"*. ⚠⚠ **IT HAD ALREADY FAILED, SILENTLY, AND THE CONSEQUENCE WAS A CONTRACT VIOLATION**: the till's `Ask.tsx` imported `DialogX` and rendered a close ✕; the portal's did not. So **every** confirm / choose / prompt dialog in the portal was missing the ✕ that **D4 makes mandatory**, and had been since D4 was written — while D4's own implementation table listed only MAUI and the web till and so read as complete. The portal did not even *have* a `DialogX.tsx`. Found only because the multi-barcode work added a fifth twin and somebody checked the other four. ⚠ **NOW PINNED BY `FrontendTwinTests`** (`tests/Plutus.Tests.Architecture`) — five theories, one per twin, comparing the bytes with line endings normalised. A **missing** twin fails too, because "the portal never had a copy" is precisely the fault it found. ⚠ **It lives in .NET, not vitest**, because the web till has no `@types/node`: a test there cannot read a sibling project off disk without a new dependency, and the architecture suite already scans source text and knows the repo root. ⚠ **Verified by deliberately breaking a twin and watching it go red** (1 of 5 failed), then restoring it. ⚠ **The lesson for the rest of this table**: a "keep these in sync" comment is not a pin, and four of these five had nothing but that comment for months. Where a twin can be compared mechanically, compare it mechanically. |
| **Password hashing** — `SharedKernel/Crypto.cs Pbkdf2` ↔ MAUI `Helpers/Security/Password.cs` ↔ `Identity/TestTokenAuth.VerifyPassword` | ⚠ **three copies, none pinned against each other** | All three are PBKDF2-HMAC-**SHA1** / 101,010 / 64-byte, and they agree today only because each was written from the same legacy constants. `Rfc2898DeriveBytes(string, byte[])`'s SHA-1 default is *implicit* in two of them — a "modernising" edit to SHA-256 in any one would silently stop every existing password verifying on that surface alone. `AuthController.SetPassword` already writes a **64-byte** salt where `Pbkdf2.SaltBytes` is 32 (harmless, stored alongside — but it shows the drift is real). MAUI's copy retires at WP8; until then, **one shared vector test would close this**. |
| **Connectivity states** — `ConnectivityProbe` ↔ web till `navigator.onLine` | ⚠ **divergent today, and the web till is the wrong one** | `navigator.onLine` reports the network interface. It says "online" in a shop whose broadband is down, and "offline" for a till the portal revoked — the two cases operators most need told apart. WP16a moves the web till onto the shared probe; until it does, the two tills give different answers to "am I connected?". |
| **Basket line merging** — `SharedKernel.BasketMerge` ↔ `till/basket.ts addLine` | ✅ **pinned by a shared vector table, BOTH SIDES RUN** (2026-08-18) | The SAME ten cases run in `BasketMergeTests` (C#) and `till/basketMerge.test.ts` (TS), with the same figures — a £5/20% item as 500 inc / 417 ex. ⚠ Ten pass in C# and ten pass in TypeScript — the TS half was run on the build Mac (`npx vitest run`, 224 green across 13 files), because a vector table nobody executes is a comment. ⚠ **This twin is on the register because it had already drifted**: MAUI merged a second unit into a hand-adjusted line at the adjusted price and the web till never did (C1, and §5c item 3b). Two tills that disagree about when a line merges disagree about **what the customer is charged**. ⚠⚠ **THE TS SIDE IS A COPY UNDER TEST, NOT THE REDUCER ITSELF** — and that is the honest weakness. The web till's rule lives inside a `find(…)` inside `addLine` and is **not exported**, so the test restates it; if the reducer's condition changes and the test does not, these vectors go on passing while the tills drift again. **Exporting that predicate is the fix, and it is not done.** `basket.test.ts` covers the reducer end to end; this covers the rule. ⚠ Both C# mutants were watched dying — removing the `adjusted` guard, and dropping the ex-half of the price comparison. |

### ⚠ The root cause, stated plainly — ⚠⚠ **AND IT IS NO LONGER TRUE. CORRECTED 2026-08-20.**

> **What this section said, until it was checked:** *"The web till has no test suite. `package.json`
> has `dev`, `build`, `preview` and `typecheck` — no test runner, no test files. So every 'pinning'
> test above holds only the .NET half of its twin."*

**That was true when it was written and is now false.** `package.json` carries `"test": "vitest run"`,
and there are **21 test files / 335 cases** — `permissions`, `roster`, `deviceStanding`,
`offlineLogin`, `cashRules`, `openingHours`, `theme`, `notices`, `memberNumbers`, `pipeline`, the
`reporting/*` pair, and `till/` (`basketMerge`, `carrierBags`, `discountReason`, `priceAdjust`,
`surcharge`, `tendering`, and the three added by the Discount plan).

⚠⚠ **THE STALE CLAIM WAS DOING REAL DAMAGE, which is worth more than the correction.** Rows on this
table say "the TypeScript half is unexecuted by anything" and "converged by review, not by CI" — and
for several of them that was **already out of date**, because a runner existed and nobody re-read this
paragraph. A register that describes its own tooling as absent invites the next author not to look.

⚠ **What IS still true, and is the real limitation:** there is **no CI** — the suite runs when
somebody runs it, on the build Mac (`npx vitest run`; node is not installed on the Windows dev box,
runbook §Frontend build). So a twin is pinned only if its author actually executed both halves, and
the honest test for any row here is *"were the TypeScript vectors watched passing?"* — not *"does a
test file exist?"* ⚠ **And no test mounts a component**, which is how the checkout hooks violation
(web 1.13.0) shipped past 205 green tests; `react-hooks/rules-of-hooks` in `npm run build` is what
covers that, not vitest.

### What the server catches regardless — and what it does not

The real safety net is that ingest refuses malformed sales, so a divergent client is usually caught
at the door rather than in a return three months later.

**`SaleV2.Validate()` rejects a sale unless:**
1. it has at least one line;
2. every line's `LineGrossPence == unit × qty − discount`;
3. `GrossPence == Σ line gross`;
4. `VatPence == Σ line VAT`;
5. net tender (`Σ amount − Σ change`) `== GrossPence`.

**`VatRateHistory.Assess` additionally** judges each line's *price pair* against the bands in force
at the moment of sale, and quarantines a till trading on a superseded rate.

⚠ **The gap those leave.** Validation checks that the header agrees with the lines — it does **not**
check that a line's `vatAmountPence` is the *right* figure for its price pair. A till that computed
VAT by rate arithmetic instead of `gross − ex` would be internally consistent, pass all five
invariants, pass the band check, and be wrong by a penny on about a third of standard-rated lines.
**That is the precise shape of the bug shared code prevents and validation does not.**

---

# Part D — Working with this

## D1. Everything now has a work package — closed 2026-08-08

**This section used to list nine items as "not yet in any work package". All nine now have a WP body
and a Definition of Done**, following Matt's parity instruction (see the binding rule).

The audit that closed it is worth remembering, because the failure was subtler than a missing plan.
Six of the nine had been *ruled in* by the retrofit plan's §3a table months ago — and the register
still said "not in any WP" because **nobody had noticed the ruling and the specification were
different documents**. The other three had a ruling and no body: real gaps hiding behind a tick.

| # | Item | Was | Now |
|---|---|---|---|
| 1 | Gift cards | ruled in, fully specified — **register was stale** | WP13 |
| 2 | Portal-controlled receipt templates | ruled in, fully specified — **register was stale** | WP3 |
| 3 | Users / employee management | ruled in, specified; **DoD missed list + set-password** | WP8 |
| 4 | Announcements · Help/support tickets | ruled in, fully specified — **register was stale** | WP5b |
| 5 | Refund-only baskets | ruled in, fully specified — **register was stale** | WP3 |
| 5 | Add-unknown-item | ⚠ ruled into WP10 and **never written into its body** | WP10 |
| 6 | The Bin + untracked stock | ⚠ ruled into WP10 and **never written into its body** | WP10 |
| 7 | Pick-from-floor notifications | ruled in, fully specified — **register was stale** | WP5b |
| 8 | Theming (pushed, not just the palette) | ⚠ WP7 said "port the palette"; **would have closed without it** | WP7b |
| 9 | Web-till test runner + `basketTotals` pin | ⚠ never ruled either way | **WP15** |

**The same audit found seven more capabilities with no WP body at all** — they were not on the D1
list because D1 was built from B-section Notes, and these had Notes that *named* a WP. Homed now:
MAUI's VAT-bands consumption → **WP5** (the most dangerous: the contract shipped, the client helpers
were built, and no WP told anyone to call them), payment-gateway awareness → **WP14** (its Notes
cited "WP17.2", which is not a work package in this plan), member-number scan-to-attach → **WP12**,
and the shared item-search matcher → **WP5** (it was attributed to WP1, which shipped without it).

**Four more were specified but gated by no DoD**, which is the same gap wearing a better disguise:
the VAT-band consistency guard (WP10), `426` handling (WP5), `LineMeta.vatBand` (WP3) and the
un-enrol request/approval round trip (WP4). All four are gated now.

> ### ⚠ The lesson, since it will recur
>
> **A ruling in a table is not a specification.** The retrofit plan's §3a is a decision log; §5/§6
> are the only half anyone builds from. When this register cites a WP, cite the **body**, and if the
> body does not describe the work, the item is not homed — however many tables say it is.

### Still genuinely open — Matt's call, not an oversight

- **WP15's timing.** The web till has no test suite at all, so every cross-language pinning test in
  C1 holds only its .NET half. It is scheduled; whether it lands before or after the MAUI UI work
  is a judgement about risk appetite.
- **A `TokenEpoch` revocation claim** (retrofit §9.8). There is no server-side session revocation
  for any principal — revoking a device or resetting a password stops the *next* sign-in and does
  not eject a live 12h token. The fix is a column, a claim and a comparison. Recommended, unscheduled.
- ~~**Whether `ATestController` should exist.**~~ **Deleted 2026-08-08.** `GET /api/ATest/testTransaction`
  was anonymous, in production routing, and **wrote a `Business` row** on every call. Nothing
  referenced it. The durable fix is `AnonymousEndpointTests` (C1) — every anonymous endpoint is now
  a reviewed line in a test, so the next one is a red build rather than an audit finding.

## D2. Adding a till

1. Reference `Plutus.Contracts.Client` + `Plutus.Client.Core` (+ `.Storage` if it trades offline).
   Do not re-implement the wire contract, the outbox, or the VAT arithmetic.
2. Read the portal contracts in C1 and cache them on the sync cadence. Hold no rates, no receipt
   layout, no colours of your own.
3. Build sale lines with `VatLineMath`. If you find yourself writing `gross × bp / (10000 + bp)`,
   stop — that disagrees with the receipt the customer is holding.
4. Populate `LineMeta.itemIdOne` on every line. `StockProjectionConsumer` **silently skips** lines
   without it: accepted ≠ stock moved.
5. **Give it its own version file** — `versions/till-<platform>.txt`, and point the project at it
   with `<PlutusVersionFile>`. A new till gets its own release history from day one, because the
   reason it exists is that it will diverge; sharing another till's number guarantees that the
   first platform-specific fix mislabels both. Add it to `ComponentVersionTests.Components`.
6. Add its column to Part B and its rows here.

## D3. Adding a feature, or a rule

**A feature ships to every till.** That is binding rule 1, and this is the procedure that makes it
mechanical rather than a matter of remembering:

1. **Write the Part B row before the code.** All till columns, filled — not just the one you are
   building. Seeing three empty cells next to your ✅ is the point of the exercise.
2. **Put the logic where every till can reach it.** If it computes money, resolves a rule, or
   decides what the operator may do, it belongs in `Plutus.SharedKernel` or `Plutus.Client.Core`,
   not in the UI project you happen to be editing. Then add its C1 row.
3. **Every till that isn't getting it now needs a WP number in Notes.** Not "TODO", not "later" —
   the work package in [`MAUI-retrofit.md`](To%20do/MAUI-retrofit.md)
   that will close it. If no WP covers it, add one; a plan is cheap and an unrecorded gap is not.
4. **A ⬜ needs a reason, not just a number.** "Online-only by design" and "no MAUI model exists
   for this yet" are reasons. An empty Notes cell is how the July drift happened.
5. **➖ is for genuinely inapplicable**, and say why — a browser has no first-run setup.
   ⚠⚠ **AND ➖ GOES STALE LIKE EVERY OTHER MARKER — RE-READ IT WHEN THE ARCHITECTURE MOVES.** This
   rule's own second example used to be *"MAUI drives OPOS directly so it has no hardware agent to
   report on"*. That stopped being true on **2026-08-10**, when MAUI moved onto the agent's print
   route — and the ➖ survived until **2026-08-17**, while a row eight lines further down recorded the
   very move that falsified it. A ➖ reads as a decision nobody need revisit, which is exactly what
   makes it the most dangerous marker in the register: ✅ and ⬜ both invite a check, and ➖ invites
   none. **When a till changes how it does something, grep the register for ➖ before anything else.**

**A rule:** put it in `Plutus.SharedKernel` and add a row to C1. If it must exist twice, add a row
to C2 saying what pins the copies — or, honestly, that nothing does.

⚠ **The failure this prevents is not hypothetical.** The web till gained nine features in eight
days at the end of July and MAUI silently fell behind on all nine, because each one was "finished"
when it worked in the browser. Parity is not a phase of the retrofit that ends; it is the standing
condition every till commit has to leave true.

## D4. ⚠⚠ THE DIALOG CONTRACT — every box an operator can open

> **Matt, 2026-08-18:** *"if I add an item to the till and want to adjust it, the box it pops has no X
> to close the box. I know you can click outside of the box to close it but its not intuative"*, then
> *"add x's to all relevant boxes and write it into the till-design.md so that it is not missed in
> future"*.
>
> **This section is that rule.** It exists because a dialog is the one control that can trap an
> operator: a screen with no visible exit, in a queue, with a customer waiting.

### The five rules

Every dialog on every till, no exceptions:

| # | Rule | Why |
|---|---|---|
| **1** | **A visible ✕, top-right.** | Backing out already worked on every dialog in this codebase — Escape/back always cancels, and tapping outside cancels on most. **What was missing was any way to KNOW that.** An exit nobody can see is not an exit |
| **2** | **Escape / the back button always cancels.** | `AlertDialogBase.OnBackButtonPressed` — it once returned `true` unconditionally and swallowed both |
| **3** | **Tapping outside cancels**, unless the dialog is deliberately modal (`interuptable: false`) — and if it is, rules 1 and 2 are the only exits, so they must work | A 40%-black scrim over a till with no way out is a dead counter |
| **4** | ⚠⚠ **THE CALLER HANDLES "BACKED OUT" — and this is the money rule.** A cancelled dialog yields *no answer*, and the caller must treat that as "the operator changed their mind", never read a value out of it | ⚠⚠ **CORRECTED 2026-08-19 — THIS ROW SAID "returns null" AND THAT IS NOT WHAT THE CODE DOES.** `InputAlertHelper.ShowAsync` ends `return await popUp.PageClosedTask ?? new Dictionary<uint, string>();` — `await` binds tighter than `??`, so backing out yields an **EMPTY DICTIONARY**, and the method's own header comment says so. So **guard on `Count == 0` / a failed `TryGetValue`, never on `== null`**: a `== null` test never fires and is dead code. ⚠ The crash is real but it is not a `NullReferenceException` — reading an absent key throws `KeyNotFoundException`, and in an `async void` handler that still **kills the till**. ⚠⚠ **The worse case is where nothing throws at all**: a caller that reads the empty dictionary through `FirstOrDefault()` gets a zeroed struct and proceeds with a **silent zero** — on a cash movement or a price adjust that is a wrong number rather than a crash. See `MAUI-retrofit.md` §0.3b |
| **5** | **The ✕ is wired to CANCEL, not to the raw dismiss.** | Not cosmetic. On MAUI the two return *different things*: Cancel blanks the fields and returns a dictionary of nulls that every caller's `if (x != null)` guard already survives; the raw dismiss returns **null**, which is rule 4's crash. Wiring the ✕ to Cancel gives the operator a visible exit **without widening the reach of a bug that is still open** |

### Where it is implemented — one place per till, on purpose

| Till | Implementation | Covers |
|---|---|---|
| **MAUI** | `Plutus/Shared/CustomViews/DialogHeader.cs` — `DialogHeader.For(title, onClose)` returns the title row with the ✕ | `InputAlert`'s three constructors, so **all 24 call sites** and the **Alterations** dialog (which inherits from it) |
| **Web till** | `src/DialogX.tsx` + `.dialog-x` in `index.css` | The 8 overlay dialogs: users, inventory item, loyalty member, sale detail, checkout, discount, parked baskets, return |
| **Portal** ⚠ **added 2026-08-20 — see below** | `src/DialogX.tsx` (byte-identical twin of the till's, pinned by `FrontendTwinTests`) + `.dialog-x` in `portal.css` | ✅ **COMPLETE 2026-08-21.** `Ask.tsx` — so **every** confirm / choose / prompt in the portal — the **item editor**, and all **21** bespoke dialogs across **12** files. The check command comes back empty |

⚠ **Both are single helpers so a new dialog cannot quietly ship without a ✕** — which is exactly what
happened before: three MAUI dialogs each hand-built their own title `Label`, and when a fourth arrived
there was nothing to notice the omission. **If you add a dialog, use the helper.** If you find yourself
writing a title label by hand, that is the smell.

⚠ **`type="button"` on the web ✕ is load-bearing.** Several of those dialogs are `<form>`s, and a bare
`<button>` inside a form defaults to `type="submit"` — which would turn Close into Confirm. On
`CheckoutDialog` that means taking money.

### ⚠ What is NOT yet true, stated honestly

- ⚠⚠ **THE PORTAL WAS NEVER IN THIS CONTRACT AT ALL, AND NOBODY NOTICED FOR AS LONG AS IT HAS EXISTED
  (found 2026-08-20).** This section is headed *"every box an operator can open"*, and its
  implementation table listed **two** surfaces — so it read as complete while a third surface, which the
  same people use every day, had **no `DialogX.tsx`, no `.dialog-x` style, and not one ✕ in fifteen
  files containing dialogs**. Matt's instruction was *"add x's to all relevant boxes … so that it is
  not missed in future"*; the portal's boxes are relevant and were missed.
  - **Found by accident.** Not by this contract, and not by a review of it — by the twin-file guard
    added for the multi-barcode work (C2), which showed the portal's `Ask.tsx` differing from the web
    till's by exactly one import: `DialogX`.
  - ✅ **CLOSED 2026-08-21.** All **21** dialogs across **12** files now carry the helper —
    `CategoryManager` (2), `CustomerDialog`, `CustomersPage`, `GiftCardsPage` (4), `LoyaltyPage`,
    `MemberCard`, `PricesPage`, `SaleDialog`, `StockPage`, `TierManagerDialog` (2), `UsersPage` (5),
    `WebstorePage` — on top of the `Ask.tsx` host and the item editor closed on 2026-08-20. Each ✕ is
    wired to **that dialog's own overlay close action**, derived from it rather than assumed, so the
    six that refuse to close while busy (`disabled={busy}`) still refuse, and the four that close a
    local piece of state (`setCreating(false)`, `setAdding(false)`, `setBindFor(null)`) do exactly
    that. **The check command below now comes back empty on all three surfaces.**
  - ⚠⚠ **AND THE LIST OF 13 WAS WRONG BY ONE, BECAUSE THE CHECK COMMAND WAS.** `ReportPublicationSection`
    has **no dialog at all** — it is an inline page section that borrows the `.dialog-actions` button
    row for its Save buttons. `grep 'className="dialog'` prefix-matches `className="dialog-actions"`,
    so the command reported a file with nothing to fix and put it on this list. **The pattern below is
    corrected to `className="dialog("| )`.** ⚠ Worth keeping: the same prefix match, in a script
    written to do this insertion, put a ✕ above **every button bar in the portal** on its first run.
    A check that over-reports is not the safe direction — it teaches you to ignore its output.
  - ⚠ **The lesson is about the table, not the ✕**: a contract that enumerates its own surfaces is a
    contract that silently excludes the ones nobody added. **If you add a surface, add its row here.**
- ✅ **Rule 4 — CLOSED 2026-08-21. This contract is now 5/5 honoured on every reachable dialog.**
  ⚠⚠ **It was never "17 places", and that number outlived its own correction by two days.** The
  2026-08-19 re-audit in `MAUI-retrofit.md` §0.3b already said so; this line, §0.3's table and §7's
  first row all kept the 17. **Re-enumerated by grep on 2026-08-21: 23 `LaunchInputAlertAsync` call
  sites, and every one an operator can open returns on `Count == 0` or a checked `TryGetValue`** —
  including all five named here (refund, gift-card sale, cash, the supervisor prompt, checkout).
  The residue closed the same morning: five **dead `answers is null` checks** (the helper ends
  `?? new Dictionary<…>()`, so null never arrives and those five only appeared to work because a
  whitespace validator ran after them) and `ViewAllViewModel.ExecuteUpdateItemStock`, where
  `Any(…)` over an empty dictionary is **false** and the cancel path fell through to
  `int.Parse(null)` **after** `db.Add(stock)` had already written a row.
  - ⬜ **What remains is 4 unguarded sites in `CopperTransferPlatform`, all UNREACHABLE** —
    `ICopperTransfer` is registered in `MauiProgram` and nothing resolves it. Left alone on the same
    reasoning as `SliderAlert` below: **delete the tool or wire it up.**
  - ⚠ **The lesson is the one this page keeps re-learning**: a count written in prose goes stale
    silently while the code it describes moves on. This line said 17 for three days after the number
    became 0, and it sat at the top of a do-first list the whole time.
- **`SliderAlert` has no ✕ and did not get one: it is dead code** (zero references in the solution).
  Recorded here rather than fixed, because adding chrome to an unreachable dialog is how dead code
  starts looking maintained. **Delete it or wire it up.**
- ⚠ **Native `DisplayAlert`/`DisplayActionSheet` are out of scope** — the platform draws those and they
  come with their own dismissal. This contract governs the dialogs *we* build.

### The check, when you touch a dialog

```bash
# MAUI — a hand-built title Label in a dialog means a missing ✕
grep -rn "MainLayout.Children.Add(new Label" Plutus/Frontend/Plutus.Frontend.AppClient/Views/CustomViews/

# Web till — every .dialog should have a DialogX.
# ⚠ `dialog("| )`, NOT a bare `dialog` prefix: that also matches `dialog-actions`, the button-row
# style used by page sections that are not dialogs at all. It reported one for months (2026-08-21).
grep -rlE 'className="dialog("| )' Plutus/Frontend/Plutus.Frontend.WebApp/src --include=*.tsx |
  xargs grep -L DialogX

# ⚠ THE PORTAL TOO — the surface this contract forgot until 2026-08-20. Run it on all THREE
# surfaces or it keeps reporting only the ones you remembered. ✅ Empty since 2026-08-21.
grep -rlE 'className="dialog("| )' Plutus/Frontend/Plutus.Frontend.Portal/src --include=*.tsx |
  xargs grep -L DialogX
```

## D5. ⚠⚠ THE LIVE-DATA CONTRACT — every screen that shows a number

> ⚠⚠ **Written 2026-08-18 because the same fault was reported three times and fixed correctly three
> times.** 2026-08-11: *"The open float was 'Waiting' and never updated. I navigated away and back
> onto the cash tab and it had updated."* Then finding N: today's takings read at sign-in, and a full
> day of trading never moving them. Then §5c item 7, as a general statement: *"Nothing updates unless
> you navigate away and back."*
>
> Each fix was right and each was **local**, so the next screen inherited nothing. **A documented
> pattern is what failed** — which is why the rules below name a shared piece of code rather than a
> habit.

**The rule.** A screen that displays data the shop can change must reload it when the operator looks
at it, and — if it shows a small live figure — keep reloading while they are looking. A screen that
loads in its constructor and never again is **wrong on every till**, because it is the constructor
that is the trap:

⚠ **`AppShell` builds every tab up front, so a viewmodel constructor runs ONCE — at sign-in.** On the
web till the equivalent is a `useEffect(…, [])` with no revalidation. Both freeze the screen for the
session.

| # | Rule | Why |
|---|---|---|
| 1 | **Reload on appearing.** MAUI: `Services.Sync.LiveScreen`, two lines in the page constructor, no `OnAppearing` override. Web: the query's `refetch` on mount | Makes *"navigate away and back"* unnecessary — that phrase in a bug report **is** this fault |
| 2 | **A small live figure also reloads on the 60-second cadence** — the drawer, today's takings, a read-only detail card | *"(waiting to send)"* against money already sent is the reported case. ⚠ The screen must not need the operator to do anything |
| 3 | ⚠ **A long scrollable table does NOT tick** (`onCadence: false`) | Rebuilding a 500-row collection sends the view back to the top under somebody's hands: **a worse fault than the staleness, and self-inflicted.** It still reloads on arrival and on an explicit Search |
| 4 | ⚠ **Unsubscribe when the screen goes away** | `TillCadence.Ticked` is a **static** event. A page that stays attached is held alive for the life of the process with every query it makes each minute — and a till runs for a fortnight without a restart |
| 5 | ⚠ **The refresh marshals to the UI thread and cannot throw** | It arrives on a background clock. Writing an `ObservableCollection` off the UI thread passes every test and throws on a shop floor; an escape goes to the dispatcher unhandled, which kills the till |
| 6 | ⚠ **A background refresh is SILENT when it fails** | Nobody asked for it. A dialog raised from a tick lands on top of whatever the operator was actually doing — see the cadence's own header: *nothing here may ever surface a dialog* |
| 7 | ⚠ **Not for modal forms or add/edit pages** | There is nothing to restate, and reloading under somebody mid-type is a fault |

**Where it lives:** `Plutus/Frontend/Plutus.Frontend.AppClient/Services/Sync/LiveScreen.cs` (MAUI).
⚠ **There is deliberately no per-screen alternative** — Cash and Statistics were rewritten onto it
*although they already worked*, so that the rule has no exceptions. An exception is how the third
report happened.

⚠⚠ **THE DANGEROUS STALENESS IS THE SILENT KIND.** A stuck *"(waiting to send)"* gets reported within
the hour. **A takings total eight hours old looks exactly like a correct one**, and it is the number a
manager counts a drawer against. When you find one stale screen, go and look for its siblings in the
same session — and the sibling that bites is the one with *no refresh code at all* to notice, which is
how Store Information sat un-refreshable for months.

⚠ **None of this can be unit-tested on MAUI.** A `Page` cannot even be constructed in the test project
(`BindableObject`'s constructor needs a live WinUI3 dispatcher — it is why three tests are skipped).
`Test Maui.md` **§G39** is the only check that exists, and it says so at the top.

## D6b. ⚠⚠ THE PRINT-ROUTE CONTRACT — a receipt goes to the RECEIPT printer

> **Matt, 2026-08-21:** *"On the webtill … if you complete a sale and print no receipt, the receipt is
> shown (do not change) but if you try to print it from the next screen, it comes out on the A4
> printer, not the receipt printer. It should always default to the receipt printer."*

**The rule:** any control that prints a **receipt** tries the hardware agent first and uses the
browser's own print path only as a fallback. `window.print()` goes to the **OS default printer**,
which in a shop is the office A4 — so a customer's receipt ends up in another room, and the operator
finds out when nothing comes out of the till.

### Where it is implemented — one place, on purpose

| Surface | Implementation | Callers |
|---|---|---|
| **Web till** | `src/till/receiptPrint.ts` — `printOnReceiptPrinter(data)`; returns **false** for "nothing came out" and never throws | `till/Receipt.tsx` (the Print button), `till/TillPage.tsx` (post-sale), `reporting/SaleDetailDialog.tsx` (copy reprint) |
| **MAUI** | `Services/Printing/TillAgentPrinting` + `ReceiptReprint`, with the OPOS/PDF route as the documented fallback | checkout, reprint |

⚠ **The helper owns the DECISION, not the fallback.** The three web callers legitimately differ in
what they do when no paper comes out — the receipt dialog raises the browser dialog, the sale-detail
dialog renders a browser receipt, the post-sale path shows the receipt with auto-print armed. Folding
those together would be the wrong kind of sharing.

⚠⚠ **`openDrawer: false` on every path through it.** The drawer is kicked once, by `TillPage`, when a
cash sale commits. Everything else here is a reprint or an afterthought receipt, and neither moves
money — a drawer that opens when nothing is happening teaches operators that the drawer opening means
nothing.

⚠ **`(COPY)` is the CALLER's business.** `SaleDetailDialog` stamps it, because this platform finds a
sale to refund by the barcode on a receipt and two indistinguishable papers is the shape of a double
refund. The receipt dialog must **not** stamp it — the operator declined the ask, so its paper is the
first one.

### ⚠ What is NOT a receipt, and must keep using the browser

- **The membership card** (`CustomerDetail.tsx`, portal `MemberCard.tsx`) — a **CR80 card**, 85.6 ×
  54 mm, deliberately rendered to an ordinary page printer (§5d WP-L1). MAUI prints a thermal *slip*
  instead, because no page printer exists behind it. **`window.print()` is correct there**; "fixing"
  it would send card stock to a receipt roll.
- **Reports and A4 documents.** Nothing in this contract touches them.

### ⚠⚠ Why this section exists at all — the lesson is about copies, not printers

**The fix already existed twice when the bug was reported.** W-P6 closed the identical fault in
`SaleDetailDialog` on **2026-08-17** — its own header reads *"this dialog could already reprint, but
only through `window.print()`"* — and `TillPage` had the agent-first logic from FE3.3. So there were
**three inline copies of one rule, one of them wrong, and nothing pointing between them.** The wrong
one survived four days after the right one was written, in a file three directories away.

**A rule that is correct in two places out of three is not a fixed bug — it is a fixed instance.**
When you find a fault of this shape, grep for the *other* copies before closing it, and if there is
more than one, make it one. `printOnReceiptPrinter` is tested (`receiptPrint.test.ts`) precisely
because it is now the only copy.

### The check, when you touch a print path

```bash
# Every browser-print call on the web till. Each ONE must be either a documented fallback or a
# non-receipt document (the membership card). A bare window.print() on a receipt control is the bug.
grep -rn "window.print()" Plutus/Frontend/Plutus.Frontend.WebApp/src --include=*.ts --include=*.tsx
```
## D6. Keeping this true

- **Verify against code, not memory.** Every ⬜ in Part B was checked by grepping both codebases;
  that is the standard, and it took under an hour.
- **C2 is the section that rots dangerously.** Parts A, B and C1 go stale *visibly* — a project
  appears, a contract moves, someone notices. C2 goes stale *invisibly*: a twin quietly added, or a
  pinning test quietly deleted, looks exactly like a document that is still true. So: **when you
  make a rule exist in two places, or stop one existing in two places, say so in C2 in the same
  commit.**
- **The API is the mechanical backstop.** Endpoint usage can be diffed without judgement:

  ```bash
  # every endpoint the web till calls — MUST exclude src/api/types.gen.ts: it is a GENERATED
  # OpenAPI type file whose path string-literals are not calls (including it injects ~30
  # endpoints the till never touches: purchase-orders, tenants, suppliers, roles, periods…)
  grep -rhoE '"/api/[^"`]*|`/api/[^`"$]*' Plutus/Frontend/Plutus.Frontend.WebApp/src \
    --exclude=types.gen.ts | sed 's/^[`"]//; s/[?&].*//' | sort -u
  ```

  Run the equivalent over the MAUI client once WP1 lands and diff the two lists. An endpoint one
  till calls and the other doesn't is either a missing row in Part B or a deliberate ⬜ — never a
  surprise. Two caveats: about a third of the web till's calls are still **legacy `/api/*`
  controllers** (Item, Employee, SavedTransaction, Auth/Login…), so a diff against a
  `/api/v1`-only MAUI client must not read those as parity gaps; and the diff won't catch
  behavioural drift (C2), which is why C2 exists as prose.

- ⚠ **A ✅ MEANS A PERSON HAS USED IT — added 2026-08-10, and it was learned the hard way.** Three
  rows in this register have now been found ✅ against code that could never work: **Inventory CRUD**
  (wrote to a database nothing reads, on a till with no item-write endpoint), the **item search**
  the till never called, and the **outbox drain** that was referenced nowhere in the app. Each was
  fully built and had passing tests; what none of them had was a caller and a human.

  So before writing ✅, ask two questions that a test suite cannot answer for you:

  1. **Who calls this?** `grep` for the entry point. A green suite proves a component works, never
     that anything uses it. This is the single most common way a row here becomes a lie.
  2. **Has anyone driven it?** If the capability has a screen, someone must have used that screen.
     If not, it is 🟡 — and the Notes say "built, not yet driven".

  ⚠ **The failure mode is specific and nasty**: a ✅ that reads as built stops anyone looking, so
  the gap survives every subsequent review of this document. A 🟡 costs nothing and is honest.

---

## ⚠⚠ A0 POSTSCRIPT — what a person found on 2026-08-18 that 1,946 tests did not

Matt ran the MAUI till against the web till and reported **nine** faults in one sitting. Every one had
passed every automated check in this project. They are itemised as a work programme in
[`MAUI-retrofit.md`](To%20do/MAUI-retrofit.md) **§5c**; what belongs *here* is what they say about this
document:

- **🟡 meant less than it reads.** Reports, Loyalty, Settings and checkout were all 🟡 — *"built,
  tested where a machine can reach, never seen by a human."* In practice that turned out to mean
  **the data arrives and the screen is wrong**: reports missing half the web till's set and all of its
  drill-down; a Loyalty tab with no way to add or edit a member; a save that **fails silently** when a
  mandatory field is empty; screens that never refresh unless you navigate away and back.
- ⚠ **Two rows were ✅ and should not have been.** "Park a basket and recall it" is now 🟠: the save
  works, and the *retrieve* is a toolbar item labelled "Baskets" that the person using it could not
  find. A capability nobody can reach is not a capability — the same lesson as the refund button
  (finding Z1) and the reprint (§5b W-P6), for the third time.
- ⚠ **One row was mine and wrong.** I argued the customer Search / Add-member buttons belonged on the
  till screen. They are on neither the web till nor NatApp, and a member is attached by **scanning
  their card** on both. Removed. **When Matt says a screen does not match, check the other screen
  before defending ours.**
- ⚠ **A capability was claimed that was never built.** "Reports approved in the portal are pushed to
  each till" does not exist anywhere in the solution — no approval, no publish, no push. Step 26 gave
  each till its *own* reports over platform data, which is a different thing. **Nothing in Part B
  claimed it; the belief formed anyway**, which is worth more caution than a stale marker: an
  unwritten assumption cannot be corrected by grep.

**The standing conclusion.** Part A0 and Part B are honest about *whether code exists*. They are
**silent about whether a person can use it**, and this review is the fourth demonstration that the gap
between those two is where the faults live. ⚠ Only [`Test Maui.md`](Test%20Maui.md) closes it, and it
has still never been run start to finish.
