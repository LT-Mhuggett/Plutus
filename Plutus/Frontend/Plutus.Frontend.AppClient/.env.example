# Copy this file to ".env" (same folder) and fill in your New Relic license key.
# ".env" is gitignored - never commit your real key.
#
# Debug builds only load .env; Release builds always require a real environment
# variable (see Services/Analytics/OtelSettings.cs and MauiProgram.cs).

NEW_RELIC_LICENSE_KEY=
