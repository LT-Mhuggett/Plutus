<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Plutus.Frontend.ClientUI.Pages.MainTill.Statistics.StatisticsPage"
             Title="{Binding Title}">
    <ContentPage.IconImageSource>
        <FontImageSource FontFamily="FASolid" Glyph="{Binding Icon}" Size="20" Color="Red" />
    </ContentPage.IconImageSource>
    <StackLayout>
        <ScrollView>
            <Grid Margin="5,0,5,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>
                <StackLayout x:Name="LeftColumn" Grid.Column="0" Grid.Row="0"/>
                <StackLayout x:Name="RightColumn" Grid.Column="1" Grid.Row="0"/>
            </Grid>
        </ScrollView>
    </StackLayout>
</ContentPage>
