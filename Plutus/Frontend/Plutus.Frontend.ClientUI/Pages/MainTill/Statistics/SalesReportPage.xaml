<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Plutus.Frontend.ClientUI.Pages.MainTill.Statistics.SalesReportPage"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             ios:Page.UseSafeArea="True"
             Shell.NavBarIsVisible="{OnPlatform True, MacCatalyst=False}"
             Title="{OnIdiom {Binding Title}, Desktop=''}">
    <StackLayout>
        <Label Text="Generate sales report from server"/>
        <StackLayout Orientation="Horizontal">
            <StackLayout>
                <Label Text="Start Date"/>
                <DatePicker Date="{Binding StartDate}"/>
            </StackLayout>
        </StackLayout>

        <StackLayout Orientation="Horizontal">
            <StackLayout>
                <Label Text="End Date"/>
                <DatePicker Date="{Binding EndDate}"/>
            </StackLayout>
        </StackLayout>
        <Button Text="Generate Report" Command="{Binding GenerateReportCommand}" />
    </StackLayout>
</ContentPage>
